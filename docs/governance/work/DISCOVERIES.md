# سجلُّ الاكتشافات — DISCOVERIES

## الهدف: منعُ ضياعِ المعرفةِ التي تظهرُ أثناءَ العمل. كلُّ ما يُكتشَفُ خارجَ نطاقِ البندِ يُقيَّدُ هنا `DISC-###` **ويُوجَّهُ** إلى مالكِه، فلا يبقى في رأسِ عاملٍ ولا يُدفَنُ في تعليقِ شِفرة
## النطاق: قيدُ الاكتشافِ ودليلُه وأثرُه **ووجهتُه**. لا يُحسَمُ هنا ما يحتاجُ قرارًا بشريًّا، ولا تُصلَحُ هنا شِفرةٌ؛ الاكتشافُ يُوجَّهُ فيصيرُ بندَ عملٍ أو سؤالًا أو خطرًا أو قيدَ دَين
## المالك: قائدُ التنفيذ
## تاريخ الإنشاء: 2026-08-25
## تاريخ آخر تعديل: 2026-09-13 (‏`W-161` — **الجولةُ الخامسةُ من مجلسِ المراجعةِ المستقلِّ ردَّت البنودَ الستَّةَ كلَّها**: أربعةٌ بإجماعٍ (`WI-023` · `WI-024` · `WI-032` · `WI-038`) واثنانِ بانقسامٍ (`WI-035` · `WI-037` — المراجعُ أ `VERIFIED` والمراجعُ ب `REJECTED`)، فلا `VERIFIED` لبندٍ واحدٍ والإجماعُ شرطُ `Q-43`؛ فقُيِّدَ `DISC-063` باتّحادِ العيوبِ، و`DISC-064` بهُويّةِ الأثرِ المولَّدِ المقروءةِ من `origin` — وبنودُ `READY` الخمسةُ تبقى محجوبةً) · وقبلَه 2026-09-13 (‏`W-160` — **حكمُ CI قِيسَ دالّةً في الزمنِ لا في الشجرةِ وحدَها**: عقدةُ `97e9acd` قُرِئَت خضراءَ 14/14 يومَ 2026-09-11 وصارَت حمراءَ بستِّ مخالفاتِ `DUE_DATE_PASSED` يومَ 2026-09-13 بلا تغييرِ حرفٍ فيها، فقُيِّدَ `DISC-062` ووُجِّهَ إلى حرسٍ يقيسُ عددَ التجديداتِ · **وستُّ استحقاقاتٍ مضَت جُدِّدَت إلى 2026-09-30 بسببٍ مكتوبٍ في صفِّ كلٍّ منها ولم يُمحَ استحقاقُها الأوّلُ** — والتجديدُ رفعُ حاجزٍ زمنيٍّ لا إنجازٌ) · وقبلَه 2026-09-06 (‏`W-138` — السببُ الجذريُّ لعودةِ `DISC-006` صارَ مقيسًا من سجلِّ استهلاكِ الحسابِ ‏2674 دقيقةً مقابلَ مُعلَنٍ منشورٍ 2000، وعبورُ الحدِّ خلالَ 2026-09-03 وهو عينُ حدِّ العَطبِ الزمنيِّ — فرُفِعَ حدُّ القياسِ المُعلَنُ في `W-137` بلا محوِ نصِّه، وقُيِّدَ `DISC-057`، وصارت إشارةُ `RK-011` المبكِّرةُ رقمًا يُقاسُ قبلَ سقوطِ وظيفةٍ. **والحاجزُ باقٍ بيدِ المالكِ ولم تُرقَّ حالةٌ**) · وقبلَه 2026-09-05 (`DISC-006` — **عادَ مقيسًا** بـ`W-137`: صفرُ عاملٍ وصفرُ خطوةٍ في وِردَي عقدةِ `febb558` (‏تشغيلا **95** و**96**)، فحالتُه `مفتوحٌ` لا `مُعالَجٌ` و`RK-011` `مرتفعٌ` — والوجهةُ بيدِ المالكِ) · وقبلَه 2026-09-05 (`DISC-056` — قُيِّدَ بـ`W-136`: قارئُ حكمِ CI يستدعي `/runs/{id}/jobs` بلا تثبيتِ محاولةٍ والواجهةُ تُرجِعُ آخرَها وحدَها، فحكمُ التشغيلِ **94** انقلبَ من `READABLE · 13/13` إلى `UNREADABLE · 0/10` بإعادةِ تشغيلٍ بنيويّةٍ **بلا تغييرِ حرفٍ في الشجرةِ ولا في الرقمِ المُحالِ إليه** · وحرسُه في `WI-048` · والخطرُ `RK-022`) · وقبلَه 2026-09-03 (`DISC-052` — قُيِّدَ بـ`W-129`: بوّابةُ أحجامِ الحزمِ تُعلِنُ حكمًا أوسعَ مِمّا قاسَت — رقمُ حزمةِ الجذرِ مكتوبٌ بصيغةٍ لا يقرؤها قارئُها فتقادَمَ **2398 مقابلَ 2421** بلا حمرةٍ، فصُحِّحَ الرقمُ ووُجِّهَ الحدُّ إلى `WI-044`) · وقبلَه 2026-09-03 (`DISC-051` — قُيِّدَ بـ`W-129`: رباطُ وجهِ `--check` بالمستودعِ الحقيقيِّ قائمةٌ بخطِّ اليدِ، فسبعةُ مُولِّداتٍ من إحدى عشرةَ بلا رباطٍ مقيسٍ · وحرسُه في `WI-043`) · وقبلَه 2026-09-03 (`DISC-050` — قُيِّدَ بـ`W-128`: بوّابةُ سرِّ الملكِ في CI غيَّرَت حكمَها على العقدةِ نفسِها بلا تغييرِ حرفٍ، وسببُها لم يُقرَأْ لأنَّ سجلَّ الوظيفةِ يُحوَّلُ إلى مُضيفٍ محجوبٍ · وحرسُه في `WI-042`) · وقبلَه 2026-09-03 (`DISC-049` — قُيِّدَ بـ`W-127`: طابعُ الالتزامِ المُنشَأِ بواجهةِ Git Data يرثُ إزاحةَ الدافعِ المحلّيّةَ ووجهُ القراءةِ يعرضُها `Z`، فسقطَ فحصانِ في تشغيلِ CI **84** وصُحِّحَ الأثرُ بإعادةِ إنشاءِ الالتزامِ · وحرسُه في `WI-041`) · وقبلَه 2026-09-03 (`DISC-048` — قُيِّدَ وأُصلِحَ موضعُه بـ`W-126`: قاسمُ الخلايا كانَ يرفضُ النقصَ ويُمَرِّرُ الزيادةَ، فصفّانِ بتسعِ خلايا يُحكَمُ عليهما بعمودٍ مُزاحٍ بلا مخالفةٍ — فصارَ العددُ مُلزَمًا في الجهتَينِ و`\|` محتوًى لا حدًّا) · وقبلَه 2026-09-03 (`DISC-047` — قُيِّدَ وأُغلِقَ بـ`W-122`: الإعفاءُ البيئيُّ كانَ يُقاسُ باستيرادِ حزمةِ المستودعِ و`conftest.py` يُضيفُ مسارَ مصدرِها، فسقطَ CI 79 بنقصِ تبعيّاتٍ لا برقمٍ ميّتٍ — فصارَ السببُ يُقرأُ من خرجِ الجمعِ بأسمائِه) · وقبلَه 2026-09-03 (`DISC-046` — نزلَ حرسُه بـ`W-121`: عددُ الجمعِ صارَ مقيسًا حيًّا يُقارَنُ بكلِّ رقمٍ مكتوبٍ، والتاريخيُّ يُعفى بإعلانٍ منسوبٍ إلى قيدٍ لا بصمتٍ) · وقبلَه 2026-09-03 (`DISC-046` — أرقامُ حجمِ الحزمِ في «الحالةُ المقيسةُ الآن» مقيسةٌ متقادمةٌ بلا حرسٍ: المكتوبُ 1748 والمقيسُ 2351) · وقبلَه 2026-09-03 (`DISC-042` — نزلَ حرسُه بـ`W-118`: نثرٌ في خليّةِ المساراتِ يُرفَضُ صراحةً ولا يُقرأُ دعوى · 8 ← 0 والتغطيةُ لم تتبدَّلْ) · وقبلَه 2026-09-03 (`WI-036` — نزلَ حرسُ `W-115` فصارَ التباسُ ترقيمِ § 10 **رقمًا يُقاسُ لا يعلو**، ومِرساةُ `DISC-045` مسارَ حرسٍ قائمًا لا وعدًا) · وقبلَه 2026-09-03 (`WI-036` — `DISC-045`: الرقمُ 9 في § 10 من سجلِّ الإكمالِ مكتوبٌ **مرّتَينِ** لحدَّينِ مختلفَينِ فصارَت **7 إحالاتٍ** إلى «الحدِّ 9» لا تُحَلُّ إلى حدٍّ واحدٍ — مقيسٌ ووُجِّهَ إلى حرسٍ يمنعُ نموَّه، ولا يُعادُ الترقيمُ لأنَّه يكسِرُ إحالاتٍ في قيودٍ مدفوعةٍ) · وقبلَه 2026-08-31 (`WI-023` — دعوى «يُشغَّلُ في كلِّ دفعةٍ» صارت مقيسةً في بوّابةٍ فقُيِّدَ `DISC-037` وأُقِرَّ حدُّ `DISC-032`؛ وقبلَه `WI-022` — القياسُ الأوّلُ للفرعِ كانَ أضيقَ من الواقعِ فصُحِّحَ في `DISC-034`، وتصادمُ معرِّفاتِ السجلِّ بينَ الفرعَينِ قُيِّدَ `DISC-036`) · وقبلَه 2026-08-30 (`WI-021` — فرعٌ لم يُدمَجْ حملَ إصلاحًا لم يبلُغِ الفرعَ الحاكمَ `DISC-034`، وحالةُ بندٍ مكتوبةٌ مرّتَينِ متناقضتَينِ `DISC-035`) · وقبلَه 2026-08-29 (`WI-019` — قراءةُ الحكمِ كشفَت ثلاثةَ عُيوبٍ كانت مستورةً: سطرُ اختبارٍ `DISC-024`، وحرسٌ لا يُقاسُ على طلبِ دمجٍ `DISC-025`، وثلاثةُ فحوصٍ لم تُنفَّذْ `DISC-026`))

> **قاعدةُ الوجهة:** اكتشافٌ بلا وجهةٍ = `UNROUTED_DISCOVERY` تُسقِطُه البوّابة.
> وجدولُ الوجهاتِ المُلزِمُ في [`THE_ROADMAP.md § 11`](THE_ROADMAP.md).

---

## 1 · الاكتشافاتُ المُقيَّدة

| المعرِّف | الخطورة | موضعُ الظهور | ما اكتُشِف | الدليل | الأثر | الوجهة | الحالة |
|---|---|---|---|---|---|---|---|
| DISC-001 | P2 | `EXECUTION_PLAN.md` | الملفُّ يُصنِّفُ المراحلَ والمجالاتِ بحالاتٍ **ملغاةٍ** (`DONE`/`TODO`) أبطلَها تعريفُ الإنجازِ، ويُعلِنُ «9 من 9 مراحل DONE» و«12 من 12 مجالًا DONE» — وإن صدَّرَه تنبيهٌ يقصرُه على السجلِّ التاريخيِّ ويقرأُ كلَّ `DONE` فيه `DESIGNED` | سطرُ التنبيهِ 12 مقابلَ جداولِ 44–60 من الملفِّ نفسِه · و`TRUTH_MATRIX.md`: لا إقليمَ واحدًا `PROVEN` من اثنَي عشر | قارئٌ عابرٌ يبني قرارًا على «مكتمل» لا يقيسُه دليل | بندُ عملٍ في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) لتحويلِ مفرداتِه إلى حالاتِ تعريفِ الإنجاز · والخطرُ `RK-002` | مُعالَج بالبندِ `WI-002` (W-047) |
| DISC-002 | P3 | `requirements-*.txt` مقابلَ بوّاباتِ CI | `PyYAML` مُعلَنٌ في `requirements-tools.txt` وحدَه، وبوّاباتُ الحوكمةِ في CI تُثبِّتُ `requirements-dev.txt` | نتيجةُ البحثِ في ملفّاتِ التبعيّات · ووظائفُ `identity-check` و`ledger-gate` في `ci.yml` | أيُّ بوّابةٍ تستوردُ `yaml` تسقُطُ في CI لسببٍ غيرِ حقيقيّ | قرارٌ تصميميٌّ مُنفَّذ: بوّابةُ `check_work_governance.py` بالمكتبةِ القياسيّةِ وحدَها وسجلّاتُها جداولُ Markdown · والخطرُ `RK-010` | مُعالَج بالتصميم |
| DISC-003 | P1 | طبقةُ الحوكمةِ كلُّها | لا سجلَّ يُعلِنُ **حجزَ النطاقاتِ**: سجلُّ الإكمالِ يوثِّقُ الماضيَ، والمصفوفةُ تقيسُ القدرةَ، و`docs/audit/ACTIVE_EXECUTION_STATE.md` يقولُ **الأمرَ التاليَ لمسارِ التنفيذِ E2.x وحدَه** ولا يقولُ «هذا النطاقُ محجوزٌ الآنَ لفلانٍ حتى تاريخِ كذا» لبقيّةِ النطاقاتِ الثلاثةِ والعشرين — **تصحيحُ صياغةٍ أوسعَ من الحقِّ كُتِبَت أوّلًا** بعدَ الاطّلاعِ على الملفِّ القائمِ منذ 2026-08-16 وعلى `docs/PROJECT_HANDBOOK.md` (W-016) | قيدُ `W-024`: خارطةٌ أرسلَت عاملًا إلى عملٍ كان مُنجَزًا قبلَ اليومِ فأُهدِرَ الجهد | تكرارُ عملٍ بينَ جهتَينِ لا تعرفُ إحداهما الأخرى | البندُ `WI-001` في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) — هذه الطبقةُ نفسُها · والخطرُ `RK-001` | مُعالَج ببندٍ قائم |
| DISC-004 | P2 | `docs/audit/COMPLETION_LEDGER.md § 9` | رقمانِ متناقضانِ لمخالفاتِ مصفوفةِ الحقيقةِ داخلَ المستودعِ نفسِه: § 9 يُعلِنُ **97**، وقيدُ `W-040` في § 8 يقولُ «ثابتٌ عندَ 63» | تشغيلُ `python tools/governance/truth_audit.py` على الرأسِ `79f4fad` = **63 مخالفةً** (60 IN_MEMORY_STORE · 2 HARDCODED_TRUTH · 1 SANDBOX_DISABLED)، وهو عينُ ما تُعلِنُه `TRUTH_MATRIX.md` المولَّدة | رقمٌ مُعلَنٌ يُقرأُ كحقيقةٍ ولا يُطابِقُ المقاسَ — وهو عينُ ما يُحرِّمُه `HARDCODED_TRUTH` | **مالِكُ النطاقِ `audit-truth`**: لم يُمَسَّ هنا لأنّه خارجَ نطاقِ `WI-001` (§ 5.2 تمنعُ توسيعَ النطاقِ الصامت) — يُفتَحُ له بندٌ مستقلٌّ | مُعالَج بالبندِ `WI-002` (W-047) |
| DISC-005 | **P1** | هذه الجلسةُ نفسُها — تصادمٌ واقعٌ لا مفترض | بينما كانَ هذا العملُ يُبنى على الأساسِ `79f4fad`، دُمِجَ الطلبُ **#13** في `main` وأخذَ المعرِّفَ **`W-042`** لعملٍ آخرَ تمامًا (حرسُ أسماءِ الجذر)، فصارَ قيدي وقيدُه برقمٍ واحد | `git log 79f4fad..origin/main` = ثلاثةُ التزاماتٍ · و`git show origin/main:docs/audit/COMPLETION_LEDGER.md` فيه `W-042` لغيرِ عملي · وتصادمُ `git rebase` في سجلِّ الإكمالِ ومصفوفةِ الحقيقة | **وقعَت الحالةُ التي بُنِيَت هذه الطبقةُ لمنعِها**: جهتانِ تعملانِ في الوقتِ نفسِه لا تعرفُ إحداهما الأخرى. وبوّابةُ السجلِّ تمنعُ `DUPLICATE_WORK_ID` عندَ الدفعِ **بعدَ** الاصطدامِ لا قبلَه؛ ولو كان `ACTIVE_WORK.md` نافذًا لظهرَ الحجزُ قبلَ كتابةِ سطرٍ واحد | **عُولِجَ في مصدرِه**: أُعيدَ الأساسُ على `origin/main` وصارَ قيدي `W-043`؛ ولا نطاقَ متقاطعًا مع #13 (‏هو في `tools/governance/check_root_file_names.py` وأنا في `check_work_governance.py`) | مُعالَج — ويُقيَّدُ **دليلًا** على وجاهةِ القرارِ `A-1` |
| DISC-006 | **P1** | بنيةُ CI نفسُها — لا شِفرةُ المستودعِ | **كلُّ وظائفِ CI تسقُطُ في ثانيتَينِ أو ثلاثٍ بلا تنفيذِ خطوةٍ واحدةٍ**: `steps=[]` و`runner_id=0` — أي لم يُسنَدْ لها عاملٌ قطُّ. **فحكمُ CI غيرُ مقروءٍ منذ ذاك الحين، لا أخضرَ ولا أحمرَ.** | `GET /actions/runs/{id}/jobs` على ثلاثةِ تشغيلاتٍ: **32903824510** (2026-08-25T21:58) = `with_steps=12` من 13 — **آخرُ تشغيلٍ نُفِّذَ فعلًا** · **32908448189** (2026-08-25T22:55) = `with_steps=0` من 13 — **أوّلُ تشغيلٍ لم يُنفَّذْ** · **32966866314** (2026-08-26T12:07 · دفعةُ `W-048`) = `with_steps=0` من 16. **فالعطبُ سبقَ عملَ `W-048` بأربعةَ عشرَ ساعةً** ولم يُحدِثْه. و`GET /actions/permissions` = `enabled: true` — فالمنعُ ليس في إعدادِ المستودعِ. · **والقياسُ الثالثُ جرى بأداةٍ محروسةٍ لا بيدٍ**: `tools/governance/ci_verdict_readability.py` (‏نزلَ حرسُها في `W-051` — **موضعُ القياسِ لا حرسُ هذا الصفِّ**؛ فمِرساةُ `DISC-006` **يدُ المالكِ** لا مسارٌ، وذِكرُ مسارٍ في نثرٍ لا يشتري حرسًا · `DISC-021`) | **كلُّ دعوى «أخضرَ» أو «أحمرَ» في CI بعدَ 2026-08-25T22:00Z دعوى بلا شاهدٍ.** ويسقُطُ معَها الشاهدُ الأقوى الذي أحالَ إليه المستودعُ مرارًا حينَ عجزَت بيئةُ العاملِ (‏`pytest` كاملةً · سجلُّ `git` كاملًا · خدماتٌ قائمةٌ · بوّابتا `Truth Audit` و`Identity Law`). **والحمراوانِ `B-9` لم تُقرَآ منذُ ذاك — فلا يُقالُ إنَّهما ما زالتا الحمراءَ الوحيدتَينِ.** | **بيدِ المالكِ وحدَه — لا يُعالَجُ بشِفرةٍ**: يُراجَعُ حسابُ `Actions` (‏دقائقُ · حدُّ إنفاقٍ · طريقةُ دفعٍ) في إعداداتِ الحسابِ؛ فلا سلطانَ لعاملٍ على فاتورةٍ. ويُفتَحُ له خطرٌ في [`RISK_REGISTER.md`](RISK_REGISTER.md) | **مُعالَجٌ — بيدِ المالكِ، وقد فعلَ.** نُقِلَ المستودعُ إلى حسابٍ فيه دقائقُ `Actions`، فصارَ الحكمُ مقروءًا مقيسًا (‏`READABLE · 13/13`) — انظر `DISC-023`. وسِجلُّ القياسِ قبلَ ذلك يُحفَظُ كما هو: وقِيسَ ثانيًا في `W-050` بعدَ تسعِ ساعاتٍ ونصفٍ وثلاثِ محاولاتٍ فلم يتحرّكْ (`with_steps=0` · `runner_id=0`)، **ودمجُ #18 لم يُغلِقْه** — الدمجُ ليس دواءً. وقِيسَ **ثالثًا** في `W-051` بعدَ نحوِ ثمانٍ وثلاثينَ ساعةً، وبأداةٍ محروسةٍ لا بيدٍ (‏الأداةُ مسمّاةٌ في خليّةِ الدليلِ — **ولا تُقرأُ حرسًا لهذا الصفِّ**: تقيسُ مقروئيّةَ الحكمِ ولا تُسنِدُ عاملًا، ويدُ الإصلاحِ في حسابِ المالكِ): ثمانيةُ تشغيلاتٍ · سبعةٌ `UNREADABLE` وواحدٌ `PARTIALLY_READABLE` وصفرٌ مقروءٌ كلًّا · ولم يُسجَّلْ تشغيلٌ جديدٌ في المستودعِ منذُ 2026-08-26T23:05:02Z. والقياسُ الثالثُ يُصدِّقُ الأوّلَ حرفًا بحرفٍ (`32903824510` → 12/13) فالمعيارُ واحدٌ لم يُخفَّفْ، والحِملُ منشورٌ في [`ci_verdict_readability.json`](../../audit/measurements/ci_verdict_readability.json) **وعادَ مقيسًا في `W-137` (2026-09-05) فحالتُه اليومَ `مفتوحٌ` لا `مُعالَجٌ`، ولا يُحذَفُ ما قبلَه**: لم يُسنَدْ عاملٌ لأيِّ وظيفةٍ في الوِردَينِ على عقدةِ `febb558` — التشغيلُ **95** (`33963635674`) 13 وظيفةً بصفرِ خطوةٍ و`runner_id` ∈ {0, غيابٌ} في أربعِ ثوانٍ، والتشغيلُ **96** (`33963635682`) وظيفةٌ واحدةٌ بصفرِ خطوةٍ و`runner_id: 0`؛ وأوّلُ ظهورٍ للعودةِ **إعادةُ** تشغيلِ 94 (2026-09-04T23:49Z). وآخرُ حكمٍ مقروءٍ محاولةُ 1 من التشغيلِ 94 (`READABLE · 13/13`). **فالوجهةُ هي هي: بيدِ المالكِ في حسابِ `Actions`** — ولا يُداوى بشِفرةٍ ولا بإعادةِ تشغيلٍ (`DISC-056`). **وقِيسَتِ العلّةُ نفسُها في `W-138` (2026-09-06) فلم تبقَ مُستنتَجةً من أثرِها**: دقائقُ `Actions Linux` في أيلولَ **2674** (‏763 + 716 + 1195) مقابلَ مُعلَنٍ منشورٍ **2000** لحسابٍ شخصيٍّ من طبقةِ `Free`، وعبورُ الحدِّ **خلالَ 2026-09-03** — وهو عينُ حدِّ العَطبِ الزمنيِّ (‏آخرُ حكمٍ نُفِّذَ 09-03T23:57:37Z · أوّلُ حكمٍ غيرِ مقروءٍ 09-04T23:49:49Z). ولا صفَّ دقائقَ يومَي 09-04 و09-05 بينما صفوفُ الخزنِ مستمرّةٌ، و`netAmount = 0.0` في كلِّ صفٍّ. ونُفِيَ البديلانِ: الصلاحيةُ مفتوحةٌ (`enabled: true`) ولا عاملَ ذاتيَّ الاستضافةِ (`total_count: 0`). **فبابُ الفعلِ أمامَ المالكِ صارَ رقمًا لا وصفًا**: طريقةُ دفعٍ صالحةٍ مع ميزانيّةٍ فوقَ صفرٍ · أو عاملٌ ذاتيُّ الاستضافةِ · أو انتظارُ تصفيرِ المُعلَنِ في أوّلِ الشهرِ (‏**توقُّعٌ لا حكمٌ**). والحالةُ باقيةٌ **مفتوحةً** بلا ترقيةٍ: القياسُ ليس إصلاحًا. |
| DISC-007 | **P1** | `tools/audit/final_audit.py § 6` — قاعدةُ قياسٍ لا شِفرةُ تطبيقٍ | **مِسبرٌ يُعلِنُ «بصمةٌ ليست في السجلِّ» وهو لا يقرأُ السجلَّ كلَّه — بثلاثةِ أوجهٍ مقيسةٍ لا مفترضةٍ:** (1) نافذةٌ مقطوعةٌ `git log --format=%h -n 400` فما قبلَ أحدثِ أربعِ مئةٍ يُعَدُّ غيرَ موجودٍ؛ (2) مقارنةُ نصٍّ بـ`%h` **المختصرِ** وطولُه ينمو بعددِ الكائناتِ (`core.abbrev=auto`) فيومَ يبلغُ ثمانيًا تُعَدُّ **كلُّ** بصمةٍ مفقودةً؛ (3) التعبيرُ يلتقطُ سبعَ خاناتٍ حصرًا `` `([0-9a-f]{7})` `` فبصمةُ الأربعينَ **لا تُفحَصُ قطُّ** | `GET /repos/zoorooz/AMOS-Fedration/commits` = **270 التزامًا في `main`** (‏وكانت 243 يومَ قُيِّدَ الواجبُ في `W-037`) · والوثيقةُ تذكرُ **43** بصمةً والقاعدةُ القديمةُ كانت تفحصُ **41** فقط — والمتروكتانِ `024f8ab149d4477321690bdd9fa5f8785e9ede83` و`6a86205ce635a777673dce7a125f6b6bfeec433e` وكلتاهما **سلفٌ لـ`main`** · وأعمقُ بصمةٍ مذكورةٍ `0111c95` عندَ **العمقِ 135** فهامشُ النافذةِ 265 التزامًا · و`bcdeccc` قيدٌ حقيقيٌّ لا كلمة | **قياسٌ يُخرِجُ رقمًا يُقرأُ حقيقةً وهو ليس حقيقةً** — وذاك عينُ ما يُحرِّمُه `HARDCODED_TRUTH`؛ والأخطرُ أنَّ العيبَ يكبرُ صامتًا بنموِّ المستودعِ فلا يُنبِّهُ أحدًا يومَ يبدأُ بالكذبِ | **البندُ `WI-007`**: القاعدةُ أُصلِحَت (‏عمقٌ كاملٌ · `%H` بالسابقةِ · 7 إلى 40 خانةً) وأُضيفَ **رفضٌ مُصنَّفٌ** بخروجٍ 2 عندَ نسخٍ ضحلٍ أو غيابِ `git`، وحُرِسَ المعيارُ بـ20 فحصًا على مستودعاتِ `git` حقيقيّةٍ · **وشِقٌّ باقٍ**: نصُّ السببِ في `tools/governance/measurement_provenance.py` ما زالَ يوصِفُ الهشاشةَ المُصلَحةَ، والمسارُ محجوزٌ لـ`WI-006` فيُصحَّحُ متى انفكَّ | مُعالَجٌ بالبندِ `WI-007` (`W-052`) — **والشِّقُّ الثاني مفتوحٌ بحجزِ `WI-006` لا بإهمالٍ** |
| DISC-008 | P2 | `OWNERSHIP.md § 1` مقابلَ شجرةِ `tools/` | **عشرةٌ من اثنَي عشرَ مجلَّدًا تحتَ `tools/` بلا نطاقٍ مُسجَّلٍ** — فالقاعدةُ 3 من `OWNERSHIP.md` («النطاقُ غيرُ المُسجَّلِ لا يُعمَلُ فيه») تحجُبُ العملَ فيها كلِّها، وفيها شِفرةٌ تعملُ اليومَ | `ls -d tools/*/` = **12 مجلَّدًا** · والمُسجَّلُ في `tooling-gates` كانَ `tools/governance · tools/dev` وحدَهما · وعددُ ملفّاتِ بايثون في غيرِ المُسجَّلِ **8**: `tools/crown` (4) · `tools/stubs` (2) · `tools/migrations` (1) · `tools/sovereignty` (1)؛ و`tools/dev` **المُسجَّلُ** فيه **صفرُ** ملفّاتِ بايثون | أيُّ عملٍ لازمٍ في `tools/crown` أو `tools/sovereignty` يصطدمُ بـ`SCOPE_UNOWNED` عندَ أوّلِ دفعةٍ، فيُدفَعُ العاملُ إلى تسجيلٍ يرتجلُه أو إلى تركِ العملِ — وكلاهما ضرر | **قرارُ المالكِ**: تعيينُ حدودِ نطاقٍ ومالكٍ لكلِّ مجلَّدٍ باقٍ ليس فعلَ منفِّذٍ · والخطرُ `RK-012` · وأمّا `tools/audit` وحدَه فسُجِّلَ في `W-052` **بأمرِ القاعدةِ 3 نفسِها** وأُلحِقَ بأقربِ نطاقٍ جنسًا (`tooling-gates` · المراجعُ نفسُه · `C1`) لا بنطاقٍ مُبتدَعٍ | **مفتوحٌ — بيدِ المالكِ.** أُغلِقَ منه صفٌّ واحدٌ (`tools/audit` في `W-052`) وبقيَت **تِسعةٌ**، ولم يُعيَّنْ مالكٌ ولا نُقِلَ مسارٌ |
| DISC-009 | **P1** | `ci.yml § sovereignty-kernel` مقابلَ `tests/sovereignty/` | **وظيفةُ نواةِ السيادةِ تسقُطُ لسببٍ غيرِ حقيقيٍّ**: تُثبِّتُ `requirements-dev.txt` وحدَه ثمَّ تُشغِّلُ `python -m pytest tests/sovereignty/ -q`، وفي `tests/sovereignty/test_enforcement_separation.py` تجهيزٌ اسمُه `مُصرِّح` يستوردُ `amos_federation.services.executive_core.sovereignty_bridge` — وهي حزمةُ الخدمةِ **التي لا تُثبِّتُها الوظيفةُ عن قصدٍ**. فالسقوطُ لا يقولُ شيئًا عن السيادةِ | `python -m pytest tests/sovereignty/ -q` عندي = **43 passed · 3 errors** كلُّها `ModuleNotFoundError: No module named 'amos_federation'` عندَ تجهيزِ ثلاثةِ فحوصٍ في `Testالوصولُالفدراليّ` · و`pytest.ini` فيه `pythonpath = .` وحدَه · و`requirements-dev.txt` يُعلِنُ ثلاثةَ أسماءٍ لا فيها الحزمةُ · وقياسٌ آليٌّ بـ`tools/governance/gate_dependency_closure.py . --check` يُسمِّي النقصَ `amos-federation` في هذه الوظيفةِ وحدَها من ثلاثَ عشرةَ مقيسةً | **حكمُ `sovereignty-kernel` غيرُ صالحٍ للقراءةِ**: أحمرُ دائمًا بثلاثِ أخطاءِ تجهيزٍ، فيُقرأُ «السيادةُ مكسورةٌ» وهي لم تُقَسْ؛ أو يُعتادُ الأحمرُ فيُبلَعُ سقوطٌ حقيقيٌّ بينَه · وهو من جنسِ عطبٍ **وقعَ قبلَ اليومِ** ومكتوبٍ في `tests/governance/test_w036_pricing_divergence.py`: سقوطٌ في وظيفةِ الهويّةِ بـ`ModuleNotFoundError: sqlalchemy` — فالخطرُ `RK-010` متحقِّقٌ مرّتَينِ لا مفترَضٌ | **قرارُ المالكِ — ثلاثةُ حلولٍ مختلفةِ المعنى ولا يُختارُ أحدُها بيدِ منفِّذٍ**: (1) تُثبِّتُ الوظيفةُ حزمةَ الخدمةِ فتصيرُ نواةُ السيادةِ مرتبطةً ببيئةِ الخدمةِ؛ (2) تُنقَلُ الفحوصُ الثلاثةُ إلى وظيفةٍ تُثبِّتُها (`crown-root-of-trust`) فيبقى الفصلُ؛ (3) تُعلَنُ التبعيّةُ في `requirements-dev.txt` فتتضخَّمُ بيئةُ الحوكمةِ. والحرسُ الآليُّ قُيِّدَ في `W-053` والخطرُ `RK-013` | **مفتوحٌ — بيدِ المالكِ.** والعطبُ **مُقيَّدٌ في أساسِ الأداةِ (`KNOWN_OPEN`) بمُوجِّهٍ**، فلا يُطوى: ومتى أُصلِحَ وبقيَ اسمُه في الأساسِ سقطَتِ الأداةُ بـ«أساسٌ تقادمَ» |
| DISC-010 | **P1** | `tools/governance/check_work_governance.py` مقابلَ `THE_ROADMAP § 7` | **حرسُ `POST_MERGE_NOT_CLOSED` كانَ أعمى عن الحالةِ التي أُنشِئَ لها**: يُشعِلُ مخالفتَه إن ذكرَت خليّةُ «قيدُ السجلّ» في صفِّ البندِ قيدًا `W-###` أُضيفَ في مجموعةِ التغييرِ نفسِها — وتلك الخليّةُ يكتبُها **من يجبُ عليه الإغلاقُ**، فتكونُ `—` بعينِ الحالةِ التي يقصِدُها § 7. ومعَه: الإحالةُ إلى `§ 13.3` في § 7 كانت تُشيرُ إلى قسمٍ **غيرِ موجودٍ** في الخارطة | `git show origin/main:docs/governance/work/ACTIVE_WORK.md` = `WI-006`/`WI-007`/`WI-008` حالتُها `IN_REVIEW` وخليّةُ قيدِها `—` بعدَ دمجِ `#20`…`#24` · و`check_work_governance.py --self-check` على `origin/main` = **صفرُ مخالفاتٍ** · و`grep -n '### 13.3' THE_ROADMAP.md` = لا نتيجة | واجبُ ما بعدَ الدمجِ لثلاثةِ بنودٍ مرَّ بلا صوتٍ، وحرسُه يُقرأُ ضمانًا وهو ليسَ ضمانًا | `WI-009` (القياسُ العكسيُّ + § 13.3) · وجعلُ إعلانِ البندِ إلزامًا في `ledger-gate` **قرارٌ للمالك** · **حرسُ الشقِّ المقيسِ مُثبَتٌ**: `tools/governance/check_work_governance.py` مع `tests/governance/test_work_governance_gate.py` و`tests/governance/test_w054_post_merge_reverse_link.py` — قِيسَ بطفرتَينِ في `W-060` أنَّ تعطيلَ موضعِ الإشعالِ الأماميِّ يُسقِطُ ثلاثةَ فحوصٍ وتعطيلَ الموضعِ العكسيِّ يُسقِطُ فحصًا واحدًا، **والملفُّ المُسمّى باسمِ القياسِ العكسيِّ لا يُغطّيه وحدَه** فلا يُذكَرُ مِرساةً منفردًا · **حرسٌ قائمٌ**: `tools/governance/check_work_governance.py` · `tests/governance/test_work_governance_gate.py` · `tests/governance/test_w054_post_merge_reverse_link.py` — قِيسَ بطفرتَينِ في `W-060`: تعطيلُ الإشعالِ الأماميِّ يُسقِطُ ثلاثةَ فحوصٍ، وتعطيلُ الموضعِ العكسيِّ يُسقِطُ فحصًا — والثلاثةُ مِرساةٌ واحدةٌ لا يُفرَدُ أحدُها | مُقيَّدٌ جزئيًّا |
| DISC-011 | P2 | `docs/audit/measurements/README.md` مقابلَ `tools/governance/stamp_readme_identity.py` | **ختمُ الفهرسِ كان قديمًا فعلًا**: `2026-08-24` والمولِّدُ يُنتِجُ `2026-08-27`. وقيدُ `W-053` نسبَ سقوطَ `test_generators_are_settled_on_the_repository[stamp_readme_identity]` إلى غيابِ تاريخِ git في بيئتِه — والسقوطُ في نسخةٍ كاملةٍ سببُه ختمٌ لم يُحدَّث | `pytest tests/governance/test_identity_law.py -q` قبلَ الإصلاحِ = **1 failed** · وبعدَ `python tools/governance/stamp_readme_identity.py .` = **passed** · والفارقُ سطرُ التاريخِ وحدَه | سببُ سقوطٍ حقيقيٌّ كان مكتوبًا سببًا بيئيًّا — وذاك بابُ اعتيادِ الأحمر | `WI-009` (أُصلِحَ فيه) | مُغلَق |
| DISC-012 | **P1** | `THE_ROADMAP § 4.3` مقابلَ `§ 7` | **واجبُ ما بعدَ الدمجِ لا طريقَ مشروعٌ إلى أدائِه**: § 7 يُلزِمُ نقلَ البندِ إلى `CLOSED` بعدَ الدمجِ، و§ 4.3 لا يسمحُ بغيرِ `IN_REVIEW → VERIFIED → CLOSED`، و`VERIFIED` «يضعُها **المراجعُ وحدَه**» — ولا مراجعَ مستقلًّا مُعَيَّنًا (`A-2` = `PENDING`). فبنودُ `WI-006`/`WI-007`/`WI-008` مدموجةُ العملِ **ومحبوسةٌ** لا تُغلَقُ إلّا بخرقِ آلةِ الحالاتِ أو بانتحالِ صفةِ المراجع | `LEGAL_TRANSITIONS['IN_REVIEW']` = `{VERIFIED, IN_PROGRESS}` · و`ILLEGAL_TRANSITION` يُقاسُ بمقارنةِ الأساس · و`A-2` في § 16 = `PENDING` · وسابقةُ `WI-001`…`WI-005` أُغلِقت لأنَّها لم تُدفَعْ قطُّ بحالةِ `IN_REVIEW` فلم يُقَسْ لها انتقال | ثلاثةُ بنودٍ تبقى مفتوحةً بلا خرقٍ من مالكِها، وأيُّ إغلاقٍ لها اليومَ دعوى؛ والحرسُ الجديدُ يُبلِغُ ولا يُسقِطُ | **قرارٌ للمالك**: تعيينُ المراجعِ (`A-2`) أو إعلانُ استثناءٍ منصوصٍ في § 4.3 بيدِه لا بيدِ منفِّذٍ (§ 16.3) | مفتوحٌ بيدِ المالك |
| DISC-013 | P3 | `PROJECT_STATE.md` و`docs/PROJECT_HANDBOOK.md` مقابلَ `docs/audit/COMPLETION_LEDGER.md § 8` | **حالةُ المشروعِ المُعلَنةُ كانت متأخِّرةً**: قِيدَ العطبُ عندَ فارقِ ستَّةِ قيودٍ (وثيقةٌ تقولُ `W-047` وأحدثُ صفٍّ `W-053`)، **فبلغَ الفارقُ تسعةً** قبلَ أن يُحرَسَ (`W-047` مقابلَ `W-056`)، والدليلُ متأخِّرٌ ثمانيةً (`W-048`) | `python tools/governance/state_document_drift.py` قبلَ التصحيحِ = خروجٌ 1 بمخالفتَي `STATE_DOC_BEHIND` (9 و8 قيودٍ) · وبعدَه خروجٌ 0 | قارئٌ يبدأُ من وثيقةِ الحالةِ يقرأُ ماضيًا حالةً | مُعالَجٌ بالبندِ `WI-012` (`W-057`) — الوثيقتانِ صُحِّحَتا بالمقيسِ، **وصارَ الانحرافُ محروسًا بأداةٍ تسقُطُ حينَ يعودُ** | مُعالَجٌ بالبندِ `WI-012` (`W-057`) · بانتظارِ دمجِ المالكِ |
| DISC-014 | P2 | `docs/audit/measurements/` مقابلَ حجزِ `tools/governance/measurement_provenance.py` لـ`WI-006` | **قياسٌ صحيحٌ لا يُنشَرُ لا لعطبٍ فيه بل لقفلِ نطاقٍ**: نشرُ أيِّ ملفٍّ في مجلَّدِ القياساتِ يوجِبُ قيدَ نَسَبٍ في `measurement_provenance.py` (‏قاعدةُ `W-037`: «لا ملفَّ بلا قيدٍ»)، وذاك الملفُّ **محجوزٌ للبندِ `WI-006` وهو `IN_REVIEW`** — فالجردُ الحيُّ المقيسُ في `W-055` بقيَ بلا نشرٍ، وكُتِبَت أرقامُه في القيدِ ووثيقةِ العمارةِ. **وهذه ثالثُ مرّةٍ يُعطِّلُ الحجزُ نفسُه عملًا مشروعًا**: `DISC-007` (نصُّ سببٍ متقادمٌ لم يُصحَّحْ) ثمَّ `W-054` (حِملُ مقروئيّةِ CI لم يُنشَرْ) ثمَّ هذا | `python tools/governance/measurement_provenance.py . --check` بعدَ القيدِ = **✓ 12 مقيَّدًا** وبعدَ التراجُعِ عنه = **✓ 11 مقيَّدًا**؛ و**حقولُ القيدِ اللازمةُ** (المُولِّدُ · أمرُ إعادةِ التوليدِ · الوضعُ `declared` · موجَزُ السببِ) مكتوبةٌ في قيدِ `W-055` فيُوضَعُ القيدُ بلا اجتهادٍ · وصفُّ `WI-006` في [`ACTIVE_WORK.md`](../governance/work/ACTIVE_WORK.md) يُعلِنُ `measurement_provenance.py` في مساراتِه وحالتُه `IN_REVIEW` | **قياسٌ يُفقَدُ لا يُخزَّنُ**: كلُّ إعادةِ قياسٍ حيٍّ تلزمُها يدُ مالكِ السرِّ، فما لم يُنشَرْ لا يُعادُ توليدُه بأمرٍ. والعِلّةُ **بنيويّةٌ لا عارضةٌ**: حجزُ ملفِّ سجلٍّ مركزيٍّ يُجمِّدُ كلَّ عملٍ يحتاجُ قيدًا فيه، وهو يتراكمُ ما دامَ `A-2` معلَّقًا (`DISC-012`) | **المالك**: يُفَكُّ بإغلاقِ `WI-006` (‏موقوفٌ على `A-2`)، أو بقرارٍ يُخرِجُ سجلَّ النَّسَبِ من قفلِ النطاقِ كما أُخرِجَت السجلّاتُ والدفترُ (`EXEMPT_EXACT`) — **وذاك تعديلُ قانونٍ لا فعلُ منفِّذٍ** | مفتوحٌ — قُيِّدَ في `W-055` |
| DISC-015 | P3 | `tools/governance/ci_verdict_readability.py` — واجهةُ الاستدعاء | **عَلَمانِ لا يجتمعانِ ولا يُعلَنُ ذلك**: `--from-json <payload>` يُفهَمُ منه الحكمُ على حِملٍ محفوظٍ بلا شبكةٍ، فإذا صُحِبَ بـ`--run <ID>` طلبَ توكنًا ورفضَ — فالمُستعمِلُ الذي يريدُ إعادةَ الحكمِ على قياسٍ سابقٍ يظنُّ أنَّ العطبَ في حِملِه وهو في استدعائِه | قِيسَ في هذه الجلسةِ: الاستدعاءُ الصحيحُ `--from-json <payload> --repo zoorooz/AMOS-Fedration` = حكمٌ يُطبَعُ؛ وإضافةُ `--run` = **رفضٌ لغيابِ توكنٍ** رغمَ وجودِ الحِملِ | **كلفةُ استعمالٍ لا كذبُ قياسٍ**: لا حكمَ خاطئًا يُنتَجُ — يُرفَضُ ولا يُبتلَعُ، فالخطورةُ P3 لا أعلى | `tooling-gates` — تُوضَّحُ الرسالةُ أو يُعلَنُ التنافي في `--help` · ولا يُلمَسُ الملفُّ الآنَ: **محجوزٌ لـ`WI-006`** (`DISC-014`) · **موقوفٌ على حسمِ المالكِ لا على استحقاقٍ يُخترَعُ**: الملفُّ محجوزٌ لـ`WI-006`، وحجزُه لا يُرفَعُ قبلَ إغلاقِ بندِه، وطريقُ الإغلاقِ مقطوعٌ حتى يُعتمَدَ `A-2` (`DISC-012`) | مفتوحٌ |
| DISC-016 | P2 | `tools/governance/check_work_governance.py:665`‑`673`‑`715` · `.github/workflows/ci.yml:190` | **وعدُ تصعيدٍ مكتوبٌ بلا آلةٍ تقرؤه**: البوّابةُ تُعلِنُ في تعليقِها ونصِّ مساعدتِها أنَّ `POST_MERGE_NOT_CLOSED` يصيرُ مُسقِطًا «بعدَ اعتمادِ `A-2`/`A-3`»، ولا سطرَ في الشجرةِ يقرأُ جدولَ § 16 — فالتصعيدُ معلَّقٌ على قرارٍ لا يراه شيءٌ في الشِفرة. | `grep -rnE "\bA-[23]\b" tools/ tests/ .github/ --include=*.py --include=*.yml` = **ستُّ إشاراتٍ**، كلُّها تعليقٌ أو نصُّ مساعدةٍ أو تعليقُ سيرِ عملٍ · لا قارئَ لحالةٍ · و`python tools/governance/sovereign_decision_status.py` يقيسُ الحالةَ الآنَ من خارجِ يدِ البوّابةِ: `A-2` `PENDING` · `A-3` `PENDING` | لو اعتمدَ المالكُ `A-2`/`A-3` اليومَ لبقيَ سلوكُ البوّابةِ كما هو: لا تصعيدَ تلقائيًّا، والوعدُ يُقرأُ نافذًا وهو نائم. | ربطُ التصعيدِ بقراءةِ § 16 يمسُّ `check_work_governance.py` (محجوزٌ لـ`WI-009`) وتفعيلَ الإسقاطِ (وهو عينُ `A-3`) — فالوجهةُ: `WI-009` بعدَ فتحِ طريقِ الإغلاقِ، ولا يُنفَّذُ في هذا البند. · **بيدِ المالكِ**: تفعيلُ الإسقاطِ هو عينُ `A-3`، وتعديلُ البوّابةِ محجوزٌ لـ`WI-009` الموقوفِ على `A-2` — فهو قرارُ المالكِ لا اجتهادُ منفِّذٍ، ولا استحقاقَ يُكتَبُ له من يدٍ غيرِ يدِه | مفتوح |
| DISC-017 | P2 | `docs/governance/work/THE_ROADMAP.md § 16` (البندانِ 2 و4 · وجدولُ القرارات) | **سندُ وضعِ الإبلاغِ انقضى، ونصَّانِ في القسمِ نفسِه يتنافيانِ**: البندُ 4 يمنحُ وضعَ الإبلاغِ «ما دامت `PROPOSED`»، وحالةُ الوثيقةِ المُعلَنةُ `ACTIVE` منذُ اعتمادِ `A-1` (2026‑08‑26) — فالإبلاغُ قائمٌ بلا سندٍ منصوصٍ؛ والبندُ 2 يقولُ إنَّ الاعتمادَ «وبها وحدَها» يُفعِّلُ البوّابةَ **بوضعِ الإسقاط**، بينما `A-3` (تفعيلُ الإسقاطِ) صفٌّ مستقلٌّ ما زالَ `PENDING`. | `python tools/governance/sovereign_decision_status.py` → `ADVISORY_BASIS_EXPIRED` (حالةُ الوثيقةِ `ACTIVE` · سندُ الإبلاغِ `PROPOSED`) وفحصُه `tests/governance/test_w056_sovereign_decision_status.py::test_expired_advisory_basis_is_reported` | يُتيحُ لقارئينِ صادقَينِ حكمَينِ متناقضَينِ: أنَّ الإسقاطَ نافذٌ بالبندِ 2، أو أنَّ الإبلاغَ نافذٌ بالبندِ 4 — والفرقُ بينَهما بناءٌ أحمرُ أو أخضرُ. | تعديلُ نصِّ § 16 حسمٌ سياديٌّ بيدِ المالكِ لا بيدِ منفِّذٍ: يُصحَّحُ البندُ 4 ليُعلَّقَ على `A-3` لا على `PROPOSED`، أو يُعتمَدَ `A-3`. قِيدَ هنا ويُبلَغُ به عندَ كلِّ قياسٍ ولا يُسقِطُ التشغيلَ (§ 13.3). | مفتوح |
| DISC-018 | P3 | `DISCOVERIES.md` مقابلَ القياسِ الحاضرِ (‏`W-057`) | **القيدُ المفتوحُ لا يمنعُ اتّساعَ العطبِ**: `DISC-013` كُتِبَ في وقتِه بفارقِ ستَّةِ قيودٍ وبقيَ «مفتوحًا» فبلغَ الفارقُ **تسعةً** — أي أنَّ ثلاثةَ قيودٍ أُضيفَت بعدَ اكتشافِ العطبِ ولم تُصحِّحْ أيٌّ منها الوثيقةَ | نصُّ `DISC-013` يقولُ «`W-053`» وقياسُ اليومِ يقولُ «`W-056`» بالأداةِ نفسِها | كلُّ عطبٍ وثائقيٍّ مُقيَّدٍ بلا حرسٍ ينمو بصمتٍ بينَ القيدِ والإصلاحِ | حُرِسَ محلُّه في `WI-012`؛ وتعميمُ القاعدةِ (لكلِّ قيدٍ حرسٌ أو تاريخُ استحقاقٍ) صارَ مقيسًا بـ`tools/governance/open_record_accountability.py` ومحروسًا بـ`tests/governance/test_w059_open_record_accountability.py` ـ وقُرِّرَ في `WI-014` أن يكونَ أداةً لا نصًّا (§ 3: لا قاعدةَ بلا حرسٍ) | مُعالَجٌ بالبندِ `WI-014` (W-059) · بانتظارِ دمجِ المالك |
| DISC-019 | P2 | `tests/governance/test_step18_restart_survival.py:78` (`LIVE_STACK_MODULES`) مقابلَ ما تستوردُه مراحلُ `tools/governance/restart_survival_probe.py` | **حرسٌ يشهدُ زورًا على محروسِه**: شرطُ القياسِ الحيِّ كانَ قائمةً مكتوبةً بيدٍ فيها اسمانِ (`fastapi` · `sqlalchemy`)، ومراحلُ المِسبارِ تبلُغُ عبورًا `amos_federation.common.auth` فتستوردُ `jwt` و`structlog` و`pydantic_settings`. فبيئةٌ فيها الاسمانِ دونَ البقيّةِ **لا تُتخطَّى**: تُقاسُ فيعودُ `UNMEASURED`، ثمَّ تُقرأُ تلك النتيجةُ **فقدَ إدامةٍ** فيُعلَنُ حرفًا «الكنارُ نجا خلافًا للتصريحِ» و«سجلُّ الذرّيّةِ لم يُكتَبْ — فالتوجيهُ مُعطَّلٌ» — وهما كاذبتانِ، والسببُ وحدةٌ غائبةٌ | `pytest tests/ -q` على `70a4081`: فحصانِ ساقطانِ، و`detail` المقيسُ في مُخرَجِ المِسبارِ `ModuleNotFoundError: No module named 'jwt'` — وترويسةُ الملفِّ نفسِها (§ الهدف · الصورةُ الثالثةُ) تُعلِنُ أنَّها أُنشِئَت لمنعِ هذا الجنسِ بعينِه | **حكمٌ أحمرُ كاذبٌ أسوأُ من غيابِ حكمٍ**: يُوهِمُ خِلافَ `Q-39 (أ)` ويُوهِمُ تلويثَ الشجرةِ، فيُفسِدُ كلَّ قراءةٍ تالِيَةٍ للحالةِ ويُهدِرُ وقتَ تشخيصٍ على عطبٍ غيرِ موجودٍ. ومن جنسِ `RK-010` و`RK-013` | صُحِّحَ محلُّه في `WI-013` بشرطٍ **مقيسٍ من مُخرَجِ المِسبارِ** لا مفترَضٍ، وحُرِسَ الحدُّ في `test_w058_live_stack_precondition.py`. **وما بقيَ ليس من نطاقِه**: نقصُ تبعيّاتِ الطبقةِ الفدراليّةِ `DISC-009` (بيدِ المالكِ · ثلاثةُ وجوهٍ) · وحكمُ `measure.yml` غيرُ مقروءٍ `DISC-006` | مُعالَجٌ محلُّه (‏`WI-013`) · والسببُ الأصليُّ باقٍ في `DISC-009` |
| DISC-020 | P2 | سجلّا `DISCOVERIES.md § 1` و`RISK_REGISTER.md § 1` مقابلَ قاعدةِ `DISC-018` | **أكثرُ من ثُلُثِ القُيودِ المفتوحةِ بلا مِرساةٍ**: أوّلُ قياسٍ للقاعدةِ يُعلِنُ أنَّ اثني عشرَ قيدًا مفتوحًا لا حرسَ لها ولا تاريخَ استحقاقٍ ولا إعلانَ أنَّها بيدِ المالكِ، وأنَّ **لا قيدَ واحدًا في السجلَّينِ حملَ تاريخَ استحقاقٍ** ـ فالشِّقُ الثاني من قاعدةِ `DISC-018` غيرُ مُستعمَلٍ أصلًا | `python tools/governance/open_record_accountability.py` عندَ الرأسِ `e5efe9c`: **37 صفًّا مقروءًا · 29 مفتوحًا · 6 محروسًا · 0 باستحقاقٍ · 11 بيدِ المالكِ · 12 بلا مِرساةٍ** (خروجٌ 1) · والأرقامُ تُعادُ بأمرٍ واحدٍ | العَطبُ الموصوفُ في `DISC-018` لم يكن حالةً مُفرَدةً بل **الحالَ الغالِبَ** في السجلَّينِ | إرساءُ كلِّ صفٍّ على حـدةٍ بندٌ تالٍ في نطاقِ `governance-docs`: ما كانَ له حرسٌ قائمٌ يُذكَرُ مسارُهُ **بعدَ التحقُّقِ من أنَّهُ يسقُطُ فعلًا عندَ عودةِ العَطبِ** ـ ولا يُخترَعُ هنا تاريخُ استحقاقٍ لما هو موقوفٌ على `A-2`/`A-3` · وحرسُ العدَّادِ نفسِه `tools/governance/open_record_accountability.py` يُشعِلُ عندَ كلِّ صفٍّ جديدٍ بلا مِرساةٍ · والخطرُ `RK-019` | مُغلَقٌ — أُرسِيَت الصفوفُ الأحدَ عشرَ كلُّها: عشرةٌ بالبندِ `WI-015` (`W-060`)، والحادي عشرَ `RK-003` بالبندِ `WI-016` (`W-061`) بعدَ بناءِ قياسِ إشارتِه `tools/governance/surface_debt_trend.py`؛ وقِيسَ بعدَ الإرساءِ: **صِفرُ صفوفٍ بلا مِرساةٍ** بأمرِ `python tools/governance/open_record_accountability.py`. والقاعدةُ تبقى محروسةً بعدَ الإغلاقِ: أيُّ صفٍّ جديدٍ بلا مِرساةٍ يُشعِلُ العدَّادَ نفسَه، والخطرُ `RK-019` |
| DISC-021 | P2 | `tools/governance/open_record_accountability.py` — مُصنِّفُ المِرساةِ | **مسارٌ يُذكَرُ دليلَ غيابِ حرسٍ يُقرأُ حرسًا قائمًا**: يُصنِّفُ المُصنِّفُ الصفَّ `GUARD` لمجرَّدِ ورودِ مسارِ ملفٍّ موجودٍ في خلايا مِرساتِه، بلا قراءةِ ما تقولُه الجملةُ عن ذلك المسارِ — فمن كتبَ «لا حرسَ، وهذانِ الملفّانِ لا يقيسانِ هذا» نالَ خُضرةً كاذبةً. | قِيسَ في `W-060`: صِيغَت خليّةُ `RK-003` نافيةً للحرسِ وذاكرةً لمسارَي القارئَينِ، فقرأَها العدَّادُ `GUARD · tools/audit/final_audit.py · tests/governance/test_w052_history_hash_probe.py` وأعلنَ **صِفرَ مخالفاتٍ**؛ ثمَّ أُزيلَ المسارانِ فعادَ الصفُّ `NONE` ومخالفتُه معه — فارقٌ في الحكمِ من نصٍّ لا من واقعٍ. | **عدَّادُ المساءلةِ نفسُه يمكنُ أن يُخضَّرَ بصياغةٍ**: أخطرُ ما في حرسٍ أن يُشترى صمتُه بكلمةٍ. الأثرُ محصورٌ الآنَ لأنَّ الصياغةَ صُحِّحَت في القيدِ نفسِه. | إحكامُ المُصنِّفِ (أن يقرأَ نفيًا صريحًا قبلَ المسارِ، أو أن يطلبَ للحرسِ إعلانًا مُهيكَلًا لا نثرًا) يمسُّ ملفَّ الأداةِ **وهو محجوزٌ لـ`WI-014`**، وحجزُه لا يُرفَعُ قبلَ إغلاقِ بندِه، وطريقُ الإغلاقِ مقطوعٌ حتى يُعتمَدَ `A-2` — **فالوجهةُ بيدِ المالكِ** لا استحقاقًا يُخترَعُ. · **وأُعيدَ قياسُ هذا الحاجزِ في `W-124` فلم يَعُدْ قائمًا** (‏والنصُّ أعلاه يبقى كما كُتِبَ لا يُمحى): `WI-014` صارَ `CLOSED` بقيدِ `W-067`، وقِيسَ من صفوفِ § 1 في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) أنَّ **ستّةَ عشرَ بندًا غيرَ مُغلَقٍ** ولا واحدٌ منها يُعلِنُ **ملفَّ أداةِ المساءلةِ نفسِه** في مساراتِه (‏ولا يُكتَبُ مسارُه هنا: خليّةُ المِرساةِ تُقرأُ مساراتٍ، فذكرُه فيها يشتري خُضرةً بكلمةٍ — وهو عينُ العَطبِ المُقيَّدِ) — فالمسارُ غيرُ مُدَّعًى، والوجهةُ صارَت البندَ `WI-039` في نطاقِ `tooling-gates` · **استحقاق: 2026-09-30** (‏**مُجدَّدٌ في `W-160` بسببٍ مكتوبٍ لا مطويٍّ**، والاستحقاقُ الأوّلُ **2026-09-11** يبقى مكتوبًا ههنا لا يُمحى: مضى ولم يُنجَزْ، والحاجزُ **مقيسٌ لا مُدَّعًى** — وجهتُه `WI-039` (`IN_PROGRESS`) — وجهتُه بندٌ بُدِئَ ولم يُنجَزْ بعدُ. والتجديدُ **رفعُ حاجزٍ زمنيٍّ لا إنجازٌ ولا إغلاقٌ ولا تخفيفُ حرسٍ**، وأفقُه عينُ أفقِ الحجوزِ القائمِ **2026-09-30** فلا يُخترَعُ أفقٌ ثالثٌ — ومتى بلغَه ولم يُنجَزْ فالحمرةُ حقٌّ تُقرأُ لا تُؤجَّلُ ثالثةً). وسقّاطةُ صفوفِ «مسارٍ في نثرٍ» تُقرأُ من هذا السطرِ وحدَه (‏مصدرُ حقيقةٍ واحدٌ لا ثابتٌ مدفونٌ في شِفرةٍ) ⇒ `ANCHOR_PROSE_BASELINE: rows=2` — لا يعلو ويُخفَضُ فقط بترقيةِ صفوفٍ إلى إعلانٍ مُهيكَلٍ. **وخُفِضَ في `W-126` من خمسةٍ وعشرينَ إلى اثنَينِ** بمراجعةِ ثلاثةٍ وعشرينَ صفًّا (‏خمسةَ عشرَ في سجلِّ الاكتشافاتِ وثمانيةً في سجلِّ المخاطرِ) صفًّا صفًّا: كلُّ صفٍّ رُقِّيَ يُعلِنُ حرسَه بالعلامةِ المُهيكَلةِ وبسببٍ مقروءٍ في الصفِّ نفسِه، و**لم يُرَقَّ ما لا حرسَ له**: `DISC-007` (‏المسارُ فيه موضعُ العَطبِ الباقي لا حارسُه) و`DISC-032` (‏«والحرسُ لم يُكتَبْ بعدُ» بنصِّ حالتِه). **ونزلَ في `W-125`** ما لا يمسُّ مُدَّعًى: علامتانِ مُعلَنتانِ («حرسٌ قائمٌ:» · «حرسٌ مُثبَتٌ:») تُقرأُ بعدَهما مساراتٌ وحدَها فتقفُ عندَ أوّلِ كلامٍ، ووجهُ `--declaration-check` يُسقِطُ `PROSE_ANCHOR_GROWTH` عندَ النموِّ و`STALE_PROSE_BASELINE` عندَ رخاوةِ الرقمِ، وحرسُه واحدٌ وعشرونَ فحصًا. **والعَطبُ لم يُغلَقْ**: حقلُ `anchor` ما زالَ يقرأُ صفَّ النثرِ `GUARD`، وقلبُه يُسقِطُ فحوصًا في حرسِ الأداةِ **المُدَّعى لـ`WI-033` (`IN_REVIEW`)** فلا يُمَسُّ — فالقيدُ يبقى مفتوحًا بسقّاطةٍ تمنعُ نموَّه لا ببراءةٍ. | مفتوحٌ |
| DISC-022 | P2 | `docs/audit/COMPLETION_LEDGER.md` — الأرقامُ المنشورةُ لدعاوى الطفرةِ، مقابلَ `tools/governance/mutation_claims.py` | **رقمٌ منشورٌ في قيدٍ مُقفَلٍ لا يُعادُ اليومَ، وتسعةُ قيودٍ تدَّعي طفرةً بلا سجلٍّ**: أوّلُ تسجيلٍ آليٍّ لدعاوى قيودٍ سابقةٍ أثبتَ أنَّ دعوى `W-056 · عدُّ الأداةِ نفسَها` نُشِرَت «فحصانِ» والمقيسُ اليومَ **فحصٌ واحدٌ** بأيٍّ من ترميزَينِ مُجرَّبَينِ (استثناءُ الأداةِ وحدَها · واستثناؤها مع فحصِها) — فالقاعدةُ المُعلَنةُ في `SELF_EXCLUDED` **يعَضُّها فحصٌ واحدٌ لا اثنانِ**، ولا فحصَ في ملفِّ `W-056` يُسمّي الثابتَ نفسَه. وأنَّ قيدَي `W-029` و`W-030` يدَّعيانِ ثمانيَ وستَّ تحوّراتٍ ولم يكونا في أوّلِ قائمةِ النقصِ التي كتبَها منفِّذُ هذا القيدِ | ‏`python tools/governance/mutation_probe.py --work W-055` = **4 طفراتٍ · 4 مُلتقَطةٌ · خروجٌ 0** (‏1·1·4·1 مطابقًا للمنشورِ حرفًا) · `--work W-059` = **2 · 2 · خروجٌ 0** (‏11 و1 — ولم يُنشَرْ لهما رقمٌ في قيدِهما فقِيسا هنا) · `--work W-056` = **7 طفراتٍ · 6 مُلتقَطةٌ · `FAILURE_COUNT_MISMATCH`: المُعلَنُ 2 والمقيسُ 1** · وفحصُ `test_declared_gap_covers_every_ledger_entry_that_claims_a_mutation` أسقطَ قائمةَ النقصِ الأولى فأضافَ `W-029` و`W-030` | **«حرسٌ مُثبَتٌ بطفرةٍ» في قيدٍ غيرِ مُسجَّلٍ دعوى تاريخيّةٌ لا رقمٌ يُعادُ**: ما لم يُسجَّلْ في `mutation_claims.py` لا يُقاسُ، ولا يُقرأُ العددُ المنشورُ في السجلِّ مقيسًا اليومَ بمجرَّدِ أنَّه كُتِبَ يومًا | إحكامُ حرسِ قاعدةِ `SELF_EXCLUDED` يمسُّ `tests/governance/test_w056_sovereign_decision_status.py` **وهو محجوزٌ لـ`WI-011`** وحجزُه لا يُرفَعُ قبلَ إغلاقِ بندِه، وطريقُ الإغلاقِ موقوفٌ على `A-2` (`DISC-012`) — فلا يُوسَّعُ هنا ولا يُخفَّفُ حدُّ المِسبارِ؛ والرقمُ المُسجَّلُ **مقيسٌ (1)** لا منقولٌ. وتسجيلُ التسعةِ الباقيةِ يُسَدُّ قيدًا بعدَ قيدٍ في `tooling-gates`، وقائمتُها محروسةٌ في `tests/governance/test_w063_registered_claims.py` بحيثُ يسقُطُ الفحصُ يومَ يُقيَّدُ قيدٌ يدَّعي طفرةً ولا يُسجِّلُها ولا يُعلِنُ نقصَها · **حرسٌ قائمٌ**: `tests/governance/test_w063_registered_claims.py` — يُسقِطُ يومَ يُقيَّدُ قيدٌ يدَّعي طفرةً ولا يُسجِّلُها ولا يُعلِنُ نقصَها — وهو حرسُ الشِقِّ المقيسِ وحدَه، وإحكامُ `SELF_EXCLUDED` يبقى محجوبًا بحجزِ `WI-011` | مفتوحٌ |
| DISC-023 | **P1** | بنيةُ CI — دقائقُ التشغيلِ في الحسابِ، وقد أُثبِتَ سببًا | **`DISC-006` زالَ مانعُه: حكمُ CI صارَ مقروءًا**. كانت كلُّ وظيفةٍ تسقُطُ في ثانيتَينِ بلا خطوةٍ (`steps=[]` · `runner_id=0`) في المستودعِ السابقِ؛ وقد أفادَ المالكُ أنَّ **دقائقَ `Actions` في ذاك الحسابِ كانت منتهيةً**، فنُقِلَ المستودعُ إلى حسابٍ آخرَ. فالتشخيصُ المُعلَنُ في `DISC-006` — «بيدِ المالكِ · فاتورةٌ لا شِفرةٌ» — **صحَّ بالتجربةِ لا بالترجيحِ** | قِيسَ بالأداةِ المحروسةِ لا باليدِ: `python tools/governance/ci_verdict_readability.py --from-json <حِملُ /actions/runs/33251600117/jobs>` ⇒ `READABLE · نُفِّذَت 13/13 وظيفةً (لم يُسنَدْ عاملٌ لـ0 · مُتخطًّى 0 · مشكوكٌ 0)`. وفي الحِملِ نفسِه لكلِّ وظيفةٍ خطواتٌ ومُدَدٌ ونتائجُ حقيقيّةٌ | **كلُّ عبارةٍ في المستودعِ تقولُ «حكمُ CI غيرُ مقروءٍ» أو «الدقائقُ منتهيةٌ» تُقرأُ عن المستودعِ السابقِ، لا عن هذا**. ومن اليومِ يُنسَبُ الأخضرُ والأحمرُ إلى CI **بشاهدٍ**: صارَ لزامًا أن تُقرأَ كلُّ حمرةٍ وتُصلَحَ من سببِها، وصارَ ادِّعاءُ الخُضرةِ بلا تشغيلٍ مقيسٍ مخالفةً لا اجتهادًا | يُقرأُ الحكمُ بعدَ كلِّ دفعةٍ بالأداةِ نفسِها، فإن سقطَت وظيفةٌ تُقيَّدُ حمرتُها بسببِها لا بعددِها. ولا يُمَسُّ `.github/` | **مُغلَقٌ — سببُه زالَ ومقروئيّتُه مقيسةٌ.** ويُحدَّثُ به `DISC-006` إلى «مُعالَجٌ بيدِ المالكِ». والحدُّ مُعلَنٌ: مقروئيّةٌ لا تعني خُضرةً — أوّلُ قراءةٍ أظهرَت وظيفتَينِ حمراوَينِ، وهما موضوعُ `DISC-024` |
| DISC-024 | P2 | `tests/sovereignty/test_outbox.py` — قيمةُ اختبارٍ سلبيٍّ، مقابلَ بوّابتَي أسرارٍ في CI | **سطرُ فحصٍ واحدٌ أسقطَ وظيفتَينِ في كلِّ تشغيلٍ**: كتلةُ `PRIVATE KEY` كُتِبَت **حرفًا** قيمةً لاختبارٍ سلبيٍّ يتأكَّدُ أنَّ الحمولةَ ترفُضُ مادّةَ المفاتيحِ. فقرأَها ماسحُ الأسرارِ في CI مادّةَ مفتاحٍ في الشجرةِ وفي التاريخِ المنشورِ معًا — **والحرسُ كانَ مُحِقًّا، والفتيلُ في الفحصِ** | مقيسٌ من سجلَّي الوظيفتَينِ عبرَ `GET /actions/jobs/<id>/logs` للتشغيلِ `33251600117`: وظيفةُ `Sovereignty Kernel` عندَ «بوابة 6 — لا مفتاح خاص للملك في المستودع» ⇒ `grep` يُطابِقُ `./tests/sovereignty/test_outbox.py` · ووظيفةُ `Crown Root of Trust` عندَ «بوابة 2ب» ⇒ `verify_secret_boundaries.py` يُعلِنُ مخالفتَينِ (الشجرةُ · والتاريخُ). و**الوظائفُ الإحدى عشرةُ الأخرى نجحَت** | ثلاثُ طرقٍ كانت مفتوحةً، واثنتانِ رُفِضتا بسببٍ مُعلَنٍ: **توسيعُ النمطِ أو إعفاءُ مجلَّدِ الاختباراتِ** يُخفِّفُ حرسًا حقيقيًّا لأجلِ الخُضرةِ — وهو عينُ ما تنهى عنه الأوامرُ الحاكمةُ؛ و**إعادةُ كتابةِ التاريخِ** تُبطِلُ بصماتِ الالتزامِ المُقيَّدةَ في السجلاتِ فتُفسِدُ سلسلةَ الإثباتِ. والمُتَّبَعُ: بناءُ القيمةِ بالتركيبِ في الشجرةِ (فتبقى تامّةً عندَ التشغيلِ)، وإعلانُ سطرِ التاريخِ الواحدِ في سجلِّ استثناءاتٍ ببصمةِ نصِّه مع مُراجِعٍ وتاريخٍ وقيدٍ، وبوّابةٌ ثانيةَ عشرةَ تُسقِطُ الإعفاءَ الميتَ | عُولِجَ في `W-064`: القيمةُ تُبنى بالتركيبِ · سجلُّ `tools/crown/secret_scan_exceptions.py` ببصمةٍ واحدةٍ (`08aaa701…` · أوّلُ ظهورٍ `0111c95`) · بوّابتانِ في `verify_secret_boundaries.py` (‏`12/12`) · وثمانيةُ فحوصٍ حرسًا للسجلِّ نفسِه | **مُغلَقٌ في الشجرةِ — والخُضرةُ لا تُدَّعى حتّى تُقرأَ من CI بعدَ الدمجِ.** والحدُّ: الإعفاءُ لبصمةِ نصٍّ رُوجِعَ، لا لملفٍّ ولا لمجلَّدٍ؛ فحرفٌ يتغيَّرُ يُخرِجُ السطرَ من الإعفاءِ |
| DISC-025 | P2 | `tests/governance/test_w054_post_merge_reverse_link.py` + `tools/governance/check_work_governance.py` — مقابلَ استخراجِ `pull_request` في CI | **حرسُ ما بعدَ الدمجِ لا يُقاسُ على طلباتِ الدمجِ، والاختبارُ يُحمِّرُ لعجزِ القياسِ لا لمخالفةٍ**: يُشغَّلُ الحرسُ بـ`--merge-base main`، وفي استخراجِ `pull_request` لا وجودَ لفرعٍ محليٍّ باسمِ `main` (رأسٌ منفصلٌ على مرجعِ دمجٍ)، فيُعلِنُ الحرسُ **«لم يُقَسْ»** ويخرُجُ بصفرٍ، فيسقُطُ الفحصُ الذي يشترطُ خروجًا بواحدٍ | مقيسٌ من سجلِّ وظيفةِ `Identity Law` في تشغيلِ الطلبِ `33279646766`: `✗ test_الإسقاطُ_يُطلَبُ_فيسقُط` ونصُّ الحرسِ حرفًا: «حرسُ ما بعدَ الدمجِ **لم يُقَسْ**: لا يُقرأُ سجلُّ الإكمالِ من «main» في هذه الشجرةِ». **وليسَ من عملِ `W-064`**: الوظيفةُ نفسُها بالخطوةِ نفسِها سقطَت في تشغيلِ طلبِ `W-061` (`33248632890`) قبلَه، **وتنجحُ على دفعِ «main»** (تشغيلُ `33251600117`) لأنَّ الاستخراجَ هناك يُبقي فرعَ `main` محليًّا | **حمرةٌ لا تدُلُّ على عَطبٍ في الشجرةِ تُعلِّمُ تطبيعَ الحمرةِ** (`RK-005`): من يقرأُ الوظيفةَ حمراءَ على كلِّ طلبِ دمجٍ يكفُّ عن قراءتِها. **ولا يُقالُ «CI أخضرُ»** إلّا مع بيانِ السياقِ: أخضرُ على دفعِ «main»، وفيه هذه الخطوةُ حمراءُ على طلباتِ الدمجِ | **عُولِجَ من سببِه في `.github/workflows/ci.yml` بأمرِ المالكِ الصريحِ «لديك كل الصلاحيات»**: خطوةٌ تُنشئُ المرجعَ المحليَّ من `origin/main` نفسِه إن غابَ، في وظيفتَي `Identity Law` و`Cross-System Suites` — فيُقاسُ الحرسُ على حالةِ الفرعِ الحاكمِ لا على شيءٍ مُصطنَعٍ. **والملفُّ كان محجوزًا** بنصِّ `DISC-006`، فالمساسُ به مُعلَنٌ هنا لا مستورٌ، وحدودُه: تجهيزُ بيئةِ القياسِ وحدَها — لا حارسٌ خُفِّفَ ولا بوّابةٌ رُفِعَت ولا قياسٌ زُيِّفَ | **مُعالَجٌ في الشجرةِ — ويُقرأُ حكمُه من CI.** ولا يُدَّعى خُضرةٌ قبلَ قراءةِ رقمِ التشغيلِ |
| DISC-026 | P2 | وظيفةُ `Sovereignty Kernel` في CI — تركيبُ التبعياتِ، مقابلَ `tests/sovereignty/test_enforcement_separation.py` | **ثلاثةُ فحوصٍ لم تُنفَّذْ قطُّ في CI وكانَ ذلك مستورًا**: الوظيفةُ تُركِّبُ `requirements-dev.txt` وحدَها ولا تُركِّبُ حزمةَ `federal/executive/services`، فسقطَ استيرادُ `amos_federation` في ثلاثةِ فحوصٍ لفصلِ الإنفاذِ الفدراليِّ بـ`ModuleNotFoundError` — **أخطاءُ تجهيزٍ لا فشلُ حكمٍ** | مقيسٌ من سجلِّ الوظيفةِ في تشغيلِ `33279646766`: `796 passed, 1 skipped, 3 errors` وثلاثةُ `ERROR at setup of Testالوصولُالفدراليّ` بنصِّ `ModuleNotFoundError: No module named 'amos_federation'`. **وكانَ مستورًا** لأنَّ بوّابةً أسبقَ في الوظيفةِ نفسِها (بوّابةُ المفتاحِ الخاصِّ · `DISC-024`) كانت تُسقِطُها قبلَ بلوغِ بوّابةِ التغطيةِ | **كلُّ دعوى «بوّاباتُ السيادةِ خضراءُ» كانت تُغطّي ثلاثةَ فحوصٍ لم تُنفَّذْ**، ومنها ما يقيسُ أنَّ موضعَ إنفاذٍ فدراليٍّ لا يملكُ بوّابةً وأنَّ الإذنَ الفدراليَّ لا يتجاوزُ ما أُعلِنَ — أي فحوصُ حدٍّ لا تفاصيلَ. وتغطيةُ الفروعِ المُعلَنةُ لتلك الوظيفةِ كانت تُحسَبُ بلا مساهمتِها | عُولِجَ في `W-064` بلا تخطٍّ ولا تخفيفٍ: يُضافُ مُجلَّدُ المصادرِ إلى مسارِ الاستيرادِ في `conftest.py` **إن لم تكنِ الحزمةُ مُركَّبةً** (‏نمطُ `src-layout`)، فتُنفَّذُ الفحوصُ الثلاثةُ حقًّا. ولم يُمَسَّ ملفُّ السيرِ المحجوزُ. وحُرِسَ العلاجُ بـ`tests/governance/test_w064_services_src_fallback.py`: يُقاسُ بمُفسِّرٍ بلا `site-packages` فتُقرأُ الحزمةُ `MISSING` بلا `conftest` و`VISIBLE` معَه. **وعُولِجَ سببُ البيئةِ كذلك في `.github/workflows/ci.yml`** (بأمرِ المالكِ الصريحِ، والملفُّ كان محجوزًا فالمساسُ مُعلَنٌ): تُركَّبُ تبعياتُ حزمةِ الخدماتِ في وظيفةِ السيادةِ بسطرٍ مسموحٍ في حارسِ إعلانِ التبعياتِ (`-r federal/executive/services/requirements.lock`) كما في وظيفةِ التاجِ — إذ قِيسَ أنَّ سلسلةَ الاستيرادِ تبلغُ `sqlalchemy` ثمَّ `structlog` ثمَّ `pydantic`، قياسًا في بيئةٍ نظيفةٍ لا تخمينًا | **مُغلَقٌ في الشجرةِ — ويُقرأُ حكمُه من CI بعدَ الدمجِ.** والحدُّ: العلاجُ يجعلُ الفحوصَ تُنفَّذُ؛ ولا يُدَّعى أنَّ بيئةَ الوظيفةِ صارَت مطابقةً لبيئةِ الإنتاجِ |
| DISC-027 | **P1** | [`THE_ROADMAP.md`](THE_ROADMAP.md) § 16.5 · § 4.3 — قيمةُ `VERIFIED` بعدَ اعتمادِ `A-2` | **الحاجزُ رُفِعَ والاستقلالُ لم يُوجَدْ**: حسمَ المالكُ `A-2` في 2026-08-30 بأنْ **يجمعَ الدورَينِ مؤقَّتًا** — قائدُ التنفيذِ وكيلُه `Driving H`، والمراجعُ «المستقلُّ» هو المالكُ نفسُه — **بشرطِ تسجيلٍ صريحٍ بأنَّ المراجعةَ ليست مستقلَّةً استقلالًا حقيقيًّا**. فطريقُ `IN_REVIEW → VERIFIED → CLOSED` انفتحَ إجراءً، **ولم تنفتحْ معه عينٌ ثانيةٌ**. ونصُّ § 4.3 «ولا ينقلُ صاحبُ التغييرِ بندَه إلى `VERIFIED`» يبقى منصوصًا وهو **مُعطَّلٌ فعلًا** ما دامَ الدورانِ في يدٍ واحدةٍ | `python tools/governance/sovereign_decision_status.py` ⇒ `A-2 · APPROVED · تاريخٌ: 2026-08-30` (كان `PENDING · —`) · وأربعةَ عشرَ بندًا انتقلَت إلى `VERIFIED` ثمَّ إلى `CLOSED` بمراجعٍ هو المالكُ، مُقيَّدًا في خليّةِ «المراجع» بنصِّها: `المالك (A-2 · غيرُ مستقلٍّ)` | **كلُّ `VERIFIED` تُوضَعُ بهذا الأساسِ تعني «الدليلُ أُعيدَ تشغيلُه وطُوبِقَ بمعيارِ القبولِ» لا «شهِدَ عليه غيرُ صاحبِه»**. وأخطرُ ما فيه أنَّ العَطبَ الذي لا يراه المنفِّذُ في عملِه لا يراه أحدٌ: لا مُنازِعَ للتأويلِ، ولا مَن يسألُ «ولِمَ هذا معيارُ قبولٍ كافٍ؟» | **بيدِ المالكِ وحدَه**: تعيينُ مراجعٍ ليسَ هو المالكَ (شخصًا أو وكيلًا مُسمًّى) — ولا يُخترَعُ له تاريخُ استحقاقٍ. والحرسُ القائمُ مقامَه إجراءً: إعادةُ تشغيلِ الدليلِ عندَ المراجعةِ وتقييدُ ما أعطاهُ التشغيلُ لا ما كُتِبَ، وحدُّ § 16.5 الثلاثيُّ · والخطرُ `RK-020` | **مُغلَقٌ بـQ-43 (2026-09-08)**: عُيِّنَ مجلسُ نماذجَ (GPT 5.6 + Grok 4.6) مراجعًا ليسَ هو المالكَ، فانتفتِ العلّةُ («المراجعةُ ليست مستقلَّةً») التي كانت قائمةً ما دامَ الدورانُ في يدٍ واحدةٍ. كانَ قبلَه قائمًا — ولا يُغلَقُ باعتمادِ القرارِ: القرارُ رفعَ الحاجزَ ولم يُنشئِ الاستقلالَ. **والحدُّ مُعلَنٌ**: المجلسُ يُدِرُه المنسِّقُ فلا استقلالٌ تامٌّ؛ والمالكُ يحسمُ القراراتِ السياديّةَ والمجلسُ يُحقِّقُ الدليلَ. |
| DISC-028 | **P1** | عقدُ دمجِ `W-051`…`W-063` مقابلَ § 7 واجب 6 وسابقةِ `W-040` | **ثلاثةَ عشرَ قيدًا صارَت حالةَ الدولةِ ولم يُشاهَدْ عليها حكمُ CI أخضرَ يومَها**: ثمانيةٌ (`W-051`…`W-058`) دُفِعَت مباشرةً إلى `main` **بلا تشغيلٍ واحدٍ** (‏`DISC-006`: دقائقُ الحسابِ السابقِ)، وخمسةٌ (`W-059`…`W-063`) دُمِجَت بطلباتِ دمجٍ وتشغيلُ CI على عقدِ دمجِها **أحمرُ كلُّه** — والحمرةُ من سببٍ واحدٍ لا يخصُّ عملَها (`DISC-024`) عُولِجَ في `W-064`. فسابقةُ `W-040` كانت تُقرأُ على ثلاثةَ عشرَ قيدًا: «مدفوعٌ لا مُثبَت» | ‏`GET /actions/runs?head_sha=` لكلِّ عقدةٍ: `9561d9a`…`eafc38e` ⇒ **لا تشغيلَ** · `764a3dc` ⇒ `33217559878` **failure** · `6a7e571` ⇒ `33219913887` **failure** · `7f6ccf1` ⇒ `33251600117` **failure** · `3356d71` ⇒ `33278679341` **failure** · `0803ae2` ⇒ `33278721516` **failure** · و`17ab3c4` ⇒ `33306534739` **success (13/13 وكلُّها بخطواتٍ)** · و`3e10398` ⇒ `33308285078` **success 13/13** | **الإغلاقُ استندَ إلى خُضرةِ حالةِ الدولةِ لا إلى خُضرةِ عقدةِ الدمجِ**: `17ab3c4` يحملُ شِفرةَ الثلاثةَ عشرَ كلَّها، فحكمُه حكمٌ عليها مجتمعةً — **وليسَ** حكمًا على كلِّ واحدةٍ منفردةً في لحظةِ دمجِها. ومن أرادَ أن يعرفَ أيَّ قيدٍ كان أخضرَ وحدَه لا يجدُ ذلك في هذا المستودعِ، ولا يُصنَعُ الجوابُ بإعادةِ كتابةِ التاريخِ | يُقيَّدُ الحكمُ كما كانَ في خليّةِ «قيدُ السجلّ» لكلِّ بندٍ (غائبًا · أحمرَ برقمِه · أخضرَ برقمِه) ولا يُطوى · ومن اليومِ يُقرأُ الحكمُ بعدَ كلِّ دفعةٍ بـ`tools/governance/ci_verdict_readability.py` قبلَ أيِّ إغلاقٍ، فالبندُ الذي لا يُقرأُ حكمُ دفعتِه لا يُغلَقُ (‏نُفِّذَ في `WI-020` نفسِه: بقيَ `IN_REVIEW` حتّى قُرِئَ حكمُه) · **حرسٌ قائمٌ**: `tools/governance/ci_verdict_readability.py` — يُقرأُ به حكمُ كلِّ دفعةٍ خطوةً خطوةً قبلَ أيِّ إغلاقٍ، فلا تُشترى خُضرةُ فردٍ بخُضرةِ مجموعٍ | **مُغلَقٌ وصفًا — ومفتوحٌ حدًّا**: الواقعةُ مُقيَّدةٌ كما كانت، ولا يُدَّعى لها تصحيحٌ؛ والحدُّ أنَّ خُضرةَ المجموعِ ليست خُضرةَ كلِّ فردٍ |
| DISC-029 | P3 | `tools/governance/check_root_file_names.py --source disk` مقابلَ `.gitignore` وأثرِ تشغيلِ الانحدارِ | **وضعُ قراءةِ القرصِ يُحمِّرُ على أثرِ اختبارٍ مُستبعَدٍ من الفهرسةِ**: تشغيلُ الانحدارِ يُنشئُ `amos_federation.db` في **جذرِ** المستودعِ، وهو مُستبعَدٌ بـ`*.db` في `.gitignore` فلا يُقيَّدُ أبدًا — ومع ذلك يُعلِنُه وضعُ `--disk` مخالفةً `UNDECLARED_ROOT_FILE`. فالبوّابةُ نفسُها خضراءُ في CI (‏تقرأُ `git ls-files`) وحمراءُ على قرصِ كلِّ مَن أعادَ تشغيلَ الانحدارِ | `python tools/governance/check_root_file_names.py .` ⇒ خروجٌ 0 · و`… --source disk` ⇒ خروجٌ 1 بمخالفةٍ واحدةٍ: `UNDECLARED_ROOT_FILE: amos_federation.db` · و`git check-ignore -v amos_federation.db` ⇒ `.gitignore:24:*.db` · و`git status --porcelain` نظيفٌ | **حمرةٌ محليّةٌ لا تدُلُّ على عَطبٍ في المستودعِ تُعلِّمُ تطبيعَ الحمرةِ** (`RK-005`): مَن يشغِّلُ وضعَ القرصِ بعدَ الانحدارِ يقرأُ مخالفةً لا يستطيعُ إصلاحَها بغيرِ حذفِ ملفٍّ. والعَطبُ الحقيقيُّ تحتَها أنَّ اختبارًا يكتبُ في **جذرِ** المستودعِ لا في دليلٍ موقوتٍ | بندٌ تالٍ في `tooling-gates`، وثلاثُ طرقٍ لا رابعَ: توجيهُ الاختبارِ إلى `tmp_path` · أو إعلانُ أثرٍ مُستبعَدٍ في قائمةِ الجذرِ بسببٍ مكتوبٍ · أو قصرُ وضعِ القرصِ على المُفهرَسِ — **والاختيارُ يمسُّ حدَّ بوّابةٍ فيُعلَنُ في بندِه**، ولا يُخفَّفُ الحرسُ هنا. **والحرسُ قائمٌ يُعادُ بأمرٍ واحدٍ**: `tools/governance/check_root_file_names.py` في وضعِ `--source disk` يخرُجُ بواحدٍ ما دامَ الأثرُ في الجذرِ ويعودُ إلى صفرٍ يومَ يزولُ سببُه، فلا يتَّسِعُ العَطبُ صامتًا · **حرسٌ قائمٌ**: `tools/governance/check_root_file_names.py` · `tests/governance/test_runtime_state_stays_outside_the_tree.py` — الأوّلُ في وضعِ `--source disk` يخرُجُ بواحدٍ ما دامَ الأثرُ في الجذرِ ويعودُ إلى صفرٍ يومَ يزولُ سببُه، والثاني ستُّ أوجهٍ تُثبِتُ بقاءَ حالةِ التشغيلِ خارجَ الشجرةِ | مفتوحٌ — **والأثرانِ عُزِلا في مصدرِهما** (`W-087` · `WI-026`): موضعُ قاعدةِ التشغيلِ يُعلَنُ في البيئةِ لمدى التشغيلِ، ورابطُ قاعدةِ اختبارِ حزمةِ الخدماتِ صارَ مُطلَقًا خارجَ الشجرةِ، فـ`check_root_file_names.py --source disk` يخرُجُ بصفرٍ بعدَ تشغيلِ الاختباراتِ — **بلا تخفيفِ بوّابةٍ ولا استثناءٍ في قائمةِ الجذرِ ولا نقلٍ للعقدِ الإنتاجيِّ**، وحرسُه ستُّ أوجهٍ في `tests/governance/test_runtime_state_stays_outside_the_tree.py`. ويبقى مفتوحًا حتّى يُقرأَ حكمُ CI ويُراجَعَ البندُ · وسجلُّ الذرّيّةِ `amos_federation_test_idempotency.json` يُكتَبُ في مجلَّدِ الخدماتِ لا في الجذرِ ولم يُعالَجْ هنا |
| DISC-030 | P2 | `tools/governance/check_work_governance.py` — سببُ الإبلاغِ ونافذةُ الالتزامِ الواحدِ، مقابلَ § 13.3 · 4 | **اعتمادُ `A-2` أزالَ العلّةَ المكتوبةَ لوضعِ الإبلاغِ ولم يُزِلْ علّةً أخرى لم تكنْ مكتوبةً**: كان نصُّ السببِ في الشِفرةِ يقولُ «طريقُ الإغلاقِ مقطوعٌ ما دامَ `A-2` معلَّقًا»، وقد صارَ `A-2` `APPROVED` — فالنصُّ **كذبٌ في الشِفرةِ** لو بقيَ. ولكنَّ جعلَ `POST_MERGE_NOT_CLOSED` مُسقِطًا افتراضًا يُحمِّرُ نافذةً حقيقيّةً: القيدُ يُدفَعُ **مع** العملِ (§ 7 واجب 1) فيصيرُ حالةَ الدولةِ لحظةَ الدمجِ، والإغلاقُ لا يصدُقُ قبلَ الدمجِ ولا يُقرأُ حكمُ CI قبلَه (§ 7 واجب 6) — فبينَ الدمجِ والإغلاقِ **التزامٌ واحدٌ لا مهربَ منه** | ‏`check_work_governance.py --self-check --enforce-post-merge` ⇒ **خروجٌ 1 بـ14 مخالفةً** قبلَ `W-065` وفي دفعتِه كذلك (‏الأربعةَ عشرَ `VERIFIED`)، ويصيرُ **خروجًا 0** في دفعةِ الإغلاقِ · والنصُّ المُصلَحُ يُقاسُ بأنَّ عبارةَ «ما دام `A-2` معلّقًا» لم تبقَ في الشِفرةِ | **إبلاغٌ سببُه كاذبٌ أسوأُ من إبلاغٍ بلا سببٍ**: مَن يقرأُ «`A-2` معلَّقٌ» بعدَ اعتمادِه يبني على حالةٍ زالَت. والوجهُ الآخرُ: إسقاطٌ افتراضيٌّ يُحمِّرُ عملًا صحيحًا في نافذةِ التزامٍ واحدٍ، فيتعلَّمُ العاملُ أن يُغلِقَ قبلَ الدمجِ — أي أن يكتُبَ دعوى مكانَ قيدٍ | **نصُّ السببِ صُحِّحَ في `W-065`** ليقولَ العلّةَ القائمةَ (نافذةُ الالتزامِ الواحدِ) لا الزائلةَ · **وتفعيلُ الإسقاطِ افتراضًا قرارُ `A-3` بيدِ المالكِ**، ولا يُستنتَجُ من اعتمادِ `A-2`؛ ومَن أرادَه اليومَ فبـ`--enforce-post-merge` صريحًا | مفتوحٌ — الشِقُّ المكتوبُ صُحِّحَ، والشِقُّ السياديُّ (`A-3`) بيدِ المالكِ |
| DISC-031 | P2 | `tools/governance/state_document_drift.py` مقابلَ حقلِ «Last Completed Work» في `PROJECT_STATE.md` | **حرسُ الانحرافِ يقيسُ أقصى قيدٍ **مذكورٍ في الملفِّ** لا القيدَ الذي يُعلِنُه الملفُّ حالةً**، فحقلُ «Last Completed Work» بقيَ يقولُ `W-057 · 2026-08-27` والسجلُّ عندَ `W-064` — **تأخُّرُ ثمانيةِ قيودٍ** لم تُسقِطْه البوّابةُ، لأنَّ ذكرَ `W-064` في ترويسةِ «تاريخِ آخرِ تعديلٍ» أرضاها. ومعه: حقلُ «المستودع» كانَ يُعلِنُ `zoorooz/AMOS-Fedration` بعدَ انتقالِ الحسابِ إلى `xoos-beep` (‏`DISC-006`)، و«Central Ledger» يقولُ «W-000 إلى W-057 — 57 صفًّا» والصفوفُ 65 | قبلَ `W-065`: `state_document_drift.py` ⇒ خروجٌ **0** ووثائقُ الحالةِ «0 متأخِّرةٌ»، وفي الملفِّ نفسِه السطرُ 24 يقولُ `W-057` والسطرُ 27 يقولُ «57 صفًّا» · وبعدَ التصحيحِ: «قيودٌ مذكورةٌ 19» و«أحدثُ قيدٍ مذكورٍ W-065» | **حرسٌ يُطمئِنُ على وثيقةٍ تكذِبُ**: مَن يبدأُ من `PROJECT_STATE.md` — وهي بابُ الدخولِ المُعلَنُ للوكيلِ القادمِ — يقرأُ حالةَ ثمانيةِ قيودٍ ماضيةً ويُرسَلُ إلى مستودعٍ ليسَ مستودعَ العملِ، والبوّابةُ خضراءُ. وهذا **أخطرُ من غيابِ حرسٍ**: الخُضرةُ تُسكِتُ السؤالَ | صُحِّحَ المحتوى في `W-065` (العنوانُ · الحقلُ · عددُ الصفوفِ · صفوفُ W-058…W-065) — **والعَطبُ في القياسِ باقٍ**: بندٌ تالٍ في `tooling-gates` يجعلُ الحرسَ يقرأُ **الحقلَ المُعلَنَ حالةً** بمِرساةٍ مُسمّاةٍ في الوثيقةِ لا أقصى ذكرٍ، ويُسقِطُ عندَ تأخُّرِه؛ **والحرسُ القائمُ مقامَه اليومَ مُسمًّى لا مُرتَجًى**: `tools/governance/state_document_drift.py` يُشغَّلُ في كلِّ دفعةٍ ويطبعُ لكلِّ وثيقةٍ «أحدثُ قيدٍ مذكورٍ» و«قيودٌ مذكورةٌ»، فتفاوتُهما عن رأسِ السجلِّ يُقرأُ رقمًا لا ظنًّا، ويبقى العَطبُ مقيسًا حتّى يصيرَ مُسقِطًا · **والقياسُ صارَ يقرأُ موضعَ الدعوى في `W-090`**: `STATE_FIELD_ANCHORS` تُسمّي لكلِّ وثيقةِ حالةٍ **حقلَها المُعلَنَ** (`Last Completed Work` في `PROJECT_STATE.md` · ترويسةُ «تاريخِ آخرِ تعديلٍ» في `PROJECT_HANDBOOK.md`)، فتأخُّرُ الحقلِ يُسقِطُ بـ`STATE_DOC_FIELD_BEHIND` بعددِ القيودِ ولو ذُكِرَ الأحدثُ في مكانٍ آخرَ، **وغيابُ المِرساةِ نفسِها يُسقِطُ** بـ`STATE_DOC_FIELD_ANCHOR_MISSING` فلا يُشترى صمتٌ بإعادةِ تسميةِ حقلٍ · وشكلُ هذا القيدِ بعينِه محروسٌ بوجهٍ (‏حقلٌ عندَ `W-003` والأحدثُ مذكورٌ في سطرٍ آخرَ ⇒ يُسقِطُ) · ولا يُقرأُ هذا إغلاقًا: الحالةُ تُقرأُ بعدَ حكمٍ مقروءٍ ومراجعةٍ (§ 4.3) · **حرسٌ قائمٌ**: `tools/governance/state_document_drift.py` — يُشغَّلُ في كلِّ دفعةٍ ويُسقِطُ بـ`STATE_DOC_FIELD_BEHIND` عندَ تأخُّرِ الحقلِ المُعلَنِ وبـ`STATE_DOC_FIELD_ANCHOR_MISSING` عندَ غيابِ المِرساةِ — وهو حرسُ القائمِ لا إغلاقُ العَطبِ | مفتوحٌ |
| DISC-032 | **P1** | `tests/governance/test_w054_post_merge_reverse_link.py::test_الإسقاطُ_يُطلَبُ_فيسقُط` | **فحصٌ كانَ يخضَرُّ من عَطبِ المستودعِ لا من صدقِ دعواهُ**: كان يُشغِّلُ `check_work_governance.py` **من مسارِها الحقيقيِّ** في عمليّةٍ منفصلةٍ ويؤكِّدُ خروجًا 1، و`REPO_ROOT` في الأداةِ مُشتَقٌّ من **موضعِ الملفِّ** لا من `cwd`، و`monkeypatch.setattr` **لا يعبُرُ إلى عمليّةٍ أخرى**. فالشجرةُ المؤقَّتةُ التي بناها الفحصُ **لم تُقرَأْ أصلًا**، والخروجُ 1 جاءَ من أربعةَ عشرَ واجبًا مُهمَلًا في المستودعِ الحقيقيِّ. فيومَ وُفِّيَ الواجبُ في `W-065` **احمَرَّ الفحصُ من الإصلاحِ** | قبلَ `W-065`: الفحصُ أخضرُ · وبعدَ إغلاقِ الأربعةَ عشرَ: `1 failed, 2188 passed, 1 skipped` وموضعُه السطرُ 248 · و`--merge-base لا-وجود-له` في الفحصِ المجاورِ يخرُجُ بـ2 **من الشجرةِ الحقيقيّةِ كذلك** — وهو ما دلَّ على أنَّ العمليّةَ المنفصلةَ لا ترى الشجرةَ المؤقَّتةَ | **فحصٌ لا يقيسُ ما يُعلِنُ أنَّه يقيسُه أخطرُ من غيابِ فحصٍ**: كان يُعطي ثقةً بأنَّ «العلمَ يُسقِطُ» وهو في الحقيقةِ يقيسُ «هل في المستودعِ واجبٌ مُهمَلٌ الآنَ». وأيُّ إصلاحٍ للمستودعِ كانَ سيُحمِّرُه — أي أنَّه كانَ **مِسمارًا يعاقِبُ الإصلاحَ**. وهذا يمسُّ دعوى `W-058` (‏«الفحوصُ تكشِفُ» مقيسةً بمِسبرِ الطفراتِ): المِسبرُ يقيسُ **الإسقاطَ عندَ الطفرةِ** ولا يقيسُ **أنَّ الفحصَ يقرأُ الشجرةَ التي يُعلِنُها** | أُصلِحَ في `W-065`: تُنسَخُ الأداةُ إلى العمقِ النسبيِّ ذاتِه (`tools/governance/`) داخلَ الشجرةِ المؤقَّتةِ فيصيرُ `REPO_ROOT` هو الشجرةَ المقصودةَ، وأُضيفَ فحصٌ مقابلٌ يُثبِتُ أنَّ **العلمَ** هو المُسقِطُ لا وجودُ الواجبِ (‏بلا العلمِ: خروجٌ 0 والإبلاغُ مطبوعٌ). **والعَطبُ الأعمُّ باقٍ**: `grep -rn "monkeypatch.setattr" tests/ \| grep -l subprocess` يُظهِرُ نمطًا قد يتكرَّرُ في فحوصٍ أخرى؛ بندٌ تالٍ في `tests-integrity` يجعلُ «الفحصُ يقرأُ شجرتَه» مقيسًا بحرسٍ لا بعينٍ — والحرسُ القائمُ مقامَه اليومَ `tools/governance/mutation_probe.py` يُشغَّلُ في كلِّ دفعةٍ فيُظهِرُ الفحوصَ التي لا تُسقِطُ عندَ الطفرةِ **وهذه الدعوى نفسُها بقيَت بلا طريقِ إنفاذٍ مقيسٍ حتّى `W-077`**: لا خطوةَ تشغيلٍ في CI تُسمّي المِسبارَ، ولا فحصَ في CI يحكُمُ به على الشجرةِ الحاضرةِ — قِيسَ ذلك وقُيِّدَ في `DISC-037`، ثمَّ رُبِطَ المِسبارُ ببوّابةٍ في وظيفةِ الهُويّةِ بكلفةٍ مقيسةٍ (‏4م 31ث · 31 وثبةً · 31 مُلتقَطةً). والنصُّ الأوّلُ باقٍ كما كُتِبَ: التاريخُ لا يُعادُ كتابتُه. **والنمطُ حُصِيَ في `W-096`**: 24 تشغيلَ أداةٍ في عمليّاتٍ فرعيّةٍ داخلَ `tests/` · 19 تقصِدُ المستودعَ الحقيقيَّ عن عمدٍ · 5 تقصِدُ شجرةً مؤقَّتةً · 4 مِرساتُها صادقةٌ · **وواحدٌ يحكُمُ على شجرةٍ لا يقرؤها** (`tests/governance/test_w054_post_merge_reverse_link.py:216`) — وهو الموضعُ الذي سمّاهُ هذا الصفُّ بنفسِه يومَ كُتِبَ: «و`--merge-base لا-وجود-له` في الفحصِ المجاورِ يخرُجُ بـ2 من الشجرةِ الحقيقيّةِ كذلك». ووُجِّهَ إلى [`WI-029`](ACTIVE_WORK.md) في نطاقِ `tests-root` — لا في نطاقٍ يُنشَأُ. | مفتوحٌ — النمطُ حُصِيَ ووُجِّهَ (`WI-029` · `W-096`)، والحرسُ لم يُكتَبْ بعدُ |
| DISC-033 | P3 | `tests/governance/test_w063_registered_claims.py::test_declared_gap_covers_every_ledger_entry_that_claims_a_mutation` | **حرسُ «كلُّ قيدٍ يدَّعي طفرةً يُعلِنُ حالتَه» يقيسُ الدعوى بجذرِ كلمةٍ لا بمعناها**: يفتِّشُ صفوفَ السجلِّ عن `طفر` أو `تحوّر`، والجذرُ `طفر` مشتركٌ بينَ **الطفرةِ** (‏عَطبٌ يُزرَعُ في مِسبرِ الفحوصِ) و**الطفرِ** (‏الوثبُ · القفزُ). فقيدُ `W-065` قالَ «§ 4.3 لا يُجيزُ **الطفرَ** من `IN_REVIEW` إلى `CLOSED`» — وهي عبارةُ حالةٍ لا دعوى مِسبرٍ — فقرأَها الحرسُ دعوى طفرةٍ غيرَ مُعلَنةٍ وأسقطَ بوّابةَ الهويةِ في CI | ‏`pytest tests/governance -q` محليًّا **قبلَ** إعادةِ صياغةِ صفِّ `W-065` ⇒ نجحَ، وبعدَها في CI ⇒ `AssertionError: قيودٌ تدَّعي طفرةً ولا تُعلَنُ حالتُها: ['W-065']` · تشغيلُ `33312805372` · الوظيفةُ `Identity Law (E3)` · وموضعُه `test_w063_registered_claims.py:117` | **سالبٌ لا موجبٌ: يُحمِّرُ على قيدٍ صادقٍ**، ويدفعُ العاملَ إلى واحدٍ من طريقَين كلاهما ضارٌّ — أن يُدرِجَ القيدَ في `UNREGISTERED_WORK` فيَكذِبَ بإعلانِ دعوى مِسبرٍ لم يَدَّعِها، أو أن يُخفِّفَ الحرسَ. **والثمنُ الحقيقيُّ أنَّ لغةَ السجلِّ صارت مُقيَّدةً بكلمةٍ**: كاتبُ القيدِ يتجنَّبُ لفظًا عربيًّا صحيحًا لا لمعناهُ بل لجذرِه | **لا تخفيفَ ولا كذبَ**: أُعيدَت صياغةُ `W-065` بلفظِ «الوثبُ» — أدقُّ في موضعِه أصلًا — و`UNREGISTERED_WORK` **لم تُمَسّ**. وبندٌ تالٍ في `tests-integrity` يجعلُ القراءةَ **بدعوى مُعلَنةٍ صريحةٍ** (‏إشارةٌ إلى `mutation_probe.py` أو صيغةٌ محجوزةٌ) لا بجذرِ كلمةٍ في نصٍّ حرٍّ. **والحرسُ القائمُ مقامَه اليومَ مُسمًّى**: `tests/governance/test_w063_registered_claims.py` يُشغَّلُ في كلِّ دفعةٍ فيُسقِطُ عندَ كلِّ قيدٍ يذكرُ الجذرَ، فلا يتَّسِعُ العَطبُ صامتًا · **حرسٌ قائمٌ**: `tests/governance/test_w063_registered_claims.py` — يُشغَّلُ في كلِّ دفعةٍ فيُسقِطُ عندَ كلِّ قيدٍ يذكرُ جذرَ الكلمةِ بلا دعوى مُعلَنةٍ، فلا يتَّسِعُ العَطبُ صامتًا | مفتوحٌ |
| DISC-034 | **P1** | الفرعُ `origin/develop` مقابلَ `main` — نسبُ الالتزامِ لا محتوى ملفٍّ | **إصلاحُ عَطبَي قياسٍ كُتِبَ ودُفِعَ ولم يبلُغِ الفرعَ الحاكمَ قطُّ، فحُلَّ العَطبانِ مرّةً ثانيةً حلًّا آخرَ وبقيَ في `main` ما هو أضعفُ**: الالتزامُ `37156e6` («إصلاحُ عطبَي القياسِ… DISC-021 وDISC-022») **ليسَ سلفًا لـ`main`**، وهو رأسُ `origin/develop`، وأساسُ الافتراقِ `e5efe9c` (دمجُ `#29`). ففيه كانت بوّابةُ السيادةِ 6 تُنادي الماسحَ المرجعيَّ (`--tree-pem-only`) وبوّابةُ حوكمةِ العملِ تقبَلُ `--repo-root`؛ وفي `main` بقيَ في `ci.yml` **ماسحٌ ثانٍ مكتوبٌ بيدٍ** (`grep -rlE` على نمطِ كتلةِ PEM) ومحلُّ القياسِ يُشتَقُّ من موقعِ الملفِّ. و`DISC-022` عُثِرَ عليه ثانيًا وقُيِّدَ باسمٍ جديدٍ `DISC-032` وحُلَّ بنَسخِ الأداةِ إلى شجرةٍ مؤقَّتةٍ — دواءٌ يُصيبُ ويقيسُ نُسخةً لا الأداةَ المنشورةَ | `git merge-base --is-ancestor 37156e6 HEAD` ⇒ **خروجٌ 1 (ليسَ سلفًا)** · `git branch -a --contains 37156e6` ⇒ `remotes/origin/develop` وحدَه · `git merge-base 37156e6 HEAD` ⇒ `e5efe9c` · `git log --oneline HEAD..37156e6` ⇒ التزامٌ **واحدٌ** · بحثُ `git log -S` عن نمطِ ترويسةِ مفتاحٍ خاصٍّ في `.github/workflows/ci.yml` على `--all` ⇒ `37156e6` و`9cf849e` · و`grep -n tree-pem-only .github/workflows/ci.yml` على `main` قبلَ `W-069` ⇒ **لا نتيجةَ** · وملفُّ فحصٍ كاملٌ في الفرعِ ولا وجودَ له في `main` باسمِ `test_secret_boundary_declaration` تحتَ `tests/crown` (124 سطرًا) — يُذكَرُ اسمًا لا مِرساةً إذ لا يحمِلُ شيئًا في هذه الشجرةِ | **عملٌ مقيسٌ صحيحٌ سقطَ من حالةِ الدولةِ بلا أن يُعلِنَ أحدٌ سقوطَه** — وهو عينُ ما وقعَ في `W-038`/`W-039` («دفعٌ فقدَ مساراتِه ودُمِجَ»)، فصارَ نمطًا لا حادثةً. وثمنُه ثلاثةٌ: جهدٌ أُعيدَ من الصفرِ · حدٌّ بقيَ أضعفَ مدّةً كاملةً وفي المستودعِ ما يرفعُه · واكتشافٌ واحدٌ بمعرِّفَينِ (`DISC-022` و`DISC-032`) فالسجلُّ يُحصي عَطبًا واحدًا مرّتَينِ. **ولا يُقالُ إنَّ الفرعَ خَلْفٌ فيُهمَل**: فيه ما ليسَ في `main` | **الشِقُّ الهندسيُّ نُفِّذَ في `W-069`** (البندُ `WI-021`): ما بقيَ صالحًا من الفرعِ استُنقِذَ إلى `main` — البوّابةُ 6 تُنادي الماسحَ المرجعيَّ وحدَه، و`--repo-root` صارَ رايةً مُعلَنةً يستعملُها فحصُ `W-054` بدلَ نَسخِ الأداةِ، وحُرِسَ الحدَّانِ بـ`tests/governance/test_w069_single_scanner_and_explicit_root.py`. **والشِقُّ الثاني قرارُ مستودعٍ بيدِ المالكِ لا يملكُه التنفيذُ**: دمجُ `origin/develop` أو إعلانُ إسقاطِه أو حذفُه — والخارطةُ لا تمنحُ التنفيذَ سلطةَ حذفِ فرعٍ منشورٍ ولا إعلانَ إسقاطِ عملٍ مدفوعٍ (§ 2 حكم 12) · والخطرُ `RK-021` · **حرسٌ قائمٌ**: `tests/governance/test_w069_single_scanner_and_explicit_root.py` — يحرسُ الشِقَّ الهندسيَّ وحدَه (‏ماسحٌ مرجعيٌّ واحدٌ للبوّابةِ 6 · و`--repo-root` رايةً مُعلَنةً بدلَ نَسخِ الأداةِ) — وقرارُ `origin/develop` بيدِ المالكِ لا حرسَ له ولا يُدَّعى | **مفتوحٌ — الشِقُّ الهندسيُّ نُفِّذَ، وقرارُ الفرعِ بيدِ المالكِ.** ولا يُغلَقُ باستنقاذِ المحتوى: الفرعُ ما زالَ مفترقًا وفيه ملفُّ حرسٍ لم يُقَسْ في `main` · **تصحيحُ قياسٍ 2026-08-31 (`W-072`) — مُعلَنٌ لا مُمحًى**: القياسُ الأوّلُ هنا أخذَ `37156e6` رأسًا للفرعِ فبنى عليه «التزامٌ **واحدٌ**»؛ والقياسُ المُعادُ يقولُ: رأسُ `origin/develop` هو **`e0be191`** (2026-08-28) والالتزاماتُ التي ليست في `main` **خمسةٌ** لا واحدٌ (`37156e6` · `2105099` W-060 · `1d8562a` W-061 · `3c9038f` W-062 · `e0be191`)، وأساسُ الافتراقِ `e5efe9c` كما قِيسَ، و`main` متقدِّمٌ بـ**ثمانيةٍ وعشرينَ** التزامًا. فما استُنقِذَ في `W-069` كانَ من **واحدٍ** من الخمسةِ، وأربعةٌ باقيةٌ لم يُقَسْ محتواها بندًا بندًا — والدعوى الأولى كانت أضيقَ من الواقعِ فصارَ القرارُ أثقلَ لا أخفَّ. وانضمَّ إلى ذلك تصادمُ معرِّفاتِ السجلِّ (`DISC-036`): فلا يُدمَجُ الفرعُ بلا ترقيمٍ من جديدٍ |
| DISC-035 | **P1** | `docs/governance/work/ACTIVE_WORK.md` — صفُّ § 2 مقابلَ كتلةِ § 3 · و`tools/governance/check_work_governance.py` | **حالةُ البندِ مكتوبةٌ في موضِعَينِ وكانَ الحرسُ يقرأُ أحدَهما، فمرَّ إغلاقٌ نصفُه مكتوبٌ**: `W-067` نقلَ أربعةَ عشرَ **صفًّا** إلى `CLOSED` وتركَ كتلَ § 3 تقولُ `VERIFIED`، فصارَ لكلِّ بندٍ منها حالتانِ مكتوبتانِ إحداهما كاذبةٌ قطعًا — والبوّابةُ خضراءُ، لأنَّ `parse_items` تقرأُ الصفَّ و`parse_detail_blocks` لا يُقاسُ منها سطرُ «الحالة:» أصلًا | قِيسَ عندَ الرأسِ `899b422` بحرسٍ أُضيفَ لهذا الغرضِ: `check_work_governance.py --self-check` ⇒ **14 مخالفةَ `STATUS_CONTRADICTION`** بأسمائِها (`WI-006`…`WI-019`)، وقبلَ إضافتِه ⇒ **صفرُ مخالفاتٍ** · و`grep -c '^الحالة:' ACTIVE_WORK.md` ⇒ 20 سطرًا · وستَّةَ عشرَ صفًّا `CLOSED` مقابلَ أربعةَ عشرَ كتلةً `VERIFIED` | **§ 2 حكم 14 يقولُ إنَّ ما كُتِبَ هو حالةُ المشروعِ — فمكتوبانِ متناقضانِ حالتانِ لا حالةٌ**، ومَن قرأَ الكتلةَ (وهي موضعُ التفصيلِ الذي يُقرأُ عندَ العملِ) بنى على حالةٍ زالَت. والأخطرُ أنَّ الخُضرةَ أسكتَتِ السؤالَ: بوّابةٌ تُطمئِنُ على سجلٍّ يُناقِضُ نفسَه أضرُّ من غيابِ بوّابةٍ | **عُولِجَ من سببِه في `W-069`**: أُضيفَ `check_block_status_agrees` فصارَ التناقضُ يُسقِطُ بـ`STATUS_CONTRADICTION`، وكتلةٌ نشِطةٌ بلا سطرِ حالةٍ تُرصَدُ `MALFORMED_ITEM` — **حرسٌ زِيدَ لا خُفِّفَ**؛ وصُحِّحَت الأربعةَ عشرَ كتلةً إلى `CLOSED` بسطرِ إقرارٍ يُعلِنُ التناقضَ الذي كانَ ولا يطويه. وحُرِسَ بـ`tests/governance/test_w069_status_contradiction.py` بطفرةٍ: يُزرَعُ التناقضُ فيُرصَدُ، ويُرفَعُ فتسكُتُ | مُعالَجٌ بالبندِ `WI-021` (`W-069`) — والحدُّ مُعلَنٌ: الحرسُ يقيسُ **اتفاقَ الحرفِ** لا صدقَ الحالةِ نفسِها |
| DISC-036 | **P1** | معرِّفاتُ سجلِّ الإكمالِ بينَ `main` و`origin/develop` | **معرِّفٌ واحدٌ يُسمّي عملَينِ مختلفَينِ في فرعَينِ، فالسجلُّ لا يُدمَجُ دمجًا نصيًّا ولو أُريدَ**: `W-060` و`W-061` و`W-062` مُستعمَلةٌ في الفرعَينِ كلَيهما لأعمالٍ **مختلفةٍ** — في `main` (إرساءُ قيودِ الطفرةِ · اتِّجاهُ دَينِ الأسطحِ · خمسةَ عشرَ عملًا يُعادُ تشغيلُها) وفي `develop` (صدقُ القياسِ: قياسُ الإصلاحِ في التكاملِ · كشفُ بقيّةِ ماسحاتِ مادةِ المفتاحِ · إنهاءُ تضادِّ الحارسَينِ). وسجلُّ الفرعِ يقفُ عندَ `W-054` صفًّا أقصى فيما `main` بلغَ `W-071` | قراءةُ سجلِّ الفرعِ بـ`git show origin/develop:docs/audit/COMPLETION_LEDGER.md` ثمَّ تصفيةُ صفوفِ `W-060` و`W-061` و`W-062` بـ`grep` ⇒ ثلاثةُ صفوفٍ بعناوينَ تخالفُ عناوينَ `main` لنفسِ المعرِّفاتِ · وتصفيةُ كلِّ معرِّفاتِ الصفوفِ في سجلِّ الفرعِ ⇒ أقصاهُ `W-054` · و`git rev-list --count HEAD..origin/develop` ⇒ **5** · و`git rev-list --count origin/develop..HEAD` ⇒ **28** | **قرارُ الفرعِ صارَ أثقلَ لا أخفَّ**: دمجٌ نصيٌّ يُنتِجُ سجلًّا فيه معرِّفٌ مكرَّرٌ بمعنيَينِ — وهو خرقُ أساسٍ في مِرساةِ القيدِ (كلُّ `W-###` يُسمّي عملًا واحدًا). فالخياراتُ ثلاثةٌ لا اثنانِ: ترقيمٌ من جديدٍ ثمَّ دمجٌ · أو إعلانُ إسقاطٍ مع سببٍ · أو حفظٌ مُعلَنٌ بلا دمجٍ | **بيدِ المالك — قرارُ مستودعٍ** — التنفيذُ لا يملكُ ترقيمَ سجلٍّ منشورٍ في فرعٍ آخرَ ولا حذفَ فرعٍ (§ 2 حكم 12) · والخطرُ `RK-021` · والبندُ `WI-022` في ACTIVE_WORK | **مفتوحٌ — القرارُ بيدِ المالكِ** |
| DISC-037 | P2 | سجلّا `DISCOVERIES.md § 1` و`RISK_REGISTER.md § 1` مقابلَ `.github/workflows/ci.yml` | **دعوى «يُشغَّلُ في كلِّ دفعةٍ» كانت تُكتَبُ سندًا لبقاءِ قيدٍ مفتوحًا بلا أن يقيسَها شيءٌ**: قيودٌ مفتوحةٌ تُبرِّرُ بقاءَها بأنَّ حرسًا قائمًا مقامَ الإصلاحِ «يُشغَّلُ في كلِّ دفعةٍ» — والعبارةُ لم تكن مقيسةً في بوّابةٍ قطُّ. وأوّلُ قياسٍ لها أظهرَ **دعوى واحدةً بلا طريقِ إنفاذٍ أصلًا** (`DISC-032` · `tools/governance/mutation_probe.py`): لا خطوةَ CI تُسمّيه، ولا فحصَ في CI يحكُمُ به على الشجرةِ الحاضرةِ. ومعه عَطبٌ أعمُّ: **سبعةَ عشرَ** أداةً حاكمةً في `tools/governance` و`tools/audit` لا تُسمّيها خطوةُ تشغيلٍ في CI، منها `state_document_drift.py` و`open_record_accountability.py` — فحكمُهما على الشجرةِ الحاضرةِ متروكٌ لذاكرةِ المنفِّذِ لا لبوّابةٍ | `python tools/governance/guard_enforcement_closure.py` عندَ الرأسِ `2508ce5` ⇒ «دعاوى مقروءةٌ 4 · منفَذةٌ 3 · غيرُ منفَذةٍ 1» ومخالفةٌ واحدةٌ `PUSH_CLAIM_UNENFORCED` على `DISC-032` · و«أدواتٌ لا تُسمّيها خطوةُ تشغيلٍ في CI: **17**» · و`grep -n 'state_document_drift' .github/workflows/ci.yml` ⇒ **لا نتيجةَ** قبلَ `W-077` · وكلفةُ مِسبارِ الوَثبِ مقيسةٌ لا مُخمَّنةٌ: **4م 31ث** لِـ**31** وثبةً في ستِّ دعاوى، وكلُّها مُلتقَطةٌ | **سندُ بقاءٍ مكتوبٌ ولا يُقاسُ أخطرُ من غيابِ سندٍ**: القيدُ يبقى مفتوحًا مطمئنًّا إلى حرسٍ قد لا يُشغَّلُ، ومَن يقرأُ السجلَّ يحسبُ الخطرَ محروسًا. وهو من جنسِ `DISC-021` (‏مسارٌ يُذكَرُ دليلَ غيابِ حرسٍ فيُقرأُ حرسًا قائمًا) لكنَّه أوسعُ: هناك **مِرساةٌ** تُقرأُ خطأً، وهنا **إنفاذٌ** يُدَّعى ولا يُقاسُ | البندُ `WI-023` في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) — نطاقُ `tooling-gates` · وحرسُه `tools/governance/guard_enforcement_closure.py` مربوطًا في `.github/workflows/ci.yml`، ومعه ربطُ `state_document_drift.py` و`open_record_accountability.py` و`mutation_probe.py` بخطواتِ تشغيلٍ · **حرسٌ قائمٌ**: `tools/governance/guard_enforcement_closure.py` — مربوطٌ بخطوةِ تشغيلٍ مُسمّاةٍ في [`ci.yml`](../../../.github/workflows/ci.yml) (‏السطرُ 234) فيُقاسُ في كلِّ دفعةٍ لا يُصدَّقُ نصًّا | مفتوحٌ — الحرسُ يُبنى في `WI-023` |
| DISC-038 | P2 | ملاحظةُ `UNWIRED_TOOL_INVENTORY` في `tools/governance/guard_enforcement_closure.py` مقابلَ شجرةِ `tools/**` | **جردُ الأدواتِ غيرِ المربوطةِ كانَ رقمًا يخلِطُ أنواعًا ويقصرُ نظرَه على مجلَّدَينِ من خمسةٍ، ولا رمزَ خروجٍ يمنعُ ولادةَ أداةٍ صامتةٍ جديدةٍ**: الجردُ يعُدُّ مكتبةً بلا مدخلِ تشغيلٍ (`mutation_claims.py`) أداةً مهجورةً، ويعُدُّ أدواتٍ يُشغِّلُها فحصٌ في CI يحكُمُ على الشجرةِ الحاضرةِ (`live_truth.py` · `sovereign_write_inventory.py` · `surface_debt_trend.py`) غيرَ مربوطةٍ، ولا يرى `tools/crown` و`tools/sovereignty` و`tools/migrations` و`tools/stubs` أصلًا — فالعددُ الصادقُ **11** لا 13 ولا 17. والأهمُّ: الجردُ **ملاحظةٌ لا حكمٌ**، فأداةٌ حاكمةٌ تُولَدُ غدًا بلا طريقِ إنفاذٍ لا يقفُ في وجهِها رمزُ خروجٍ | ‏`guard_enforcement_closure.py` ⇒ «أدواتٌ لا تُسمّيها خطوةُ تشغيلٍ في CI: 13» ملاحظةً بلا إسقاطٍ · و`enforcement_path_ledger.py` عندَ الرأسِ نفسِه قبلَ إضافةِ الإعلاناتِ ⇒ **رمزُ 1 · 12 مخالفةً**: 11 ملفًّا قابلًا للتشغيلِ بلا طريقٍ ولا إعلانٍ، منها اثنانِ لم يرَهما الجردُ (`tools/migrations/r4_unify_agent_identity.py` · `tools/stubs/registry_check.py`) · وثلاثةٌ من «الثلاثةَ عشرَ» قِيسَ طريقُها `COVERED_TEST` بدليلٍ مُسمّى (‏مثالًا: `tests/governance/test_w061_surface_debt_trend.py::test_real_repository_is_measured_and_not_written_to`) | **رقمٌ مُعلَنٌ يخلِطُ أنواعًا يُقرأُ دَينًا حيثُ لا دَينَ ويُسكِتُ دَينًا حيثُ هو**: من يقرأُ «13 أداةً مهجورةً» يظنُّ الحرسَ أوسعَ عَطبًا من حقيقتِه، ومن يثقُ بالجردِ يحسبُ `tools/stubs` و`tools/migrations` محروسَينِ وهما خارجَ نظرِه. وهو من جنسِ `DISC-037` (‏إنفاذٌ يُدَّعى ولا يُقاسُ) لكنَّه في **قياسِ الجردِ نفسِه** لا في الدعوى | الأداةُ `tools/governance/enforcement_path_ledger.py` وفحوصُها في `tests/governance/test_w079_enforcement_path_ledger.py` — نازلتانِ في القيدِ `W-080` نفسِه · والبندُ `WI-024` في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) — نطاقُ `tooling-gates` · وربطُ السجلِّ بخطوةِ تشغيلٍ صريحةٍ في `.github/workflows/ci.yml` بندٌ تالٍ لأنَّ المسارَ مقفولٌ بـ`WI-023` · **حرسٌ قائمٌ**: `tools/governance/enforcement_path_ledger.py` · `tests/governance/test_w079_enforcement_path_ledger.py` — السجلُّ قائمٌ ويُسقِطُ، وحرسُه نزلَ معه في `W-080` — وربطُه بخطوةِ تشغيلٍ صريحةٍ يبقى مُعلَّقًا على تحرُّرِ `ci.yml` (‏مقفولٌ بـ`WI-023`) | مفتوحٌ — السجلُّ قائمٌ ويُسقِطُ، وربطُه بخطوةِ تشغيلٍ صريحةٍ مُعلَّقٌ على تحرُّرِ مسارِ `ci.yml` |
| DISC-039 | P3 | `docs/audit/measurements/write_inventory_p13.json` مقابلَ `tools/governance/measurement_provenance.py --check` | **قياسٌ منشورٌ مُفتَّحٌ بأرقامِ الأسطرِ يتقادمُ من تعديلِ ترويسةٍ لا يمسُّ سلوكًا، وبوّابةُ الطزاجةِ تُسقِطُ CI لسببٍ لا علاقةَ له بالسلوكِ المقيسِ**: إضافةُ سطرَي إعلانٍ في ترويسةِ `tools/migrations/r4_unify_agent_identity.py` نقلَت موقعَ كتابةٍ من السطرِ 354 إلى 356، فصارَ الجردُ المنشورُ (217 موقعًا · العددُ نفسُه) متقادمًا في حقلٍ واحدٍ. **والأخطرُ أنَّ القياسَ المحليَّ لم يُظهِرْه**: `measurement_provenance.py --check --only-deps` يفحصُ شطرَ ما يحتاجُ حزمًا ويُؤجِّلُ هذا القيدَ، فمرَّ محليًّا أخضرَ وسقطَ في وظيفةِ النواةِ الدستوريّةِ في CI | ‏`measurement_provenance.py . --check --only-deps` محليًّا ⇒ رمزُ 0 «مُؤَجَّلٌ إلى الشطرِ الآخرِ: 5» · و`--check` كاملًا ⇒ «`write_inventory_p13.json`: القياسُ المنشورُ متقادمٌ في 1 حقلًا — sites» · وحكمُ عقدةِ `1cd662c` (‏تشغيلُ 33447618294) ⇒ وظيفةُ Truth Audit **failure** في الخطوةِ 8 وحدَها و12 وظيفةً أخرى ناجحةً · والفرقُ المقيسُ: `('…r4_unify_agent_identity.py', 356)` مقيسًا مقابلَ `(…, 354)` منشورًا | **بوّابةُ طزاجةٍ حسّاسةٌ لأرقامِ الأسطرِ تُعاقِبُ تعديلَ الترويسةِ عقابَ تعديلِ السلوكِ**، فتُعلِّمُ العاملَ أن يتجنَّبَ لمسَ الترويساتِ — وهو ضدُّ ما تطلبُه بوّاباتُ الهُويّةِ. وأثرٌ ثانٍ: **شطرا الفحصِ يجعلانِ الأخضرَ المحليَّ غيرَ دالٍّ على الأخضرِ في CI** ما لم يُشغَّلْ `--check` كاملًا | أُعيدَ توليدُ القياسِ المنشورِ بأمرِه المُقيَّدِ في القيدِ `W-081` (‏فرقٌ سطرٌ واحدٌ) · و`WI-025` **محجوزٌ** في نطاقِ `audit-truth` لِيُجعَلَ مفتاحُ الموقعِ مستقلًّا عن رقمِ السطرِ (‏مسارٌ · مالكٌ · دالّةٌ · رتبةٌ)، **من مصدرِ القياسِ لا من حاكمِه** · وواجبُ التشغيلِ: `--check` كاملًا قبلَ الدفعِ لا شطرًا منه · **والحرسُ القائمُ مقامَ الإصلاحِ الآنَ**: `tools/governance/measurement_provenance.py` مربوطٌ بخطوةِ تشغيلٍ في وظيفةِ `Truth Audit` في `.github/workflows/ci.yml` فيُسقِطُ أيَّ تقادمٍ في القياسِ المنشورِ — وهو الذي كشفَ هذا العَطبَ نفسَه · **حرسٌ قائمٌ**: `tools/governance/measurement_provenance.py` · `tests/governance/test_w083_measurement_site_key.py` — الأوّلُ مربوطٌ بخطوةِ تشغيلٍ في وظيفةِ `Truth Audit` فيُسقِطُ تقادُمَ القياسِ المنشورِ — وهو الذي كشفَ هذا العَطبَ نفسَه — والثاني أحدَ عشرَ فحصًا تقيسُ الطزاجةَ والإنفاذَ معًا | مفتوحٌ — **وهشاشةُ المفتاحِ عُولِجَت في مصدرِها بقياسٍ** (`W-084`): حِملُ `--json` صارَ مُفتَّحًا بـ(‏مسارٌ · مالكٌ · دالّةٌ · رتبةٌ) بلا رقمِ سطرٍ، وحرسُه 11 فحصًا في `tests/governance/test_w083_measurement_site_key.py` تقيسُ الطزاجةَ والإنفاذَ معًا. ويبقى القيدُ مفتوحًا حتّى يُقرأَ حكمُ CI على عقدتِه ويُراجَعَ `WI-025` — والشِقُّ الثاني (‏أنَّ شطرَي الفحصِ يجعلانِ الأخضرَ المحليَّ غيرَ دالٍّ) واجبُ تشغيلٍ مكتوبٌ لا شِفرةٌ |
| DISC-040 | P2 | مجموعةُ ما قبلَ الدفعِ في [`THE_ROADMAP.md § 5.4`](THE_ROADMAP.md) مقابلَ خطوةِ «التحقق من أن المصفوفة المدفوعة محدّثة» في `.github/workflows/ci.yml:515` | **مجموعةُ ما قبلَ الدفعِ تأمرُ بتشغيلِ أداةٍ تكتُبُ أثرًا مُولَّدًا ولا تقيسُ أنَّ المكتوبَ يُدفَعُ**: `truth_audit.py` يكتُبُ `docs/audit/TRUTH_MATRIX.md` و`truth_matrix.json`، و§ 5.4 يأمرُ بتشغيلِه — فيُرضي الأمرُ آمِرَه ولا وجهَ محلّيًّا يقيسُ طزاجةَ المكتوبِ في الشجرةِ. والبوّابةُ الوحيدةُ التي تقيسُها `git diff --exit-code` في CI. فصارَ ممكنًا أن تخضَرَّ كلُّ بوّابةٍ محلّيّةٍ ثمَّ يحمَرَّ الحكمُ على أثرٍ مُولَّدٍ متقادمٍ | تشغيلُ [33570417864](https://github.com/xoos-beep/AMOS-Fedration/actions/runs/33570417864) (‏رقمُ 55) على `9bf0b00` ⇒ `failure` · `READABLE` · 12/13 · الوظيفةُ الساقطةُ الوحيدةُ `Truth Audit (E0 — Ratchet Gate)` في الخطوةِ «التحقق من أن المصفوفة المدفوعة محدّثة» — وكلُّ بوّابةٍ محلّيّةٍ قبلَ تلك الدفعةِ كانت رمزَ 0 و`--ratchet` ثابتًا عندَ 63 · وبعدَ قطعِ السببِ في مصدرِه (`W-098`) قُرِئَ الحكمُ على `815d707` أخضرَ 13/13 (تشغيلُ [33572642082](https://github.com/xoos-beep/AMOS-Fedration/actions/runs/33572642082) · رقمُ 56 · `READABLE`) | **الأثرُ مضاعَفٌ لأنَّه يلتقي بمصيدةٍ مُقيَّدةٍ منذُ `W-008`**: حقلُ `repo` في المصفوفةِ يُقرأُ من `origin` ويرتدُّ إلى **اسمِ المجلَّدِ** حينَ لا بُعدَ للمستودعِ (`_repo_identity` · `tools/governance/truth_audit.py:301`)، ومرآةُ العملِ كانت بلا `origin` لأنَّ الشبكةَ محصورةٌ في `api.github.com`. فصارَ رَدُّ الملفِّ بعدَ كلِّ توليدٍ عادةً، فسقطَ معَ الحقلِ تغييرٌ **حقيقيٌّ** في المضمونِ. **والأعمُّ من ذلك أنَّ الأخضرَ المحلّيَّ يصيرُ غيرَ دالٍّ** — وهو عينُ ما قُيِّدَ في `DISC-039` من جهةٍ أخرى | البندُ `WI-030` في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) — نطاقُ `tooling-gates`: يُزادُ وجهُ `--check` في الأداةِ التي تُولِّدُ الأثرَ نفسِها، وحرسُه فحصٌ جديدٌ تحتَ `tests/governance/` مُسمًّى في مساراتِ البندِ ‏— وقد نزلَ فصارَ يُسمّى: `tests/governance/test_w099_generated_artifact_freshness.py` (‏قيدُ `W-100`)؛ ولم يُذكَرْ مسارُه قبلَ أن يوجدَ لأنَّ مِرساةً مكتوبةً لا تحمِلُ شيئًا أسوأُ من غيابِها · `DISC-021` — فيصلُ الإنفاذُ عبرَ `pytest tests/ -q` القائمِ في مجموعةِ ما قبلَ الدفعِ **بلا مسِّ نصِّ § 5.4** · **استحقاق: 2026-09-08** (‏أجلُ حجزِ `WI-030` المُعلَنُ) · **وتعديلُ نصِّ § 5.4 نفسِه قرارُ مالكٍ لا فعلُ عاملٍ** · **حرسٌ قائمٌ**: `tools/governance/truth_audit.py` · `tests/governance/test_w099_generated_artifact_freshness.py` — وجهُ `--check` يُسقِطُ تقادُمَ الأثرِ المُولَّدِ **قبلَ** الدفعِ لا في CI وحدَها، وحرسُه ستّةُ فحوصٍ لا تكتُبُ في الشجرةِ التي تحكُمُ عليها | مفتوحٌ — **والبوّابةُ المحلّيّةُ نزلَت** في `W-100`: وجهُ `--check` في `tools/governance/truth_audit.py` وحرسُه `tests/governance/test_w099_generated_artifact_freshness.py` (‏6 فحوصٍ مرَّت · ولا يكتُبُ في الشجرةِ التي يحكُمُ عليها). وحكمُ CI على عقدةِ نزولِها قُرِئَ أخضرَ: تشغيلُ [`33581117015`](https://github.com/xoos-beep/AMOS-Fedration/actions/runs/33581117015) (‏رقمُ 58) على `48082c5` ⇒ `success` · **13/13** · `READABLE` — فالحرسُ منفَذٌ في CI لا محلّيًّا وحدَه. **ولا يُقرأُ هذا إغلاقًا**: `WI-030` في يدِ المراجعِ، والحالةُ لا تُقرأُ قبلَ مراجعةٍ (§ 4.3 · `A-2` غيرُ مستقلٍّ) |
| DISC-041 | P2 | `tools/` و`tests/` — نمطُ `REPO_ROOT = Path(__file__).resolve().parents[N]` | **جذرُ القياسِ يُثبَّتُ بعُمقٍ مكتوبٍ لا بعلامةٍ**: الأداةُ تحسِبُ محلَّ القياسِ بعَدِّ مجلَّداتٍ صاعدةٍ من موضعِ ملفِّها. فإن نُقِلَ الملفُّ أو أُعيدَ تنظيمُ المجلَّداتِ صارَ «الجذرُ» مجلَّدًا آخرَ **بلا أيِّ خطأٍ**: البوّابةُ تُشغَّلُ وتقرأُ شجرةً غيرَ التي يُظَنُّ أنَّها تُقرأُ وتُخرِجُ رمزَ صفرٍ. وهذا **السببُ الأعمقُ** لعائلةِ `DISC-032` لكنَّه في الأدواتِ لا في الفحوصِ | قياسٌ تركيبيٌّ (‏`ast`) على `tools/` و`tests/` — بعدَ استثناءِ `.venv`: **جذرٌ مُشتَقٌّ من عُمقٍ ثابتٍ: أوّلَ قياسٍ (`W-102`) 25 موضعًا تحتَ `tools/` و53 تحتَ `tests/`، ومنها بلا وجهٍ يُمرِّرُ جذرًا صريحًا 2 و37** — ثمَّ **خُفِضَ في `W-106` بترحيلِ 34 موضعًا حرًّا إلى مُكتشِفٍ بعلامةٍ فصارَ 25 و19 ومنها 2 و12، ثمَّ في `W-107` بترحيلِ 9 مواضعَ حرّةٍ باقيةٍ تحتَ `tests/` فصارَ 25 و10 ومنها 2 و3، ثمَّ في `W-109` بترحيلِ `stamp_readme_identity.py` فصارَ 24 و10** (‏والقديمُ لا يُمحى: يُقرأُ مع خفضِه). والرقمانِ يُعادُ إنتاجُهما بالحرسِ المُسمّى في الوجهةِ · والدَّينُ المُعلَنُ يُقرأُ من هذا السطرِ وحدَه (‏مصدرُ حقيقةٍ واحدٌ لا ثابتٌ في فحصٍ) ⇐ `ROOT_PROVENANCE_BASELINE: tools=24 tests=10 tools_no_override=2 tests_no_override=3` — لا يُعَلَّى ويُخفَضُ فقط | **الأخضرُ قد يكونُ صامتًا لا صادقًا**: بوّابةٌ تقرأُ مجلَّدًا خطأً لا تُخبِرُ بذلك — ولا حرسَ اليومَ يمنعُ نموَّ النمطِ، فكلُّ أداةٍ جديدةٍ تُنسَخُ عنه. **والإصلاحُ الشاملُ يُصادِمُ دعاوى قائمةً**: مساراتٌ كثيرةٌ منها مُدَّعاةٌ لبنودٍ في يدِ المراجعِ (`WI-023`…`WI-030`) فمسُّها الآنَ مصادمةُ دعوى (§ 6.1) وتوسيعُ نطاقٍ (§ 5.2) | البندُ `WI-031` في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) — نطاقُ `tooling-gates`: حرسٌ قارئٌ تحتَ `tests/governance/` يُحصي النمطَ ويُعلِنُ رقمَينِ **لا يعلوانِ** (‏سقّاطةٌ تمنعُ النموَّ ولا تُبيحُ القائمَ)، ثمَّ يُخفَضُ الرقمُ رقمًا رقمًا حينَ تنفكُّ الدعاوى · **والحرسُ نزلَ فصارَت المِرساةُ مسارًا قائمًا لا وعدًا**: [`tests/governance/test_w102_measurement_root_provenance.py`](../../../tests/governance/test_w102_measurement_root_provenance.py) — سقّاطتانِ تُقرآنِ من هذا السطرِ نفسِه · **حرسٌ قائمٌ**: `tests/governance/test_w102_measurement_root_provenance.py` — سقّاطتانِ تُقرآنِ من سطرِ الإعلانِ نفسِه فلا تعلوانِ — حرسٌ من النموِّ لا براءةٌ للقائمِ | مفتوحٌ — **محروسٌ من النموِّ لا مُصلَحٌ**: النمطُ باقٍ في 78 موضعًا، والحرسُ يمنعُ زيادتَه فقط، والخفضُ يجري بندًا بندًا حينَ تنفكُّ الدعاوى (`WI-023`…`WI-031` في يدِ المراجعِ) |
| DISC-042 | P2 | `tools/governance/check_work_governance.py` — قراءةُ خليّةِ المساراتِ في § 1 من [`ACTIVE_WORK.md`](ACTIVE_WORK.md) | **نثرٌ في خليّةِ المساراتِ يُقرأُ دعوى واسعةً**: الخليّةُ تُشطَرُ على «·» ويُعامَلُ كلُّ جزءٍ مسارًا، ثمَّ تُقاسُ التغطيةُ بالبادئةِ. فجزءٌ نثريٌّ يذكرُ مجلَّدًا (‏مثلُ «و34 ملفَّ فحصٍ تحتَ `tests/governance/` …») **يُغطّي المجلَّدَ كلَّه**، فتمرُّ ملفّاتٌ لم يُعلِنْها البندُ اسمًا | قِيسَ في `W-106` عيانًا: بخليّةٍ فيها ذلك النثرُ مرَّت **32** من 34 ملفًّا مُرحَّلًا ولم يُعترَضْ إلّا على اثنينِ؛ ولمّا صارَت الخليّةُ أسماءً صريحةً اعتُرِضَ على **17** حتّى عُدِّدَت كلُّها — والفرقُ نفسُه هو الدليلُ | **الدعوى تُقرأُ أوسعَ ممّا كُتِبَت**: بندٌ يمسُّ ملفًّا لم يُعلِنْه قد لا يُوقَفُ، فيضعُفُ `CLAIM_CONFLICT` بينَ بندَينِ يعملانِ في مجلَّدٍ واحدٍ — وهو حرسُ الملكيّةِ نفسُه (§ 6.1) | `WI-037` — **نزلَ الحرسُ**: `tools/governance/check_work_governance.py` (‏`parse_claim_cell` · مخالفةُ `NONPATH_CLAIM`) · `tests/governance/test_w117_claim_cell_shape.py` (‏11 فحصًا: نثرٌ **لا يُغطّي** ملفًّا، ومسارٌ مكتوبٌ اسمًا يُغطّي، وخليّةُ الفراغِ لا تُدَّعي) · **استحقاق: 2026-09-09** | **محروسٌ** بـ`W-118`: الأجزاءُ غيرُ المساريّةِ صارَت **8 ← 0** والتغطيةُ **لم تتبدَّلْ** (‏قِيسَ صفرُ ملفٍّ من **1855** مُتتبَّعًا). والحاجزُ المكتوبُ هنا («‏الإصلاحُ يمسُّ أداةً مُدَّعاةً لبندٍ في يدِ المراجعِ») **أُعيدَ قياسُه فلم يَعُدْ قائمًا**: لا بندَ غيرَ مُغلَقٍ يُعلِنُ الملفَّ — صَدَقَ يومَ كُتِبَ ولم يُورَّثْ. **ويبقى حدٌّ مُعلَنٌ**: المقيسُ **شكلُ** الجزءِ لا **وجودُ** الملفِّ، فمسارٌ مكتوبٌ خطأً ولا يوجدُ يمرُّ |
| DISC-043 | P2 | سجلُّ التزاماتِ المستودعِ — طوابعُ الزمنِ (‏حدُّ الصدقِ 9 في [`COMPLETION_LEDGER.md § 10`](../../audit/COMPLETION_LEDGER.md)) | **طابعُ الالتزامِ كانَ يُحفَرُ نصًّا لا يُقرأُ من ساعةٍ**، فحملَت `W-023`…`W-027` طابعًا واحدًا بعينِه؛ وأُصلِحَت الأداةُ في `W-028` **ولم يُصحَّحِ الماضي** (‏تصحيحُه يوجبُ إعادةَ كتابةِ التاريخِ ومحوَ قيودٍ مدفوعةٍ). وبقيَ الحدُّ **مرويًّا بلا رقمٍ ولا حرسٍ** حتّى `W-110` | قِيسَ من سجلِّ المستودعِ الكاملِ في `W-110`: **353 التزامًا** · بإزاحةٍ غيرِ `+00:00`: **134** (‏قُيِّدَ أوّلًا **0** لأنَّ واجهةَ المستودعِ البرمجيّةَ **تُوحِّدُ** الإزاحاتِ إلى `Z` فلا تُرى منها الإزاحةُ الأصليّةُ؛ والرقمُ الصادقُ قِيسَ من `git` نفسِه في CI · تشغيلٌ **69** · تعليقُ الفحصِ على العقدةِ `1e800dd` · وصُحِّحَ في `W-112`) · طوابعُ مكرَّرةٌ: **3 مجموعاتٍ** تضمُّ **10 التزاماتٍ** (‏أكبرُها 6 بطابعِ `2026-08-22T09:55:00Z`) · وطفلٌ يسبقُ أباهُ زمنًا: **4** (‏قُيِّدَ أوّلًا **3** بخطأِ منهجٍ — عُدَّ بترتيبِ القائمةِ لا بنسَبِ الأبوَّةِ — فأسقطَ CI العقدةَ `89efcdec` وصُحِّحَ في `W-111`؛ والعددُ يُشتَقُّ من الأداةِ نفسِها لا من نصٍّ جانبيٍّ) — والرقمُ المُعلَنُ يُقرأُ من هذا السطرِ: `COMMIT_STAMP_BASELINE: non_utc=135 duplicate_groups=3 duplicate_commits=10 non_monotonic=4 history_commits=356 baseline_node=66c217bacf32ad9852b13226a25276df640ced75`‏ — و**تصحيحٌ ثالثٌ مقيسٌ لا تخفيفٌ** (`W-113` · `DISC-044`): `non_utc` صارَ **135** لا 134 لأنَّ **العقدةَ التي أعلنَت 134 حملَت نفسُها إزاحةً `+03:00`** (`66c217b`) فزادَ الدَّينُ واحدًا والتاريخُ لا يُعادُ كتابتُه، و`history_commits` صارَ **356** (‏مقيسًا من `git` لا من الواجهةِ البرمجيّةِ)، وأُضيفَ حقلُ `baseline_node` فصارَ **الماضي مُجمَّدًا مُعلَنًا والمستقبلُ مُلزَمًا بإزاحةٍ `+00:00` يُقاسُ قبلَ الدفعِ** | **الطابعُ يُقرأُ ترتيبًا وهو ليسَ ترتيبًا**: من رتَّبَ العملَ بالطوابعِ قرأَ ستَّ عقدٍ متزامنةً وثلاثَ عقدٍ مقلوبةً — والترتيبُ الصادقُ نسَبُ الأبوَّةِ. ونموُّ العددِ يعني عودةَ الحفرِ النصّيِّ بلا كاشفٍ | حرسٌ قائمٌ: `tools/governance/commit_timestamp_integrity.py` · `tests/governance/test_w110_commit_stamp_integrity.py` — يُسقِطُ إن علا رقمٌ، ويُوجِبُ خفضَ المُعلَنِ إن انخفضَ المقيسُ على سجلٍّ كاملٍ | مفتوحٌ — **مقيسٌ ومحروسٌ من النموِّ لا مُبرَأٌ**: العشرةُ الماضيةُ تبقى في السجلِّ ولا تُصحَّحُ، والحرسُ لا يقيسُ صدقَ الساعةِ التي كتبَت الطابعَ بل تناسُقَ الطوابعِ |
| DISC-044 | P2 | `tools/governance/commit_timestamp_integrity.py` — زمنُ القياسِ لا محلُّه (‏أثرُ `DISC-043`) | **سقّاطةٌ بَعديّةٌ تُسقِطُ العقدةَ التي تُعلِنُها**: الرقمُ المُعلَنُ يُكتَبُ في الشجرةِ **قبلَ** أن يوجدَ الالتزامُ الحامِلُ له، فإن حملَ ذاكَ الالتزامُ نفسُه إزاحةً محلّيّةً صارَ المقيسُ أكبرَ من المُعلَنِ بواحدٍ لحظةَ الدفعِ — لا بعدَه ولا بفعلِ غيرِه. والأعمُّ: **كلُّ سقّاطةٍ تقيسُ مجموعًا يُدخِلُ الالتزامَ الحاليَّ في مقياسِه لا يمكنُ أن تُخضَرَ من أوّلِ مرّةٍ إلّا بمنعِ السَّببِ لا برصدِ الأثرِ** | تشغيلُ [`33621763476`](https://github.com/xoos-beep/AMOS-Fedration/actions/runs/33621763476) على العقدةِ `66c217b` ⇒ `failure` · **11/13** · الوظيفتانِ الساقطتانِ `Identity Law (بوّابةُ 5)` و`Cross-System Suites`، والفحصُ الساقطُ واحدٌ: `test_w110_commit_stamp_integrity.py::test_دَينُ_طوابعِ_الالتزامِ_لا_يعلو` ⇒ `non_utc: مقيسٌ 135 · المُعلَنُ 134` · وأُعيدَ محلّيًّا بعينِه: `pytest tests/governance/ -q` ⇒ **1 failed · 1014 passed** و`--check` ⇒ `مقيسٌ non_utc=135 · من 356 التزامًا · سجلٌّ كاملٌ` | **والأثرُ أنَّ حرسًا بَعديًّا يصيرُ حاجزًا دائمًا لا يُفَكُّ إلّا بأحدِ أمرَينِ ممنوعَينِ أو ثالثٍ مشروعٍ**: إعادةُ كتابةِ التاريخِ (‏تمحو قيودًا مدفوعةً · ممنوعٌ)، أو تخفيفُ الفحصِ (‏ممنوعٌ)، أو **تصحيحُ المُعلَنِ إلى المقيسِ معَ منعِ النموِّ في مصدرِه** — والثالثُ وحدَه يجمعُ صدقَ الرقمِ وقوّةَ الحرسِ. وما دامَ المنفِّذُ يعملُ في منطقةٍ محلّيّةٍ (`+03:00`) فكلُّ التزامٍ قادمٍ يُنمي الدَّينَ حتمًا | البندُ `WI-035` نفسُه (‏الحدُّ كانَ مُعلَنًا في قسمِ «حدُّ البندِ» منه): وجهٌ ثانٍ للحرسِ يُلزِمُ **ما بعدَ `baseline_node`** بإزاحةٍ `+00:00` فيسقُطُ قبلَ الدفعِ لا بعدَه · وحرسُه [`tests/governance/test_w113_new_commit_stamp_utc.py`](../../../tests/governance/test_w113_new_commit_stamp_utc.py) — ويصلُ الإنفاذُ CI عبرَ استدعاءِ `pytest tests/governance/` القائمِ **بلا مسِّ `ci.yml`** (‏مقفولٌ بـ`WI-023`) · **حرسٌ قائمٌ**: `tests/governance/test_w113_new_commit_stamp_utc.py` — يُلزِمُ ما بعدَ `baseline_node` بإزاحةِ `+00:00` فيسقُطُ قبلَ الدفعِ لا بعدَه، ويصلُ الإنفاذُ CI عبرَ استدعاءِ `pytest` القائمِ بلا مسِّ `ci.yml` | مفتوحٌ — **السَّببُ مقطوعٌ والماضي مُعلَنٌ لا مُبرَأٌ**: المقيسُ 135 والمُعلَنُ 135، والحرسُ الجديدُ يمنعُ 136 من أن تُولَدَ أصلًا · وحدُّه مُعلَنٌ: يقيسُ إزاحةَ الطابعِ لا صدقَ الساعةِ التي كتبَته |
| DISC-045 | P3 | [`COMPLETION_LEDGER.md § 10`](../../audit/COMPLETION_LEDGER.md) — ترقيمُ حدودِ الصدقِ المُعلَنةِ | **رقمُ حدٍّ مكتوبٌ مرّتَينِ فصارَت الإحالةُ إليه لا تُحَلُّ**: القسمُ يُعلِنُ **17 حدًّا** بأرقامٍ 1…16، و**الرقمُ 9 يحملُه حدّانِ مختلفانِ**: «تواريخُ `W-023`…`W-027` باطلةٌ» (‏صدقُ طوابعِ الالتزامِ) و«البوّابةُ لا تُصدَّقُ على أساسٍ بائدٍ». فمن قرأَ «الحدَّ 9» في قيدٍ أو بندٍ **لا يعرفُ أيَّهما يُقصَدُ** إلّا بقرينةٍ نثريّةٍ — والإحالةُ تُقرأُ حجّةً لا لغزًا | قياسٌ يُعادُ على نصِّ القسمِ: `^\d+\. \*\*` ⇒ **17** عنصرًا · تكرارٌ واحدٌ (`9` مرّتَينِ) · ولا فجوةَ في 1…16 · والإحالاتُ إلى «الحدِّ 9» **7** في 5 ملفّاتٍ: `docs/audit/COMPLETION_LEDGER.md` (4) · `docs/PROJECT_HANDBOOK.md` · `docs/audit/ACTIVE_EXECUTION_STATE.md` · `tools/governance/commit_timestamp_integrity.py` · `tests/governance/test_w113_new_commit_stamp_utc.py` | **مصدرُ الحقيقةِ الحاكمُ يُحيلُ إلى نفسِه بمُعرِّفٍ غيرِ فريدٍ**: البنودُ `WI-034` و`WI-035` تُعلِنُ مسارَها بـ«حدُّ الصدقِ 8/9»، فإن أُعيدَ الترقيمُ كُسِرَت إحالاتٌ في **قيودٍ مدفوعةٍ لا تُمحى**، وإن بقيَ الحالُ بقيَ الالتباسُ ونما بكلِّ حدٍّ جديدٍ. والأعمُّ: **مُعرِّفٌ يُحالُ إليه ولا يُقاسُ تفرُّدُه هو دعوى بلا حرسٍ** | البندُ `WI-036` في [`ACTIVE_WORK.md`](ACTIVE_WORK.md) — نطاقُ `tooling-gates` · **والحرسُ نزلَ فصارَتِ المِرساةُ مسارًا قائمًا لا وعدًا** (`W-115`): [`tools/governance/truth_limit_integrity.py`](../../../tools/governance/truth_limit_integrity.py) قارئٌ لا مُعدِّلٌ يُعيدُ أربعةَ أرقامٍ، وحرسُه [`tests/governance/test_w115_truth_limit_integrity.py`](../../../tests/governance/test_w115_truth_limit_integrity.py) — سقّاطةٌ **لا تعلو ولا تُترَكُ رخوةً**، وعددُ العناصرِ يُطابِقُ المُعلَنَ مطابقةً تامّةً فلا يُحذَفُ عنصرٌ ولا يُزادُ في الظلِّ · والرقمُ المُعلَنُ يُقرأُ من هذا السطرِ وحدَه (‏مصدرُ حقيقةٍ واحدٌ لا ثابتٌ مدفونٌ في فحصٍ) ⇒ `TRUTH_LIMIT_BASELINE: limits=17 duplicate_numbers=1 gaps=0 ambiguous_references=23` (‏خُفِضَ من 24 إلى 23 في `W-154`: إحالةٌ ملتبسةٌ واحدةٌ إلى الحدِّ المُكرَّرِ رقمُه (‏تسعةً · `DISC-045`) زالت من ترويسةِ `PROJECT_STATE.md` حينَ صِيغَ سطرُ «آخرِ تعديلٍ» على `W-154` — والخفضُ مقيسٌ لا مُخفَّفٌ، والسقّاطةُ تُخفَضُ ولا تُعَلَّى) — لا يُعَلَّى ويُخفَضُ فقط · **استحقاق: 2026-09-10** (‏نهايةُ حجزِ `WI-036`) · **حرسٌ قائمٌ**: `tools/governance/truth_limit_integrity.py` · `tests/governance/test_w115_truth_limit_integrity.py` — قارئٌ لا مُعدِّلٌ يُعيدُ أربعةَ أرقامٍ، وسقّاطةٌ لا تعلو ولا تُترَكُ رخوةً، وعددُ العناصرِ يُطابِقُ المُعلَنَ مطابقةً تامّةً | مفتوحٌ — **مقيسٌ ولم يُحرَسْ بعدُ**: والترقيمُ لا يُعادُ (‏يكسِرُ إحالاتٍ مدفوعةً وينشئُ مصدرَ حقيقةٍ ثانيًا)، فالالتباسُ القائمُ **يُعلَنُ مقيسًا** ويُمنَعُ نموُّه |
| DISC-046 | P2 | [`PROJECT_HANDBOOK.md § 4`](../../PROJECT_HANDBOOK.md) — «الحالةُ المقيسةُ الآن» وقائمةُ ما قبلَ الدفعِ في § 12 | **أرقامُ حجمِ الحزمِ تُعلَنُ حالةً «الآنَ» وتتقادمُ بلا حرسٍ**: القسمُ يُصدَّرُ بأنَّ «كلَّ سطرٍ هنا قِيسَ في هذه البيئةِ بالأمرِ المذكورِ»، ويُعلِنُ حزمةَ الجذرِ **1748 نجحَت · 1 مُتخطّاة في ثلاثِ دقائقَ ونصفٍ** (‏قِيسَ 2026-08-22 · W-030)، ويُعيدُ الرقمَ نفسَه في كتلةِ الأوامرِ «المتوقَّع». والمقيسُ اليومَ **2351 نجحَت · 1 مُتخطّاة في 472 ثانيةً** — فارقُ **603** اختبارًا وضِعفُ الزمنِ. ولا بوّابةَ تقرأُ هذا الرقمَ: حرسُ نَسَبِ القياساتِ (`measurement_provenance.py` · W-037) يحرسُ ملفّاتِ `docs/audit/measurements/` وحدَها، **ولا يرى رقمًا مكتوبًا في نثرٍ**. فمن قرأَ «المتوقَّع» قبلَ دفعِه ورأى 2351 حسِبَ بيئتَه معطوبةً، ومن نقلَ الرقمَ بنى على ميّت | قِيسَ 2026-09-03 على العقدةِ `d8e56a8`: `pytest tests/ -q` ⇒ **2351 نجحَت · 1 مُتخطّاة · 0 فشل** في 472.64 ثانيةً · و`pytest tests/ -q --collect-only` ⇒ **2352 مُجمَّعًا** في 0.58 ثانيةٍ (‏2351+1 — فالعددانِ متَّسِقانِ) · وحزمةُ الخدماتِ `--collect-only` ⇒ **1428 مُجمَّعًا** مقابلَ **1356 نجحَت · 27 مُتخطّاة** مكتوبةً · و`rg` على الوثيقةِ يُظهِرُ الرقمَ مكتوبًا في موضعَينِ لكلِّ حزمةٍ | **يُصدَّقُ رقمٌ ميّتٌ فيُقاسُ عليه**: القسمُ مُعلَنٌ «الآنَ» لا «تاريخًا»، فالفارقُ ليس تقادُمَ أرشيفٍ بل **دعوى حالةٍ كاذبةٍ في وثيقةِ التشغيل**. والأثرُ الأعمُّ أنَّ جنسَ `DISC-039` (‏قياسٌ منشورٌ يتقادمُ بلا شيءٍ يُظهِرُه) **ما زالَ مفتوحًا في النثرِ** بعدَ أن سُدَّ في ملفّاتِ القياس | `WI-038` (‏عددُ الجمعِ يصيرُ رقمًا يُقاسُ بأمرٍ رخيصٍ لا يُنقَلُ) · **استحقاق: 2026-09-10** · **حرسٌ قائمٌ**: `tools/governance/suite_size_inventory.py` · `tests/governance/test_w120_suite_size_claims.py` — يجمعُ الحزمَ حيًّا ويُقارِنُ بكلِّ رقمٍ مكتوبٍ، وحرسُه يُثبِتُ رَدَّ الرقمِ الميّتِ — و**زمنُ** الحزمةِ غيرُ محروسٍ بإعلانٍ فلا يُدَّعى | **محروسٌ ومفتوحٌ** — نزلَ الحرسُ في `W-121`: [`tools/governance/suite_size_inventory.py`](../../../tools/governance/suite_size_inventory.py) يجمعُ الحزمَ حيًّا ويُقارِنُ بكلِّ رقمٍ مكتوبٍ، و[`tests/governance/test_w120_suite_size_claims.py`](../../../tests/governance/test_w120_suite_size_claims.py) يُثبِتُ رَدَّ العَطبِ بثلاثةَ عشرَ فحصًا؛ وصُحِّحَت أربعةُ مواضعَ بقياسٍ طازجٍ (‏الجذرُ **2364 نجحَت · 1 مُتخطّاة** · الخدماتُ **1401 · 27**). ويبقى مفتوحًا لأنَّ البندَ في يدِ المراجعِ ولأنَّ **زمنَ** الحزمةِ غيرُ محروسٍ بإعلانٍ، و**حزمةُ الخدماتِ غيرُ مقيسةٍ في وظيفةِ بوّاباتِ الحوكمةِ** بسببٍ مكتوبٍ: ولا يُحذَفُ الرقمُ القديمُ من قيودِ § 8 التاريخيّةِ (‏كانَ صادقًا يومَ كُتِبَ)، المُصحَّحُ **دعوى «الآنَ» وحدَها** |
| DISC-047 | P2 | [`tools/governance/suite_size_inventory.py`](../../../tools/governance/suite_size_inventory.py) — مِقياسُ «أمُثبَّتةٌ الحزمةُ في هذه البيئةِ؟» مقابلَ [`conftest.py`](../../../conftest.py) في الجذرِ | **استيرادُ حزمةِ المستودعِ لا يقيسُ صلاحيّةَ البيئةِ لجمعِها**: كانَ الإعفاءُ البيئيُّ يُمنَحُ بـ`importlib.util.find_spec("amos_federation")`، و`conftest.py` في الجذرِ **يُضيفُ `federal/executive/services/src` إلى مسارِ الاستيرادِ** إن لم تكنِ الحزمةُ مُثبَّتةً — فتُرى «مُثبَّتةً» في وظيفةٍ لا تحملُ **تبعيّاتِها الخارجيّةَ** (`fastapi` وما بعدَها)، فيُشرَعُ في الجمعِ فيسقُطُ بأخطاءِ تجهيزٍ، ويُرفَعُ `SUITE_UNCOLLECTABLE` — **حمرةٌ من نقصِ بيئةٍ لا من رقمٍ ميّتٍ**. وهو جنسُ `DISC-019` نفسُه: شرطُ بيئةٍ يُقاسُ باسمٍ واحدٍ وسلسلةُ الاستيرادِ أطولُ منه | قِيسَ في تشغيلِ CI **79** ([‏33713758813](https://github.com/xoos-beep/AMOS-Fedration/actions/runs/33713758813)) على العقدةِ `d7333c0`: وظيفةُ «قانونِ الهُويّةِ» **حمراءُ** وحدَها من ثلاثةَ عشرَ، وخطوتُها الساقطةُ «بوابة 5» بـ`FAILED tests/governance/test_w120_suite_size_claims.py::test_المستودعُ_نفسُه_لا_يحملُ_رقمَ_حزمةٍ_ميّتًا` — `SuiteCollectionFailed: SUITE_UNCOLLECTABLE · services … رمزُ الخروجِ 2` وخرجُه `184 tests collected, 66 errors in 2.79s`. ووظيفةُ الحوكمةِ تُثبِّتُ `requirements-dev.txt` وحدَه ([‏`ci.yml:91`](../../../.github/workflows/ci.yml)) ولا تُثبِّتُ حزمةَ الخدماتِ، بينما `conftest.py:94` يُضيفُ مسارَ مصدرِها | **حرسٌ صادقٌ في حكمِه يُسقِطُ CI لسببٍ غيرِ حقيقيٍّ**، فيدفعُ إلى أرخصِ العلاجَينِ الخاطئَينِ: تخطٍّ صامتٌ للحزمةِ، أو حذفُ الفحصِ الحيِّ. والعلاجُ الصادقُ **قراءةُ السببِ من خرجِ الجمعِ**: فشلٌ يُسمّي حزمًا خارجيّةً غائبةً إعفاءٌ مُعلَنٌ **بأسمائِها**، وما لا يُسمّيها إخفاقٌ مُسمًّى يبقى | `WI-038` (‏البندُ نفسُه · `IN_PROGRESS`) | **مُغلَقٌ بـ`W-122`** — أُضيفَت طبقةُ قراءةٍ ثانيةٌ: `missing_external_modules()` تقرأُ أسماءَ الحزمِ الغائبةِ من خرجِ المُفسِّرِ (‏وتستثني حزمَ المستودعِ نفسِها فغيابُها عَطبٌ لا نقصُ بيئةٍ)، و`SuiteDependenciesMissing` تُعفي الحزمةَ **بسببٍ مكتوبٍ يحملُ أسماءَها**، و`live_sizes()` تُدرِجُها في «غيرِ المقيسِ هنا». وأربعةُ فحوصٍ تُثبِتُ الفرقَ: حزمةٌ تفقدُ تبعيّةً خارجيّةً تُعفى، وحزمةٌ **معطوبةُ الشِفرةِ** تُرفَعُ إخفاقًا ولا تُعفى |
| DISC-048 | P1 | [`tools/governance/open_record_accountability.py`](../../../tools/governance/open_record_accountability.py) — قاسمُ خلايا الصفِّ | **حكمٌ يُنطَقُ على صفٍّ لم يُقرَأْ**: كانَ القاسمُ يشطُرُ السطرَ على كلِّ `\|` بلا تمييزِ المهروبِ، ويرفضُ الصفَّ إن قلَّت خلاياه عن ثمانٍ **ولا يرفضُه إن زادت**. فخليّةٌ فيها أمرُ أنبوبٍ تنشطرُ، فتزحفُ الأعمدةُ عمودًا كاملًا: يُقرأُ ذيلُ خليّةِ الوجهةِ **حالةً**، وتُقرأُ الحالةُ الحقيقيّةُ في عمودٍ لا يُنظَرُ إليه — ويُقرأُ للمِرساةِ نصٌّ غيرُ نصِّها. وهو جنسُ `DISC-021` نفسُه من الجهةِ المقابلةِ: هناكَ خُضرةٌ تُشترى بصياغةٍ، وهنا حكمٌ يُبنى على عمودٍ مُزاحٍ | قِيسَ في `W-126` بتشغيلِ [`open_record_accountability.py`](../../../tools/governance/open_record_accountability.py) على الشجرةِ نفسِها: صفّانِ يحملانِ **تسعَ** خلايا (‏`DISC-032` بأمرِ `grep` في خليّةِ دليلِه، و`RK-013` بـ`try/except ImportError\|Exception` في خليّةِ تصرُّفِه) ومرَّا **بلا مخالفةٍ واحدةٍ** في كلِّ تشغيلٍ سابقٍ؛ وأُعيدَ القياسُ بعدَ إلزامِ العددِ ثمانيًا فرُفِعَ صفّانِ `MALFORMED_ROW`، ثمَّ صارَ القسمُ يقرأُ `\|` محتوًى فعادَ العددُ **68 صفًّا · 50 مفتوحًا · 29 محروسًا** كما هو | **عدَّادُ المساءلةِ يمكنُ أن يُصمَّتَ بمحرفٍ واحدٍ**: كاتبُ قيدٍ يضعُ `\|` في نصِّه فيُنقَلُ حكمُ صفِّه إلى عمودٍ آخرَ بلا أن تُرفَعَ مخالفةٌ — وأخطرُ منه أنَّ الرفضَ كانَ **أحاديَّ الجهةِ** (‏نقصٌ يُرفَضُ وزيادةٌ تُمَرُّ)، وهو نمطٌ يستحقُّ مسحًا في كلِّ أداةٍ تقرأُ جدولًا بالقسمِ الساذجِ | `WI-039` (‏قُيِّدَ وأُصلِحَ سببُه في البندِ نفسِه: الأداةُ ملفٌّ مُدَّعًى له) · **ومسحُ بقيّةِ قارئاتِ الجداولِ وُجِّهَ إلى `WI-040`** (‏`READY`) · **استحقاق: 2026-09-30** (‏**مُجدَّدٌ في `W-160` بسببٍ مكتوبٍ لا مطويٍّ**، والاستحقاقُ الأوّلُ **2026-09-11** يبقى مكتوبًا ههنا لا يُمحى: مضى ولم يُنجَزْ، والحاجزُ **مقيسٌ لا مُدَّعًى** — وجهتُه `WI-040` (`READY`) — ومسارُه `check_work_governance.py` **مقفولٌ** لِـ`WI-037` (`IN_REVIEW`) بنصِّ § 6.1. والتجديدُ **رفعُ حاجزٍ زمنيٍّ لا إنجازٌ ولا إغلاقٌ ولا تخفيفُ حرسٍ**، وأفقُه عينُ أفقِ الحجوزِ القائمِ **2026-09-30** فلا يُخترَعُ أفقٌ ثالثٌ — ومتى بلغَه ولم يُنجَزْ فالحمرةُ حقٌّ تُقرأُ لا تُؤجَّلُ ثالثةً) | **مُغلَقٌ في أداةِ المساءلةِ بـ`W-126`** — العددُ صارَ يُلزَمُ ثمانيًا في الجهتَينِ (‏`DECLARED_COLUMNS`) برسالةٍ تُسمّي سببَ الزيادةِ، والقسمُ صارَ على `\|` غيرِ المهروبِ وحدَه فـ`\|` محتوًى لا حدٌّ (‏قاعدةُ GFM)، وصُحِّحَ الصفّانِ بالهربِ لا بحذفِ نصِّهما. **ويبقى مفتوحًا مسحُ سائرِ الأدواتِ** في `WI-040` — فالإغلاقُ هنا لموضعٍ واحدٍ لا لجنسِ العَطبِ |
| DISC-049 | P2 | إنشاءُ الالتزامِ عبرَ واجهةِ Git Data في GitHub مقابلَ حرسَي طابعِ الالتزامِ ([`test_w110_commit_stamp_integrity.py`](../../../tests/governance/test_w110_commit_stamp_integrity.py) · [`test_w113_new_commit_stamp_utc.py`](../../../tests/governance/test_w113_new_commit_stamp_utc.py)) | **إزاحةُ الطابعِ تُؤخَذُ من البيئةِ حينَ لا تُصرَّحُ**: إنشاءُ التزامٍ بـ`POST /git/commits` بلا حقلَي `author.date`/`committer.date` يكتُبُ في كائنِ الالتزامِ إزاحةَ المُنشِئِ المحلّيّةَ لا `+00:00`، ووجهُ الواجهةِ (`GET`) يعرضُ الطابعَ **مُطبَّعًا إلى `Z`** فيُقرأُ سليمًا وهو ليسَ كذلك. فالمُنشِئُ لا يرى العَطبَ في الأداةِ التي أنشأَ بها، ويراه `git log` في CI وحدَه | قِيسَ في `W-127`: الالتزامُ `0b907583` أُنشِئَ بلا تصريحٍ فحملَ `2026-09-03T16:34:30+03:00`، فسقطَ فحصانِ في تشغيلِ CI **84** (`دَينُ الطوابعِ: مقيسٌ 136 · المُعلَنُ 135` · `التزامٌ جديدٌ بإزاحةٍ محلّيّةٍ`) في وظيفتَي «Identity Law» و«Cross-System Suites» معًا، و`GET /git/commits/0b907583` يقولُ في الوقتِ نفسِه `2026-09-03T13:34:30Z` | **حمرةٌ لا يدُلُّ عليها شيءٌ محليًّا**: الحرسانِ يُعلِنانِ «لم يُقَسْ» في مرآةٍ مقطوعةِ التاريخِ (`BaselineNodeUnreachable`) فيُتخطّيانِ، فلا يظهرُ العَطبُ إلّا بعدَ الدفعِ. والأخطرُ أنَّ سببَه **أداةُ الدفعِ لا محتوى العملِ**، فيُلتمَسُ في التغييرِ ولا يُوجَدُ | `WI-039` (‏قُيِّدَ وصُحِّحَ أثرُه في `W-127` بإعادةِ إنشاءِ الالتزامِ بإزاحةٍ مُصرَّحةٍ `+00:00`) · **وحرسُه المُسبَقُ غيرُ مكتوبٍ بعدُ** ففُتِحَ له `WI-041` (`READY`) لأنَّ مسارَه غيرُ مسارِ `WI-040` · **استحقاق: 2026-09-30** (‏**مُجدَّدٌ في `W-160` بسببٍ مكتوبٍ لا مطويٍّ**، والاستحقاقُ الأوّلُ **2026-09-11** يبقى مكتوبًا ههنا لا يُمحى: مضى ولم يُنجَزْ، والحاجزُ **مقيسٌ لا مُدَّعًى** — وجهتُه `WI-041` (`READY`) — ومسارَاه `commit_timestamp_integrity.py` و`docs/PROJECT_HANDBOOK.md` **مقفولانِ** لِـ`WI-035` و`WI-023` (`IN_REVIEW`). والتجديدُ **رفعُ حاجزٍ زمنيٍّ لا إنجازٌ ولا إغلاقٌ ولا تخفيفُ حرسٍ**، وأفقُه عينُ أفقِ الحجوزِ القائمِ **2026-09-30** فلا يُخترَعُ أفقٌ ثالثٌ — ومتى بلغَه ولم يُنجَزْ فالحمرةُ حقٌّ تُقرأُ لا تُؤجَّلُ ثالثةً) | **مُصحَّحٌ أثرُه · مفتوحٌ سببُه**: الالتزامُ أُعيدَ إنشاؤُه `b2aa8306` بحقلَي تاريخٍ مُصرَّحَينِ `+00:00` فعادَ الدَينُ إلى **135** كما هو مُعلَنٌ، ولم يُرفَعْ رقمٌ مُعلَنٌ ولا خُفِّفَ فحصٌ. **والسببُ باقٍ**: لا شيءَ يمنعُ دفعةً قادمةً من تكرارِه — التصريحُ بالإزاحةِ اليومَ **عُرفٌ لا حرسٌ**، وحرسُه في `WI-040` |
| DISC-050 | P2 | خطوةُ «بوّابة 2ج — اختباراتُ حدِّ سرِّ الملكِ» في [`ci.yml:432`](../../../.github/workflows/ci.yml) (‏وظيفةُ «Crown Root of Trust») | **بوّابةٌ تُغيِّرُ حكمَها على شجرةٍ لا تتغيَّرُ**: الخطوةُ سقطَت في المحاولةِ الأولى ونجحَت في الثانيةِ **على العقدةِ نفسِها بلا تغييرِ حرفٍ**، فالحكمُ ليس دالّةً في الشجرةِ وحدَها. والخطوةُ تُثبِّتُ الحزمةَ بـ`pip install -e . --no-deps` فتُحَلُّ تبعيّاتُها من محيطٍ متغيِّرٍ لا من قفلٍ. **وسببُ السقوطِ لم يُقرَأْ**: تنزيلُ سجلِّ الوظيفةِ يُحوَّلُ إلى مُضيفٍ غيرِ `api.github.com` وهو محجوبٌ في بيئةِ التنفيذِ، وتعليقاتُ الفحصِ لم تحمِلْ إلّا «رمزُ الخروجِ 1» | قِيسَ في `W-128`: تشغيلُ CI **86** (`33767648556` · العقدةُ `8de10b14`) — المحاولةُ **1** فشلٌ **12/13** بسقوطِ هذه الخطوةِ وحدَها، والمحاولةُ **2** نجاحٌ **13/13** (`READABLE`) بلا تعديلٍ. وتشغيلُ **85** على العقدةِ الأمِّ كانَ أخضرَ في هذه الوظيفةِ نفسِها | **حمرةٌ لا تدُلُّ على عَطبٍ، وخُضرةٌ لا تُبرِّئُ**: بوّابةٌ متذبذبةٌ تُعلِّمُ قارئَها أنَّ الحمرةَ قد تُعادُ لا أن تُصلَحَ — وذاكَ أخطرُ من سقوطٍ ثابتٍ. **ولا يُدَّعى هنا أنَّها تذبذُبٌ محضٌ**: السببُ لم يُقرَأْ، فيبقى احتمالُ عَطبٍ حقيقيٍّ يظهرُ بشرطٍ لم يُعزَلْ | `WI-042` (‏عزلُ السببِ: تثبيتُ التبعيّاتِ من قفلٍ · وطريقٌ لقراءةِ سجلِّ الوظيفةِ لا يمرُّ بمُضيفٍ محجوبٍ) · **استحقاق: 2026-09-30** (‏**مُجدَّدٌ في `W-160` بسببٍ مكتوبٍ لا مطويٍّ**، والاستحقاقُ الأوّلُ **2026-09-11** يبقى مكتوبًا ههنا لا يُمحى: مضى ولم يُنجَزْ، والحاجزُ **مقيسٌ لا مُدَّعًى** — وجهتُه `WI-042` (`READY`) — ومسارُه `.github/workflows/ci.yml` **مقفولٌ** لِـ`WI-023` (`IN_REVIEW`). والتجديدُ **رفعُ حاجزٍ زمنيٍّ لا إنجازٌ ولا إغلاقٌ ولا تخفيفُ حرسٍ**، وأفقُه عينُ أفقِ الحجوزِ القائمِ **2026-09-30** فلا يُخترَعُ أفقٌ ثالثٌ — ومتى بلغَه ولم يُنجَزْ فالحمرةُ حقٌّ تُقرأُ لا تُؤجَّلُ ثالثةً) | **مفتوحٌ · ولم يُصلَحْ ولم يُلتَمَسْ له عذرٌ**: أُعيدَ تشغيلُ الوظيفةِ **لقياسِ ثباتِ الحكمِ لا لتخضيرِه**، والنتيجتانِ مكتوبتانِ معًا في السجلِّ. ولم تُعَدَّلْ `ci.yml` (‏مقفولةٌ بـ`WI-023`) ولم يُخفَّفْ فحصٌ ولم يُسكَتْ |
| DISC-051 | P2 | [`tests/governance/test_identity_law.py:279`](../../../tests/governance/test_identity_law.py) — قائمةُ المُولِّداتِ المُسوَّرةِ · وعمومًا: **رباطُ وجهِ `--check` بالمستودعِ الحقيقيِّ** | **وجهُ الاستقرارِ مكتوبٌ في أدواتٍ ولا يُشغَّلُ على الشجرةِ الحقيقيّةِ، والرباطُ قائمةٌ بخطِّ اليدِ**: الفحصُ الذي يُثبِتُ أنَّ المُولِّدَ «مُستقرٌّ» يُسمّي ثلاثةَ مُولِّداتٍ بأسمائِها نصًّا، فمُولِّدٌ جديدٌ يُضافُ إلى `tools/` **لا يدخُلُ الحرسَ ولا يُبلَّغُ عن غيابِه** — وغيابُ الرباطِ لا يُقاسُ أصلًا. فالأثرُ المُولَّدُ قد يتقادَمُ في الشجرةِ ولا تحمَرُّ بوّابةٌ محلّيّةٌ، وهو **جنسُ عَطبِ `DISC-040`** الذي أُغلِقَ لأداةٍ واحدةٍ (`truth_audit.py` · `W-099`) لا لجنسِه | قِيسَ في `W-129` على الشجرةِ الحقيقيّةِ: في `tools/` **إحدى عشرةَ** أداةً تكتُبُ أثرًا في الشجرةِ **وتُعلِنُ وجهَ `--check`**، ولا يُشغَّلُ وجهُها على المستودعِ الحقيقيِّ إلّا لـ**أربعٍ** (`write_domain_readmes` · `stamp_readme_identity` · `generate_identity_cards` عبرَ `test_identity_law.py` · و`truth_audit` عبرَ `test_w099`)، فالباقيةُ **سبعٌ** بلا رباطٍ مقيسٍ: `generate_crown_truth_matrix` · `generate_crown_threat_doc` · `in_memory_inventory` · `pricing_divergence` · `restart_survival_probe` · `schema_inventory_drift` · `verify_cross_system_suites`. والرقمُ المُعلَنُ يُقرأُ من هذا السطرِ وحدَه (‏مصدرُ حقيقةٍ واحدٌ لا ثابتٌ مدفونٌ في فحصٍ) ⇒ `GENERATOR_SETTLEMENT_BASELINE: generators=11 bound=4 unbound=7` | **حرسٌ يُسمّي محروسَه بخطِّ اليدِ يحرُسُ قائمتَه لا جنسَ العَطبِ**: كلُّ مُولِّدٍ يُضافُ بعدَه يولَدُ خارجَ الحرسِ بصمتٍ، والصمتُ يُقرأُ سلامةً. وأخطرُ من مُولِّدٍ غيرِ محروسٍ **أن لا يُعرَفَ عددُ غيرِ المحروسِ** | حرسٌ قائمٌ: `tools/governance/generator_settlement_closure.py` · `tests/governance/test_w129_generator_settlement_closure.py` — يُحصي المُولِّداتَ ورباطَها ويُسقِطُ إن نما غيرُ المربوطِ أو نقصَ المربوطُ · `WI-043` | مفتوحٌ — **مقيسٌ ومحروسٌ من النموِّ لا مُبرَأٌ**: السبعةُ تبقى بلا رباطٍ، والمُنجَزُ أنَّ عددَها صارَ مُعلَنًا مقيسًا لا يعلو · وربطُ كلِّ واحدةٍ يمسُّ مساراتٍ مُدَّعاةً ويحتاجُ بيئةً حيّةً لبعضِها |
| DISC-052 | P2 | [`tools/governance/suite_size_inventory.py`](../../../tools/governance/suite_size_inventory.py) — قارئُ الدعاوى · ورقمُ حزمةِ الجذرِ في [`PROJECT_HANDBOOK.md:156`](../../PROJECT_HANDBOOK.md) و**541** | **بوّابةٌ تُعلِنُ حكمًا أوسعَ مِمّا قاسَت**: خرجُها الناجحُ يقولُ «كلُّ رقمِ حزمةٍ مكتوبٍ يُطابِقُ الجمعَ الحيَّ» وهي **لا تقرأُ رقمَ حزمةِ الجذرِ أصلًا**. فقارئُها لا يُصنِّفُ «حيًّا» إلّا صيغةَ «نجحَت/مُتخطّاة»، ورقمُ الجذرِ مكتوبٌ بصيغةِ «**المُجمَّعُ حيًّا** N» فيُقرأُ **تاريخيًّا** أو لا يُقرأُ — فرقمٌ كُتِبَ قياسًا حيًّا يتقادَمُ بلا حرسٍ، وذاكَ **عينُ `DISC-046`** الذي فُتِحَ له `WI-038`، عائدًا في صيغةٍ لم يرَها القارئُ | قِيسَ في `W-129`: `python tools/governance/suite_size_inventory.py --json` ⇒ الدعوى الحيّةُ **2** وكلتاهما `suite=services` (‏السطرانِ 157 و548)، والسطرُ 156 يُعطي أربعَ دعاوى كلُّها `HISTORICAL_DECLARED` **فلا دعوى حيّةً لحزمةِ الجذرِ**. والمكتوبُ فيه `2398` (‏قياسُ `W-126`) والمقيسُ الآنَ `python -m pytest tests/ -q -p no:randomly --collect-only` ⇒ **2421** بعدَ إنزالِ ثلاثةٍ وعشرينَ فحصًا في `W-129` — فارقٌ **23** مرَّ على بوّابةٍ رمزُ خروجِها 0 | **خُضرةٌ أوسعُ من قياسِها تُعلِّمُ قارئَها أنَّ ما لا يُقاسُ مقيسٌ**: رقمٌ في وثيقةِ الحالةِ يُقرأُ محروسًا وهو حرٌّ، فكلُّ دفعةٍ تُغيِّرُ حجمَ الحزمةِ تُقادِمُه بصمتٍ. **ولا يُدَّعى أنَّ العَطبَ في الرقمِ**: الرقمُ صُحِّحَ في `W-129` بقياسٍ طازجٍ، والعَطبُ في **حدِّ القارئِ ونصِّ حكمِه** | `WI-044` (‏قارئُ الدعاوى يقرأُ صيغةَ «المُجمَّعُ حيًّا» فيُقاسُ رقمُ الجذرِ بـ`--collect-only`، **أو** يُضيَّقُ نصُّ الحكمِ فلا يُعلِنُ ما لم يُقَسْ) · **استحقاق: 2026-09-30** (‏**مُجدَّدٌ في `W-160` بسببٍ مكتوبٍ لا مطويٍّ**، والاستحقاقُ الأوّلُ **2026-09-11** يبقى مكتوبًا ههنا لا يُمحى: مضى ولم يُنجَزْ، والحاجزُ **مقيسٌ لا مُدَّعًى** — وجهتُه `WI-044` (`READY`) — ومسارَاه `suite_size_inventory.py` و`test_w120_*` **مقفولانِ** لِـ`WI-038` (`IN_REVIEW`). والتجديدُ **رفعُ حاجزٍ زمنيٍّ لا إنجازٌ ولا إغلاقٌ ولا تخفيفُ حرسٍ**، وأفقُه عينُ أفقِ الحجوزِ القائمِ **2026-09-30** فلا يُخترَعُ أفقٌ ثالثٌ — ومتى بلغَه ولم يُنجَزْ فالحمرةُ حقٌّ تُقرأُ لا تُؤجَّلُ ثالثةً) | مفتوحٌ — **الرقمُ صُحِّحَ والحدُّ لم يُحرَسْ**: ولم يُخفَّفْ حرسٌ ولم يُسكَتْ ماسحٌ، ولم تُمَسَّ الأداةُ (‏مُدَّعاةٌ لـ`WI-038` · `IN_REVIEW`) |
| DISC-055 | P2 | [`tools/governance/`](../../../tools/governance) — **الملاحظاتُ المُبلِّغةُ لا المُسقِطةُ** في ستِّ بوّاباتٍ (`enforcement_path_ledger` · `guard_enforcement_closure` · `open_record_accountability` · `sovereign_decision_status` · `state_document_drift` · `surface_debt_trend`) | **دَينٌ يُعَدُّ ولا يُرتَّجُ**: بوّابةٌ تطبعُ سطرًا يبدأُ بـ«ملاحظة ·» يحمِلُ **عددًا مقيسًا** ثمَّ تخرجُ برمزِ **0** — فالعددُ يُقرأُ ولا يُمسَكُ. ولا يُفرَّقُ في الخرجِ بينَ رقمٍ **إخباريٍّ** لا يُرادُ حبسُه (‏جردُ أنواعٍ) ورقمِ **دَينٍ** يجبُ ألّا يعلوَ (‏أدواتٌ بلا طريقِ إنفاذٍ · صفوفٌ بيدِ المالكِ بلا حرسٍ): الحرفُ واحدٌ والرمزُ واحدٌ. فيستطيعُ الدَّينُ أن ينموَ **إلى ما لا نهايةَ** وكلُّ بوّاباتِ § 5.4 خضراءُ | قِيسَ في `W-132` تركيبيًّا على الشجرةِ الحقيقيّةِ (‏استدعاءُ `‹تقرير›.notes.append(...)` بوسيطٍ أوّلَ ثابتٍ ورسالةٍ تحمِلُ `FormattedValue`): **ثمانيةَ عشرَ** سطرَ ملاحظةٍ كلُّها **تحمِلُ عددًا مقيسًا**، و**لا واحدَ** منها يحبِسُ عددَه أساسٌ مُعلَنٌ — فثمانيةَ عشرَ عددًا مقيسًا **بلا سقفٍ**، ثمَّ صارَت تسعةَ عشرَ بملاحظةِ هذا الحرسِ نفسِه وواحدةٌ منها محبوسةٌ. ومنها ما هو دَينٌ صريحٌ: `VERDICT_CAPABLE_UNWIRED` (‏قِيسَ **11** أداةً تحكُمُ برمزِ خروجٍ ولا يُشغِّلُها شيءٌ) و`OWNER_HELD_WITHOUT_GUARD` و`OUTSIDE_RANGE_REFERENCES`. **وحدَثَ نموُّه فعلًا في هذه الدورةِ**: جردُ `UNWIRED_TOOL_INVENTORY` صعِدَ **20 ← 21** بينَ `W-130` و`W-131` بنزولِ حرسٍ جديدٍ، ولم تُسقِطْ ذاكَ الصعودَ بوّابةٌ ولا سمَّتْه — قُرِئَ بالعينِ في سطرِ خرجٍ. والرقمُ المُعلَنُ يُقرأُ من هذا السطرِ وحدَه ⇒ `REPORT_NOTE_BASELINE: notes=19 counted=19 ratcheted=1 unratcheted=18` — والحبسُ **إعلانٌ مقصودٌ** يُقرأُ من سطرٍ مُخصَّصٍ لا من ذِكرٍ عارضٍ في نثرٍ: `REPORT_NOTE_RATCHETED: REPORT_NOTE_TALLY` (‏‏وحرسُ هذا القيدِ يُحصي نفسَه: ملاحظتُه `REPORT_NOTE_TALLY` **مُعلَنٌ حبسُها** أعلاه لأنَّ عددَها هو ما يحبِسُه `REPORT_NOTE_BASELINE` فعلًا — لا لأنَّها ذُكِرَت) | **الخُضرةُ صارَت تحمِلُ دَينَها**: `DISC-053` قالَ إنَّ الحكمَ الكلّيَّ لا يقولُ كم قرأَ، و`DISC-054` قالَ إنَّ الأثرَ يشهدُ بنقصِه ولا سامِعَ — وهذا ثالثُهما: البوّابةُ **تقولُ الرقمَ صراحةً** ثمَّ تُخرِجُ 0. فالمقامُ حاضرٌ والشهادةُ حاضرةٌ **والسقفُ غائبٌ**، وغيابُ السقفِ يجعلُ القياسَ الصادقَ بلا أثرٍ تنفيذيٍّ. وليسَ الحلُّ أن يُسقِطَ كلُّ عددٍ (‏ذاكَ قرارُ مالكٍ في كلِّ رقمٍ على حدةٍ · فئةُ تغييرٍ أعلى)، بل أن يصيرَ **عددُ الأعدادِ بلا سقفٍ سقفًا** لا يعلو | `WI-047` (‏حرسٌ يُحصي ملاحظاتِ الإبلاغِ الحاملةَ عددًا ويرتِّجُ عددَ ما لا سقفَ له، والمرتَّجُ **أرضٌ لا تنزلُ** فلا يُحذَفُ إعلانُ سقفٍ) · **استحقاق: 2026-09-13** | مفتوحٌ — **قِيسَ ونُزِلَ حرسُه في `W-132`**: صارَ الصمتُ عن الدَّينِ مقيسًا محبوسًا، وبقيَ إنزالُ سقفٍ لكلِّ رقمٍ على حدةٍ عملًا لاحقًا يمسُّ مساراتٍ مُدَّعاةً |
| DISC-057 | **P2** | منهجُ القياسِ نفسُه — إعلانُ حدِّ صدقٍ عن نقطةِ نهايةٍ مهجورةٍ | **حدُّ صدقٍ أُعلِنَ أوسعَ من الحقيقةِ**: أُعلِنَ في `W-137` أنَّ علّةَ الحاجزِ «لا تُعرَفُ عيانًا» وأنَّ المقيسَ أثرُها وحدَه — وسندُ الإعلانِ استدعاءٌ واحدٌ رجعَ **HTTP 410**. ورسالةُ الـ410 نفسُها تُحيلُ إلى النقطةِ الحاليّةِ، ولم تُجرَّبْ قبلَ الإعلانِ. فجُرِّبَت في `W-138` فرجعَت **200** والعلّةُ **رقمٌ يُعادُ بأمرٍ واحدٍ**. **فالحدُّ لم يكن حدَّ صلاحيةٍ بل حدَّ أداةٍ**، وإعلانُ المجهولِ حيثُ المعلومُ مُتاحٌ **يُغلِقُ بابَ قياسٍ باسمِ الصدقِ** ويُثبِّتُ حاجزًا في «بيدِ المالكِ» بلا رقمٍ يُخاطَبُ به المالكُ | `W-137` أعلنَ الحدَّ · و`W-138` رفعَه: `.../billing/actions` ⇒ 410 · `.../billing/usage?year=2026&month=9` ⇒ 200 · أيلولُ **2674** دقيقةً مقابلَ مُعلَنٍ منشورٍ **2000** | **حاجزٌ يُقيَّدُ بلا رقمٍ يُقرأُ غيرَ قابلٍ للمخاطبةِ**: المالكُ يُطلَبُ منه «راجِعِ الحسابَ» بدلًا من «نفَدَت 2674/2000 وتُصفَّرُ في أوّلِ الشهرِ». وفي الاتّجاهِ الأخطرِ: حدٌّ مُعلَنٌ بلا تجريبِ البديلِ المُحالِ إليه يصيرُ **عذرًا بنيويًّا** يُعادُ في كلِّ قيدٍ بعدَه | **قاعدةٌ تُقيَّدُ ولا يُدَّعى لها حرسٌ**: قبلَ إعلانِ حدِّ صدقٍ بصيغةِ «لا يُعرَفُ»، تُجرَّبُ النقطةُ البديلةُ المُعلَنةُ في جسمِ الخطأِ نفسِه (‏410 · 301 · `documentation_url`) ويُقيَّدُ ما رجعَ. **ولا حرسَ آليًّا لهذه القاعدةِ اليومَ** لأنَّ نيّةَ كاتبِ الحدِّ لا تُقاسُ نصًّا؛ فتُقيَّدُ قاعدةً لا سقّاطةً، ولا يُدَّعى لها إنفاذٌ | مُعالَجٌ موضِعًا في `W-138` — الحدُّ المُعلَنُ في `W-137` رُفِعَ بقياسٍ ولم يُمحَ نصُّه، والقاعدةُ فوقَه مُقيَّدةٌ بلا دعوى حرسٍ |
| DISC-058 | P1 | `THE_ROADMAP.md § 16.5` مقابلَ `.github/workflows/ci.yml` | **وثيقةٌ تُعلِنُ إنفاذًا وملفُّ التشغيلِ يقيسُ إبلاغًا، ولا بوّابةَ تقارِنُ بينَهما.** اعتُمِدَ `A-3` `APPROVED` في 2026-09-07 فكُتِبَ في § 16.5 أنَّ `work-governance-gate` «نافذةٌ بوضعِ الإسقاطِ»، وبقيَ `ci.yml:211-222` يستدعي الفاحصَ بـ`--advisory` باسمِ خطوةٍ «(إبلاغ)» وبتعليقٍ يقولُ «حتى يُعتمَدَ A-3». فالدعوى عاشَت **يومًا كاملًا** بلا أثرٍ يقيسُها، ولم يكشِفْها فحصٌ آليٌّ — كشفَها **مراجعانِ مستقلّانِ** بإجماعٍ. والعلّةُ الأعمُّ: **بوّاباتُنا تقيسُ الشجرةَ ولا تقيسُ التطابقَ بينَ ما تقولُه وثيقةٌ حاكمةٌ وما يفعلُه ملفُّ تشغيلٍ** | `grep -n -- "--advisory" .github/workflows/ci.yml` عندَ `a995b00` ⇒ سطرانِ في وظيفةِ الحوكمةِ · و§ 16.5 الحدُّ 3 عندَ العقدةِ نفسِها يقولُ «نافذةٌ بوضعِ الإسقاطِ» · وتقريرا المراجعَينِ المعزولَينِ يُسقِطانِه معًا | كلُّ التزامٍ مُخالِفٍ بينَ 2026-09-07 و`W-150` مرَّ بوظيفةِ حوكمةٍ خضراءَ مهما خالفَ. **ولم يُقَسْ أثرٌ فعليٌّ**: لا التزامَ مُخالِفٌ عُرِفَ في تلك النافذةِ، والضررُ **احتماليٌّ لا محقَّقٌ** — ولا يُضخَّمُ ولا يُصغَّرُ | `WI-051` — أُصلِحَ في `W-150`: رُفِعَ `--advisory`، وضُيِّقَ نصُّ § 16.5 ليقولَ تاريخَه، ونزلَ حارسٌ `tests/governance/test_w150_gate_enforcement_in_ci.py` يقرأُ الملفَّ ويُسقِطُ متى عادَ | **مُغلَق** (‏`W-150` · 2026-09-08) |
| DISC-061 | P2 | [`tools/governance/open_record_accountability.py`](../../../tools/governance/open_record_accountability.py) — `_classify_open` والسطران 106–107 (`CLOSED_MARKERS` ثمَّ `OPEN_MARKERS`) | **صفٌّ يُعلِنُ إغلاقَه في أوّلِ خليّتِه تقرؤه الأداةُ مفتوحًا لأنَّ كلمةَ «مفتوح» وردَت في سردِ تاريخِه.** الترتيبُ في `_classify_open` يفحصُ علامةَ الفتحِ أوّلًا ويُرجِعُ `STATUS_SAYS_OPEN` بمجرَّدِ ورودِها في أيِّ موضعٍ من الخليّةِ، فيغلِبُ سردُ الماضي («كانَ قبلَه مفتوحًا») إعلانَ الحاضرِ («مُغلَقٌ بـQ-43») الواقعَ في أوّلِ الخليّةِ. وهذا صِنفُ عَطبِ `RK-020` في الجولةِ الأولى: **دعوى إغلاقٍ والقياسُ يقرأُ فتحًا** — عولِجَ للخطرِ برفعِ الصفِّ من الجدولِ، ولم يُعالَجْ للأداةِ | أسقطَه المراجعُ ب (Grok 4.6) في الجولةِ الثانيةِ بقياسٍ مباشرٍ: `_classify_open(cells[7])` على صفِّ `DISC-027` ⇒ `(True, 'STATUS_SAYS_OPEN')` بينما الخليّةُ تبدأُ «**مُغلَقٌ بـQ-43**» | **العلاجُ الصحيحُ للأداةِ لا للنصِّ**: تُقرأُ العلامةُ من **مطلعِ** الخليّةِ لا من كلِّ موضعٍ فيها، أو تُقدَّمُ `CLOSED_MARKERS` عندَ وقوعِها في أوّلِ جملةٍ. وقد صُحِّحَ النصُّ ههنا («قائمًا» بدلَ «مفتوحًا») حتى يصدُقَ القياسُ، **وهذا ترقيعٌ يُسمّى ولا يُدَّعى إصلاحًا للأداةِ** | `WI-051` — قُيِّدَ في `W-152`؛ وتعديلُ الأداةِ خارجَ نطاقِ هذا البندِ (مسارُه محجوزٌ بـ`WI-024`) · **استحقاق: 2026-09-15** | **مفتوح** |
| DISC-062 | P1 | [`tools/governance/open_record_accountability.py`](../../../tools/governance/open_record_accountability.py) — سقّاطةُ `DUE_DATE_PASSED` · ووظيفةُ «Work Governance» في [`ci.yml`](../../../.github/workflows/ci.yml) خطوةَ «بوّابة 6» | **حكمُ CI دالّةٌ في الزمنِ لا في الشجرةِ وحدَها: عقدةٌ قُرِئَت خضراءَ 14/14 تصيرُ حمراءَ بلا تغييرِ حرفٍ فيها.** سقّاطةُ `DUE_DATE_PASSED` تُقارِنُ الاستحقاقَ المكتوبَ بتاريخِ **يومِ التشغيلِ**، فكلُّ قيدٍ مفتوحٍ باستحقاقٍ يصيرُ مخالفةً بمجرَّدِ مرورِ يومِه — و`main` يحمرُّ من نفسِه دونَ أن يدفعَ أحدٌ شيئًا. وهذا يُبطِلُ في الحقيقةِ **دلالةَ الحكمِ المُقيَّدِ على عقدةٍ**: القيدُ يقولُ «هذه العقدةُ خضراءُ» وهو صادقٌ في لحظتِه فقط، فلا يُستشهَدُ به بعدَ يومِه إلّا بتاريخِ قراءتِه — وهو **الوجهُ الزمنيُّ من `DISC-060`** (حدُّ تسلسلِ الحكمِ) في الاتِّجاهِ المقابلِ: هناكَ عقدةٌ بلا حكمٍ بعدُ، وههنا حكمٌ يتقادَمُ على عقدةٍ ثابتةٍ | **قِيسَ عيانًا في `W-160` لا استنتاجًا**: عقدةُ `97e9acd` (`W-158`) قُرِئَت في `W-159` نفسِه **أخضرَ 14 · 0 حمراءَ · 0 متخطّاةٍ** (‏التشغيلُ [`34564791386`](https://github.com/vooovg-ui/AMOS-Fedration/actions/runs/34564791386) · 2026-09-11)؛ ثمَّ شُغِّلَت الأداةُ نفسُها على **عينِ تلكَ العقدةِ** في شجرةِ عملٍ منفصلةٍ (`git worktree add /tmp/mainwt origin/main`) يومَ 2026-09-13 ⇒ **رمزُ خروجٍ 1** وستُّ مخالفاتِ `DUE_DATE_PASSED` (`DISC-021` · `DISC-048` · `DISC-049` · `DISC-050` · `DISC-052` · `DISC-054`). وفي تشغيلِ CI **24** على فرعِ `W-159` سقطَت وظيفةُ «Work Governance» في خطوةِ «بوّابة 6» وحدَها بالسببِ عينِه، وبقيَت الاثنتا عشرةَ وظيفةً خضراءَ — فالسقوطُ **ليسَ من عملِ `W-159`** | **ولا يُخفَّفُ الحرسُ ولا يُسكَتُ**: هذه السقّاطةُ تعملُ عملَها المقصودَ — استحقاقٌ مضى لم يُنجَزْ، والحمرةُ صدقٌ لا عَطبٌ. والعَطبُ المُقيَّدُ ههنا **في دلالةِ الحكمِ لا في السقّاطةِ**: (‏1) كلُّ قيدٍ يقولُ «العقدةُ خضراءُ» يلزمُه من اليومِ **تاريخُ القراءةِ** لا رقمُ التشغيلِ وحدَه؛ (‏2) و`main` قابلٌ للحمرةِ بمرورِ الوقتِ فلا تُقرأُ خُضرتُه صفةً دائمةً؛ (‏3) والتجديدُ المتكرِّرُ للاستحقاقِ يُنتِجُ **حمرةً مُؤجَّلةً بلا حدٍّ** إن لم يُقَسْ عددُ التجديداتِ — وذاكَ ما يلزمُه حرسٌ | **وجهتُه الأداةُ لا النصُّ**: يُوجَّهُ إلى `tooling-gates` حرسٌ يقيسُ **عددَ تجديداتِ الاستحقاقِ لكلِّ قيدٍ** ويرفضُ نموَّه (أرضٌ لا تنزلُ)، **و**سطرُ حكمٍ في السجلِّ يُلزِمُ ذِكرَ **تاريخِ قراءةِ الحكمِ** مع رقمِ التشغيلِ. ومسارُ `open_record_accountability.py` **مقفولٌ الآنَ لِـ`WI-039`** (`IN_PROGRESS`) بنصِّ § 6.1، فلا يُمَسُّ ههنا ولا يُفتَحُ له بندٌ مُتقاطِعٌ: يُقيَّدُ ويُوجَّهُ · **استحقاق: 2026-09-30** | **مفتوح** |
| DISC-063 | **P1** | [`docs/governance/work/ACTIVE_WORK.md`](ACTIVE_WORK.md) — صفوفُ § 1 وكتلُ § 3 للبنودِ `WI-023` · `WI-024` · `WI-032` · `WI-035` · `WI-037` · `WI-038`؛ و[`tools/governance/suite_size_inventory.py`](../../../tools/governance/suite_size_inventory.py) | **الجولةُ الخامسةُ من مجلسِ المراجعةِ المستقلِّ (`Q-43`) ردَّت البنودَ الستَّةَ كلَّها، وأكثرُ عيوبِها عيوبُ سجلٍّ لا عيوبُ شِفرةٍ: الأدلّةُ التقنيّةُ أُعيدَ تشغيلُها فخرجَت خضراءَ، والحقولُ الإلزاميّةُ التي تصِفُها كاذبةٌ أو متعارضةٌ بينَ الصفِّ والكتلةِ.** واتّحادُ العيوبِ سبعُ طبقاتٍ: (‏1) خليّةُ «العائق» في الصفوفِ الستَّةِ تقولُ «البندُ لم يُسلَّمْ ولم يُشغَّلْ له مراجعٌ بعدُ» والحالةُ `IN_REVIEW`؛ (‏2) وهي تناقضُ سطرَ «العائق:» في الكتلةِ (‏«انتظارُ دورِ المراجعةِ» أو `—`)؛ (‏3) خليّةُ مساراتِ `WI-023` ملفّانِ والكتلةُ تُعلِنُ خمسةً — فقفلُ § 6.1 **لا يقعُ** على `.github/workflows/ci.yml` مع أنَّ صفَّ `WI-042` يكتبُ أنَّه مقفولٌ به؛ (‏4) أرقامُ دليلِ `WI-023` وحدِّه متقادمةٌ (‏المكتوبُ «4 دعاوى · 4 منفَذةٌ» و«ثلاثَ عشرةَ أداةً غيرَ مربوطةٍ»)؛ (‏5) «الخطوةُ التالية» و«قيدُ السجلّ» في صفِّ `WI-024` يسبقانِ تسليمَه بـ`W-082`؛ (‏6) أمرُ الدليلِ المكتوبُ في `WI-038` لا يُعادُ تشغيلُه؛ (‏7) `suite_size_inventory.py` يطبعُ حكمًا كلّيًّا ولا يقرأُ دعوى حزمةِ الجذرِ أصلًا (‏جنسُ `DISC-052`). **وطبقةٌ ثامنةٌ أسقطَها المراجعُ أ على تاريخٍ مدموجٍ**: `docs/PROJECT_HANDBOOK.md` مقفولٌ لِـ`WI-023` ومُسَّ في `W-080` (`1cd662c`) و`W-121` (`d7333c0`) بلا دعوى ولا استثناءٍ من مسالكِ § 6.1 الثلاثةِ | **أحكامٌ قُرِئَت من تقريرَينِ مستقلَّينِ لا من دعوى**: الجولةُ شُغِّلَت على عقدةِ `b951dcac` في حاويتَينِ منفصلتَينِ، وكلُّ مراجعٍ أعادَ تشغيلَ الدليلِ بنفسِه وقرأَ حكمَ CI بنفسِه. **المراجعُ أ (‏GPT 5.6 Sol)**: `WI-023` `REJECTED` `P1=4` · `WI-024` `REJECTED` `P1=3` · `WI-032` `REJECTED` `P1=1` · `WI-035` `VERIFIED` `P1=0` · `WI-037` `VERIFIED` `P1=0` · `WI-038` `REJECTED` `P1=3` — جملةُ `P1` **11**. **والمراجعُ ب (‏Grok 4.6)**: الستّةُ `REJECTED` — `P1` بندًا بندًا 3 · 4 · 2 · 2 · 2 · 4، وجملةُ عيوبِه المميَّزةِ **7** (`D-1`…`D-7`) لأنَّ عيبَينِ منها يقعانِ على الستّةِ معًا. وأرقامُهما المقيسةُ متوافقةٌ حيثُ قاسا الشيءَ نفسَه: `guard_enforcement_closure` ⇒ **5 دعاوى · 5 منفَذةٌ · `UNWIRED_TOOL_INVENTORY` 22** · `test_w077` **19 نجحَت** · `mutation_probe` **31/31** · `gate_dependency_closure` **13 · 12 · 1** · `commit_timestamp_integrity --check` **135 / 3 / 10 / 4** · `test_w117` **11 نجحَت** · `test_w120` **17 نجحَت** · `suite_size_inventory . --check` ⇒ **رمزُ 2** · حزمةُ الحوكمةِ **1178 نجحَت** (‏ب) · وحزمةُ الجذرِ **2490 نجحَت و1 متخطًّى** | **الرفضُ يُقيَّدُ كما وردَ ولا يُطوى خلافٌ**: البنودُ الستُّ نُقِلَت `IN_REVIEW → IN_PROGRESS` (§ 4.3 — انتقالٌ مشروعٌ أوجبَه الردُّ)، ولم يُنقَلْ بندٌ إلى `VERIFIED`: الإجماعُ شرطُ `Q-43`، والانقسامُ في `WI-035` و`WI-037` **لا يُجمَعُ جمعًا حسابيًّا**. وبنودُ `READY` الخمسةُ (`WI-040` · `WI-041` · `WI-042` · `WI-044` · `WI-048`) **تبقى محجوبةً** لأنَّ المساراتِ تبقى مقفولةً في `IN_PROGRESS` كما في `IN_REVIEW` (§ 6.1) — فالحاجزُ لم يُرفَعْ بهذه الجولةِ، وهذا **مقيسٌ لا مُدَّعًى** | **لكلِّ طبقةٍ وجهتُها، ولا يُصلَحُ عيبٌ في قيدِ تقييدِه** (§ 5.2): الطبقاتُ 1–5 وجهتُها بنودُها أنفسُها (`WI-023` · `WI-024`) وقد صارت `IN_PROGRESS` بمالكِها؛ والطبقةُ 6 و7 وجهتُها `WI-038` (‏الأمرُ المكتوبُ) و`WI-044` (‏قارئُ دعوى الجذرِ · `DISC-052`)؛ والطبقةُ 8 وجهتُها `WI-024` و`WI-038` يُعلِنُ كلٌّ منهما خرقَ قفلٍ في تاريخِه المدموجِ كما أعلنَ `W-133` خرقَه (‏إعلانٌ لا محوٌ)؛ وعيبُ `WI-032` وجهتُه `DISC-064` لأنَّ سببَه مقيسٌ في الهُويّةِ لا في البندِ · **استحقاق: 2026-09-30** | **مفتوح** |
| DISC-064 | P2 | [`tools/governance/truth_audit.py`](../../../tools/governance/truth_audit.py) — `_repo_identity` السطرُ **311–320**؛ وأثرُها [`docs/audit/truth_matrix.json`](../../audit/truth_matrix.json) حقلُ `repo`؛ و[`tools/dev/bootstrap.sh`](../../../tools/dev/bootstrap.sh) خطوةَ «نظافةُ الشجرةِ بعدَ التشغيل» | **هُويّةُ الأثرِ المولَّدِ تُقرأُ من `origin`، فكلُّ نسخةٍ بُعدُها مسارٌ محلّيٌّ تُولِّدُ مصفوفةً مختلفةً وتُوسِّخُ شجرتَها** — فأمرُ الدليلِ `bootstrap.sh --verify` (‏الذي يشترطُ شجرةً نظيفةً) **لا يمكنُ أن يخرجَ أخضرَ داخلَ حاويةِ مراجعةٍ مستنسَخةٍ من مسارٍ محلّيٍّ**، وإن كانَ أخضرَ على المستودعِ الحقيقيِّ. والأداةُ تُعلِنُ في وثيقتِها أنَّها تركَت اسمَ المجلَّدِ لأنَّه «يتغيّرُ باستنساخٍ إلى مسارٍ آخرَ» — وقرأَت `origin` فوقَعَت في عينِ ما فرَّت منه حينَ يكونُ `origin` مسارًا محلّيًّا | **قِيسَ عيانًا على العقدةِ نفسِها في بيئتَينِ**: في `/home/user/workspace/amos` (`origin` = بُعدُ GitHub) ⇒ `truth_audit.py . --ratchet` رمزُ **0** · «ثابت عند 63» · `git diff` **فارغٌ**. وفي `council_r5/reviewer_a_repo` (`origin` = `/home/user/workspace/amos`) ⇒ الأمرُ نفسُه على العقدةِ نفسِها يُنتِجُ `- "repo": "AMOS-Fedration"` / `+ "repo": "amos"` و`git status` ⇒ `M docs/audit/truth_matrix.json`، ثمَّ `bootstrap.sh --verify` يختمُ بـ`✗ التشغيلُ غيَّرَ الشجرة` بعدَ **2490 نجحَت · 1 متخطًّى** | **أثرانِ**: (‏1) دعوى «الأثرُ المولَّدُ يُعادُ إنتاجُه بلا فرقٍ» صادقةٌ **بشرطِ بُعدٍ** غيرِ مكتوبٍ — وهو شرطٌ مطويٌّ صارَ الآنَ مُعلَنًا؛ (‏2) **حدُّ طريقةِ العزلِ نفسِها**: استنساخُ حاويةِ المراجعِ من مسارٍ محلّيٍّ **يُغيِّرُ قيمةً مقيسةً**، فأيُّ دليلٍ يشترطُ نظافةَ الشجرةِ يُقرأُ أحمرَ فيها بسببِ الطريقةِ لا بسببِ الشجرةِ — وعلى ذلكَ يُقرأُ عيبُ `WI-032` في الجولةِ الخامسةِ: **مقيسٌ صادقٌ في حاويتِه، وسببُه ههنا لا في البندِ**، ولا يُمحى الرفضُ بهذا البيانِ بل يُقرأُ معَه | `tooling-gates` — ومسارُ `truth_audit.py` **مُدَّعًى لِـ`WI-030`** (`IN_REVIEW`) فهو مقفولٌ بنصِّ § 6.1 ولا يُمَسُّ ههنا: يُقيَّدُ ويُوجَّهُ. والعلاجُ المقترَحُ: تُقرأُ الهُويّةُ من إعلانٍ في الشجرةِ (‏بطاقةُ هُويّةِ المستودعِ) لا من بُعدٍ بيئيٍّ، أو يُعلَنُ الشرطُ في خرجِ الأداةِ. **وحدُّ العزلِ يُعلَنُ في تكليفِ الجولةِ السادسةِ** فلا يُقاسُ مراجعٌ بما لا تملكُه حاويتُه · **استحقاق: 2026-09-30** | **مفتوح** |
| DISC-065 | **P1** | [`tools/governance/commit_timestamp_integrity.py`](../../../tools/governance/commit_timestamp_integrity.py) و[`tests/governance/test_w113_new_commit_stamp_utc.py`](../../../tests/governance/test_w113_new_commit_stamp_utc.py) و[`tests/governance/test_w110_commit_stamp_integrity.py`](../../../tests/governance/test_w110_commit_stamp_integrity.py) — ومحلُّ القياسِ في [`.github/workflows/ci.yml`](../../../.github/workflows/ci.yml) | **حرسُ طوابعِ الالتزامِ يقيسُ التزامًا لا تُنشِئُه الشجرةُ ولا تملكُه**: على حدثِ `pull_request` يكونُ رأسُ الاستخراجِ التزامَ دمجٍ عارضًا تُولِّدُه GitHub في `refs/pull/<N>/merge`، وطابعُه بإزاحةٍ محلّيّةٍ لا يختارُها المستودعُ — فيسقطُ الحرسانِ على التزامٍ **لن يوجدَ في `main` أبدًا**، والحكمُ يصيرُ دالّةً في منطقةِ الفاعلِ الزمنيّةِ لا في الشجرةِ. وهو جنسُ `DISC-062` (‏حكمٌ دالّةٌ في الزمنِ) في وجهٍ آخرَ: **حكمٌ دالّةٌ في محلِّ القياسِ** | **مقيسٌ على العقدةِ نفسِها في محلَّينِ**: التزامُ الفرعِ `51fdc3e` طابعُه `2026-09-13T23:03:06+00:00` (‏`git log -1 --format=%ad`) وحرساهُ يخرجانِ خضراءَ محلّيًّا (‏`pytest tests/governance/test_w110... test_w113... -q` ⇒ **6 نجحَت**، وحزمةُ الحوكمةِ كلُّها **1176 نجحَت و2 متخطًّى**). وفي CI على `PR #2`: تشغيلُ `34788571210` (‏رقمُ 27 · محاولةُ 1 · حدثُ `pull_request`) ⇒ `failure` في وظيفةِ «Identity Law» وحدَها، خطوةً 10، برسالةٍ حرفيّةٍ: «التزامٌ جديدٌ بإزاحةٍ محلّيّةٍ: `a8a02ea018`=`2026-09-14T02:03:24+03:00`» و«`assert not {'non_utc': (136, 135)}`» — و`a8a02ea0189bfccb2048a4ea38f473cacfb0e5d8` قُرِئَ بجلبِ `refs/pull/2/merge` فكانَ نصُّه `Merge 51fdc3e into b951dca` بفاعلٍ `vooovg-ui` وإزاحةٍ `+0300`: **التزامٌ لم يُنشِئْه هذا العملُ ولا يملكُ إعادةَ إنشائِه**. وساعةُ القراءةِ `2026-09-13T23:28:08Z` (`DISC-062`) | **ثلاثةُ آثارٍ**: (‏1) حكمُ `pull_request` **غيرُ حاسمٍ** لهذَينِ الحرسَينِ: أخضرَ كانَ أو أحمرَ فهو قياسُ عقدةٍ عارضةٍ — وحكمُ حدثِ `push` على العقدةِ المدموجةِ هو الحاسمُ، وهو سنَّةُ `W-159` و`W-160` و`W-161` المقيسةُ لا اختراعُ ههنا؛ (‏2) خُضرةُ `PR #1` سابقًا (‏تشغيلُ 25) **لا تنفي العَطبَ** بل تُثبِتُ عدمَ حَتمِيّتِه: مرجعُ الدمجِ يُولَدُ بإزاحةٍ تتغيّرُ، ومرجعُ `PR #1` زالَ بالدمجِ فلا يُعادُ قياسُه؛ (‏3) **الحرسُ نفسُه لا يُخفَّفُ ولا يُسكَتُ**: هو صادقٌ في مداهُ وخطؤُه في مدى القياسِ لا في الحدِّ | `tooling-gates` — **والمسارانِ مقفولانِ فلا يُمَسّانِ ههنا**: `commit_timestamp_integrity.py` وحرساهُ مُدَّعًى لِـ`WI-035` (‏صارَ `IN_PROGRESS` بحكمِ الجولةِ الخامسةِ) بنصِّ § 6.1؛ و`.github/workflows/ci.yml` تعديلُه **قرارُ مالكٍ لم يُحسَمْ** كما هو مُعلَنٌ في `WI-019` («تعديلُه هو الإصلاحُ نفسُه») فلا يُقرِّرُه منفِّذٌ. **والعلاجُ المقترَحُ لِـ`WI-035`**: يُحصَرُ مدى القياسِ في التزاماتٍ يُنشِئُها المستودعُ (‏ما يبلغُه `refs/heads/*`) ويُستثنى رأسُ `refs/pull/*/merge` بإعلانٍ مكتوبٍ في خرجِ الأداةِ — استثناءُ محلِّ قياسٍ لا استثناءُ حدٍّ. **وحتّى يُحسَمَ ذلكَ**: قراءةُ حكمِ حدثِ `push` على العقدةِ المدموجةِ هي الدليلُ، ويُقيَّدُ حمرةُ `pull_request` كما هي بسببِها المقيسِ ولا تُطوى · **استحقاق: 2026-09-30** | **مفتوح** |
| DISC-060 | P2 | [`.github/workflows/ci.yml`](../../../.github/workflows/ci.yml) — عقدةُ `needs:` بينَ `lint` و`test` و`cross-system-suites` | **حمرةٌ واحدةٌ تُخفي مجموعتَي اختباراتٍ خلفَ حالةٍ لا تُقرأُ حمراءَ ولا خضراءَ.** وظيفتا «Tests» و«Cross-System Suites» تعتمدانِ على `lint`، فحينَ سقطَت `lint` بمخالفتَي `F401` على `377c6ad` رجعَتا **`skipped`** لا `failure`. ومن عدَّ الوظائفَ فوجدَ «حمراءَ اثنتَينِ لا غيرُ» قرأَ الباقيَ خُضرةً — **والمتخطّى ليسَ ناجحًا؛ هو غيرُ مقيسٍ**، وهذا أخطرُ لأنَّه يُقرأُ طُمأنينةً | التشغيلُ **34272407985** · <https://github.com/vooovg-ui/AMOS-Fedration/actions/runs/34272407985> على `377c6ad`: 11 `success` · 2 `failure` (‏Lint · Truth Audit) · **2 `skipped`** (‏Tests · Cross-System Suites) | **كلُّ حكمٍ على CI يُقيَّدُ بثلاثةِ أعدادٍ لا بعددَينِ**: الخضراءُ والحمراءُ **والمتخطّاةُ**، ويُسمّى سببُ التخطّي. ولا يُقالُ «مرَّت الوظائفُ إلّا كذا» ما دامَ فيها متخطًّى | `WI-051` — قُيِّدَ في `W-151`؛ وسببُ التخطّي عولِجَ برفعِ حمرةِ `lint`، **وأمّا قاعدةُ العدِّ الثلاثيِّ فلم تنزلْ بوّابةً بعدُ** — استحقاق: 2026-09-15 | **مفتوح** |
| DISC-059 | P2 | `tests/governance/test_w150_gate_enforcement_in_ci.py` (‏صياغتُه الأولى) | **حارسٌ يُبنى ليقيسَ شجرةً مصطنَعةً فيقيسُ المستودعَ الحقيقيَّ ويمرُّ أخضرَ وهو لا يقيسُ شيئًا.** أوّلُ صياغةٍ للحارسِ أنشأَت مستودعَ `git` مؤقّتًا فيه مسارٌ غيرُ محجوزٍ، ثمَّ استدعَتِ البوّابةَ بعمليّةٍ فرعيّةٍ بـ`cwd=tmp_path` منتظِرةً رمزًا غيرَ صفرٍ. والبوّابةُ **تحسِمُ جذرَ المستودعِ من موضعِ ملفِّها** لا من مجلَّدِ العملِ، فقرأَت `AMOS-Fedration` نفسَه وخرجَت بصفرٍ صادقًا. فلو كُتِبَ الحارسُ بالمنطقِ المعكوسِ (‏«أتوقَّعُ نجاحًا») لمرَّ ولَبَقيَ سنينَ يُقرأُ برهانًا وهو لا يمسُّ موضوعَه | سقوطُ الاختبارِ عندَ أوّلِ تشغيلٍ بـ`assert 0 != 0` ورسالةِ البوّابةِ «✓ كلُّ عملٍ مُسجَّلٌ» — وهي رسالةُ المستودعِ الحقيقيِّ لا الشجرةِ المؤقّتةِ | **العَطبُ مقيسٌ في صياغةٍ لم تُدفَعْ** — كُشِفَ بالتشغيلِ قبلَ الالتزامِ. والدرسُ الباقي: أداةٌ تحسِمُ جذرَها من `__file__` لا تُوجَّهُ بـ`cwd`، ومن أرادَ قياسَها على شجرةٍ أخرى فليستدعِ دالّتَها لا عمليّتَها | `WI-051` — عولِجَ في `W-150` بإعادةِ الصياغةِ: يُحمَّلُ الفاحصُ وحدةً وتُستبدَلُ `changed_paths` بـ`monkeypatch`، فيُقاسُ `PATH_UNCLAIMED` نفسُه لا رمزُ عمليّةٍ | **مُغلَق** (‏`W-150` · 2026-09-08) |
| DISC-056 | **P1** | [`tools/governance/ci_verdict_readability.py`](../../../tools/governance/ci_verdict_readability.py) السطرُ **249** — وواجهةُ `GET /actions/runs/{id}/jobs` | **حكمٌ مقيَّدٌ برقمِ تشغيلٍ يتبدَّلُ بعدَ قيدِه**: الواجهةُ الافتراضيّةُ `/runs/{id}/jobs` تُرجِعُ **آخرَ محاولةٍ وحدَها** لا كلَّ المحاولاتِ، وقارئُ الحكمِ يستدعيها **بلا تثبيتِ محاولةٍ** ولا يكتُبُ في خرجِه **أيَّ محاولةٍ قرأَ**. فإذا أُعيدَ تشغيلُ تشغيلٍ سبقَ أن قُرِئَ أخضرَ مقروءًا، صارَ الاستدعاءُ نفسُه **بالرقمِ نفسِه** يُرجِعُ حِملًا آخرَ — فينقلبُ الحكمُ المقروءُ إلى `UNREADABLE`، والرقمُ المُحالُ إليه في السجلِّ **لم يتغيَّرْ حرفًا**. وحكمُ التشغيلِ الكلّيُّ `conclusion` يُعادُ كتابتُه كذلك فيصيرُ `failure` على شجرةٍ حكمُها المقروءُ أخضرُ. **وهذا غيرُ `DISC-050`**: ذاكَ بوّابةٌ تُغيِّرُ حكمَها على العقدةِ نفسِها، وهذا **قارئُ الحكمِ يقرأُ حِملًا مختلفًا بالرقمِ نفسِه** | قِيسَ في `W-136` على التشغيلِ **94** ([33818260611](https://github.com/xoos-beep/AMOS-Fedration/actions/runs/33818260611)) · عقدةِ `283bf1b`: **المحاولةُ 1** (‏23:40:10Z ← 23:57:37Z يومَ 2026-09-03 · عاملٌ مُسنَدٌ في 13 وظيفةً · خطواتٌ منفَّذةٌ في كلِّها) ⇒ **13/13 `success`** · `ci_verdict_readability.py --from-json` رمزُ خروجٍ **0** · `READABLE · 13/13`. **والمحاولةُ 2** (‏أُعيدَت 2026-09-04T23:49:49Z ← 23:51:04Z · `runner_id` ∈ {0, غيابٌ} · **صفرُ خطوةٍ منفَّذةٍ في 13 وظيفةً**) ⇒ **10 `failure` · 3 `skipped`** · رمزُ خروجٍ **1** · `UNREADABLE · 0/10 · لم يُسنَدْ عاملٌ لـ10`. و`gh api ".../runs/33818260611/jobs?per_page=100" --jq '[.jobs[].run_attempt]\|unique'` ⇒ **`[2]`** — فالمحاولةُ الأولى **لا تُرى** من الواجهةِ الافتراضيّةِ وتُقرأُ من `.../attempts/1/jobs` وحدَها. و`GET /runs/33818260611` ⇒ `run_attempt: 2` · `conclusion: "failure"`. وسببُ سقوطِ المحاولةِ 2 **بنيةٌ لا شِفرةٌ** (‏عينُ `DISC-006` · `RK-011`) | **الدليلُ يفقدُ قابليّةَ الإعادةِ** وهي شرطُه في هذا المشروعِ: قيدٌ يقولُ «تشغيلُ كذا ⇒ 13/13 `READABLE`» يصيرُ غيرَ قابلٍ للتحقُّقِ بإعادةِ تشغيلٍ **لا يملِكُها كاتبُ القيدِ**. وفي الاتّجاهِ المعاكسِ أشدُّ: إعادةُ تشغيلٍ **ناجحةٌ** تستُرُ حمرةً مقيَّدةً فتُمحى من الواجهةِ بلا محوٍ من السجلِّ — وهو ما تُحرِّمُه § 2 حكم 5. **والقيودُ القائمةُ كلُّها تُحيلُ إلى أرقامِ تشغيلٍ بلا رقمِ محاولةٍ**، فكلُّ إحالةٍ منها معرَّضةٌ لهذا التبدُّلِ | `WI-048` (‏قارئُ الحكمِ **يُثبِّتُ رقمَ المحاولةِ** ويكتُبُه في خرجِه، ويُعلِنُ عددَ المحاولاتِ القائمةَ فلا يُقرأُ حكمٌ بلا محاولةٍ مسمّاةٍ · والخطرُ `RK-022`) · **استحقاق: 2026-09-13** | مفتوحٌ — **قِيسَ في `W-136` ولم يُنزَلْ حرسُه**: المسارُ **مُدَّعًى لـ`WI-024`** وحالتُه `IN_REVIEW` فهو مقفولٌ (§ 6.1)، والانتظارُ مُعلَنٌ لا مطويٌّ |
| DISC-054 | P2 | [`docs/audit/CROSS_SYSTEM_SUITE_MATRIX.md`](../../audit/CROSS_SYSTEM_SUITE_MATRIX.md) و[`docs/audit/CROWN_TRUTH_MATRIX.md`](../../audit/CROWN_TRUTH_MATRIX.md) — **الأثرُ المُولَّدُ الذي يشهدُ على شروطِ قياسِه** · ولا قارئَ لشهادتِه | **أثرٌ مُولَّدٌ يشهدُ على نفسِه بالنقصِ ولا يقرؤه حرسٌ**: وثيقةٌ مُولَّدةٌ تكتُبُ في متنِها شروطَ قياسِها (‏«آخرُ توليدٍ» · «بيئةُ PostgreSQL الحقيقيّةُ لحظةَ التوليدِ: **غائبة**» · خليّةُ **FAIL**)، فتُعلِنُ صراحةً أنَّ أرقامَها **قِيسَت في بيئةٍ ناقصةٍ** — ولا يقرأُ تلكَ الشهادةَ أيُّ حرسٍ. فتشغيلُ المُولِّدِ في بيئةٍ ناقصةٍ **يستبدلُ حقيقةً مقيسةً كاملةً بأخرى معطوبةٍ** في شجرةِ المستودعِ، وتمرُّ الوثيقةُ المعطوبةُ على كلِّ بوّابةٍ برمزِ خروجٍ 0. والوثيقةُ نفسُها تقولُ «تحريرُها يدويًّا لا يُغيِّرُ شيئًا في الواقعِ» — وهي محقّةٌ: **الخطرُ ليسَ التحريرَ بل التوليدَ الناقصَ** | **حدَثَ فعلًا في `W-130` لا فرضًا**: تشغيلُ `verify_cross_system_suites.py` بلا وسائطَ ضمنَ جولةِ البوّاباتِ أعادَ توليدَ الوثيقةِ في هذه البيئةِ، فصارَ متنُها `root-core` **2429 نجحَت · 4 ساقطةٌ · 3 أخطاءُ تهيئةٍ ⇒ FAIL** و`services-sqlite` **0/0/0 ⇒ FAIL** و`services-postgres` **0/0/0 ⇒ FAIL** و«بيئةُ PostgreSQL … **غائبة**» بدلًا من المقيسِ الكاملِ المُقيَّدِ في الفرعِ (**1447 · 1264 · 25 كلُّها PASS** و«**مُفعّلة**»). ثمَّ رمزُ خروجِ **كلِّ** بوّاباتِ § 5.4 بقيَ **0** — بما فيها `truth_audit.py . --check` (‏طزاجةُ الأثرِ المُولَّدِ) — ولم تُقَسْ المخالفةُ إلّا بمقارنةِ بايتاتِ الملفِّ بنسخةِ الفرعِ يدويًّا قبلَ الدفعِ. والمقيسُ تركيبيًّا: **وثيقتانِ** تحتَ `docs/` تحملانِ شهادةَ توليدٍ · **صفرٌ** منهما يُعلِنُ نقصًا الآنَ (‏بعدَ استرجاعِ نسخةِ الفرعِ) · و**صفرُ** قارئٍ لتلكَ الشهادةِ في `tools/` أو `tests/` (‏ذِكرُ اسمِ الوثيقةِ في `check_completion_ledger.py` و`check_work_governance.py` **قائمةُ إعفاءٍ** تُخرِجُها من حرسِ التحريرِ اليدويِّ لا قراءةٌ لمتنِها). والرقمُ المُعلَنُ يُقرأُ من هذا السطرِ وحدَه ⇒ `ARTIFACT_WITNESS_BASELINE: witnessing=2 degraded=0 witness_readers=0` | **الإعفاءُ من حرسِ التحريرِ اليدويِّ صارَ إعفاءً من الحرسِ كلِّه**: الوثيقةُ المُولَّدةُ أُخرِجَت من قائمةِ «لا تُحرَّرْ يدويًّا» فلم يبقَ عليها حرسٌ **من جهةِ المُولِّدِ** أيضًا. وذاكَ يقلبُ الحُجّةَ: الوثيقةُ المُولَّدةُ يُفترَضُ أنَّها «لا تستطيعُ أن تكذبَ على القارئِ» — وهي تستطيعُ، متى وُلِّدَت في بيئةٍ لا تملكُ ما تقيسُه، فتشهدُ على نقصِها بلسانِها ولا سامِعَ. وهذا **جنسٌ أوسعُ** من `DISC-053`: هناكَ الخُضرةُ لا تقولُ كم قرأَت، وهنا الأثرُ **يقولُ إنَّه لم يقرأْ** ولا يُصدَّقُ | `WI-046` (‏حرسٌ يقرأُ شهادةَ التوليدِ في كلِّ أثرٍ مُولَّدٍ مُقيَّدٍ في الشجرةِ ويرفضُ المُعلِنَ نقصَه، وعددُ الشاهدينَ **أرضٌ لا تنزلُ** فلا تُحذَفُ الشهادةُ هربًا) · **استحقاق: 2026-09-30** (‏**مُجدَّدٌ في `W-160` بسببٍ مكتوبٍ لا مطويٍّ**، والاستحقاقُ الأوّلُ **2026-09-12** يبقى مكتوبًا ههنا لا يُمحى: مضى ولم يُنجَزْ، والحاجزُ **مقيسٌ لا مُدَّعًى** — وجهتُه `WI-046` (`IN_REVIEW`) — البندُ نفسُه ينتظرُ حكمَ المجلسِ. والتجديدُ **رفعُ حاجزٍ زمنيٍّ لا إنجازٌ ولا إغلاقٌ ولا تخفيفُ حرسٍ**، وأفقُه عينُ أفقِ الحجوزِ القائمِ **2026-09-30** فلا يُخترَعُ أفقٌ ثالثٌ — ومتى بلغَه ولم يُنجَزْ فالحمرةُ حقٌّ تُقرأُ لا تُؤجَّلُ ثالثةً) | مفتوحٌ — **قِيسَ ونُزِلَ حرسُه في `W-131`**: الوثيقةُ المعطوبةُ استُرجِعَت من الفرعِ في `W-130` قبلَ الدفعِ (‏فلم تُدفَعْ)، والباقي أنَّ الحالةَ صارَت **مقيسةً محروسةً** لا معتمدةً على يقظةِ دافعٍ |
| DISC-053 | P2 | [`tools/`](../../../tools) — **كلُّ سطرٍ ناجحٍ يُطلِقُه وجهُ `--check`** · ومنه [`suite_size_inventory.py`](../../../tools/governance/suite_size_inventory.py) و[`truth_audit.py`](../../../tools/governance/truth_audit.py) و[`generator_settlement_closure.py`](../../../tools/governance/generator_settlement_closure.py) (‏والحرسُ داخلٌ في حكمِه) | **خُضرةٌ بلا مقامٍ**: السطرُ الذي تُخرِجُه البوّابةُ عندَ النجاحِ يُعلِنُ حكمًا **كلّيًّا** («كلُّ …» · «لا مخالفةَ» · «مطابقٌ») **ولا يقولُ كم قرأَ**. فقارئُ الخرجِ لا يملكُ سبيلًا لتمييزِ «قِيسَ ألفٌ فلم يُخالِفْ واحدٌ» من «قِيسَ صفرٌ فلم يُخالِفْ شيءٌ» — والحكمانِ يُطبَعانِ بالحرفِ نفسِه ورمزِ الخروجِ نفسِه. وهذا **جنسُ `DISC-046` و`DISC-052`** لا حالتُهما: في كلتَيهما كانَ المقامُ الحقيقيُّ **2** والحكمُ المطبوعُ كلّيًّا، ولم يُكشَفِ الفرقُ إلّا بقراءةِ `--json` يدًا. فالمقامُ ليس زينةَ تقريرٍ: هو **الشرطُ الذي يجعلُ الخُضرةَ قابلةً للتكذيبِ** | قِيسَ في `W-130` تركيبيًّا على الشجرةِ الحقيقيّةِ: في `tools/` **تسعٌ وأربعونَ** وحدةً تُحَلُّ نحوًا (‏والحرسُ الجديدُ منها — يُحصي نفسَه)، تُطلِقُ **ستًّا وثلاثينَ** جملةَ نجاحٍ (‏تحمِلُ «✓»)، منها **عشرونَ** جملةً حكمُها **كلّيٌّ**، ولا يُرفِقُ المقامَ المقيسَ منها إلّا **ثلاثٌ** (`gate_dependency_closure` · `write_domain_readmes` · وجملةُ هذا الحرسِ) — فالباقيةُ **سبعَ عشرةَ** جملةً كلّيّةً **بلا مقامٍ**، ومنها جملةُ `truth_audit` وجملةُ `check_work_governance` وجملةُ `suite_size_inventory` نفسِها. ثمَّ أُعيدَ القياسُ في `W-131` بعدَ نزولِ حرسِ `WI-046`، فصارَ المقيسُ **خمسينَ** وحدةً و**سبعًا وثلاثينَ** جملةَ نجاحٍ منها **إحدى وعشرونَ** كلّيّةً و**أربعٌ** بمقامٍ (‏الحرسُ الجديدُ يُرفِقُ مقامَه فلا يُزادُ الصامتُ) — و**سبعَ عشرةَ** بلا مقامٍ كما كانَت: أرضُ المقامِ ارتفعَت وسقفُ الصامتِ لم يعلُ. والرقمُ المُعلَنُ يُقرأُ من هذا السطرِ وحدَه (‏مصدرُ حقيقةٍ واحدٌ لا ثابتٌ مدفونٌ في فحصٍ) ⇒ `GREEN_DENOMINATOR_BASELINE: tools=51 green_sentences=38 universal=22 bounded=5 unbounded=17` | **بوّابةٌ لا يُعرَفُ مقامُها لا تُكذَّبُ**: إن ضاقَ قارئُها يومًا — بتغيُّرِ صيغةٍ أو مسارٍ أو مُصنِّفٍ — بقيَ حكمُها الكلّيُّ يُطبَعُ ورمزُ خروجِها صفرًا، فيُقرأُ اطمئنانًا وهو صمتٌ. و`DISC-052` مثالٌ حادثٌ لا فرضٌ: ضاقَ المقامُ إلى 2 وبقيَ الحكمُ كلّيًّا سنةً كاملةً من القيودِ | حرسٌ قائمٌ: `tools/governance/green_denominator_closure.py` · `tests/governance/test_w130_green_denominator_closure.py` — يُحصي الجملَ الكلّيّةَ ومقاماتِها ويُسقِطُ إن نما ما لا مقامَ له أو نقصَ المُقامُ · `WI-045` | مفتوحٌ — **مقيسٌ ومحروسٌ من النموِّ لا مُبرَأٌ**: السبعَ عشرةَ تبقى بلا مقامٍ، والمُنجَزُ أنَّ عددَها صارَ مُعلَنًا مقيسًا لا يعلو. وإرفاقُ المقامِ بكلِّ واحدةٍ يمسُّ خمسةَ عشرَ ملفًّا أكثرُها مُدَّعًى لبنودٍ في يدِ المراجعِ (`WI-023`…`WI-043`)، فلا يُمَسُّ في هذا البندِ |

---

## 2 · كيفَ يُقيَّدُ اكتشافٌ جديد

1. **لا تُصلِحْه في مكانِه** إن كان خارجَ نطاقِ بندِك؛ ذاك توسيعُ نطاقٍ صامتٌ يمنعُه § 5.2 من الخارطة.
2. اكتبْ صفًّا هنا بمعرِّفٍ تسلسليٍّ جديدٍ لا يُعادُ استخدامُه.
3. **الدليلُ ليس رأيًا:** رقمٌ، أو مخرجُ أمرٍ، أو رقمُ سطرٍ وملفٍّ، أو قيدٌ سابق.
4. **الوجهةُ إلزاميّة** بحسبِ § 11 من الخارطة: `Q-###` للقرارِ البشريّ · `ADR` للقرارِ المعماريّ · جردُ الأسطحِ للدَّينِ · مدقِّقُ الحقيقةِ للمخالفةِ المقيسة · `RK-###` للخطر · `WI-###` للعملِ القابلِ للتنفيذِ الآن.
5. **الخطورةُ** `P0…P4` كما في § 11؛ و`P0` يوقفُ المسارَ حتى المعالجة.
6. عندَ إغلاقِ الاكتشافِ يُكتَبُ في `الحالة` ما أُغلِقَ به: بندٌ، أو قرارٌ، أو تصميمٌ مُنفَّذ. ولا يُحذَفُ صفٌّ ولا يُعادُ ترقيمُه.
