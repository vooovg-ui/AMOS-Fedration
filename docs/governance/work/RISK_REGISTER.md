# سجلُّ المخاطر — RISK REGISTER

## الهدف: تسميةُ المخاطرِ التي تهدِّدُ الطريقَ **قبلَ** أن تتحوَّلَ إلى تعطُّلٍ أو تكرارِ عملٍ أو خرقٍ للحكم، مع إشارةٍ مبكِّرةٍ لكلِّ خطرٍ ومالكٍ يُسأَلُ عنه وتصرُّفٍ مُعلَنٍ عندَ ظهورِ الإشارة
## النطاق: مخاطرُ **الطريقِ والتنفيذِ والحوكمة** `RK-###`. لا تُسجَّلُ هنا القراراتُ البشريّةُ (تلك `Q-###` في [`SOVEREIGN_DECISION_REGISTER.md`](../../audit/SOVEREIGN_DECISION_REGISTER.md))، ولا المخالفاتُ المقيسةُ آليًّا (تلك في [`TRUTH_MATRIX.md`](../../audit/TRUTH_MATRIX.md))
## المالك: قائدُ التنفيذِ، ومراجعُه المجلس التأسيسي
## تاريخ الإنشاء: 2026-08-25
## تاريخ آخر تعديل: 2026-08-29 (`WI-016` — إشارةُ `RK-003` المبكِّرةُ صارَت رقمًا مقيسًا بحرسٍ يسقُطُ، فلم يبقَ في السجلَّينِ قيدٌ مفتوحٌ بلا مِرساةٍ)

> كلُّ خطرٍ هنا **مأخوذٌ من قياسٍ أو من قيدٍ في المستودع**، لا من تخيُّلِ ما قد يحدث.
> ومصدرُ كلِّ صفٍّ مذكورٌ في عمودِه الأخير. والخطرُ بلا إشارةٍ مبكِّرةٍ قابلةٍ
> للقياسِ ليس خطرًا مُدارًا بل قلقًا مكتوبًا.

---

## 1 · المخاطرُ المفتوحة

| المعرِّف | الخطر | الاحتمال | الأثر | الإشارةُ المبكِّرةُ التي تُقاس | التصرُّفُ عندَ ظهورِها | المالك | المصدر |
|---|---|---|---|---|---|---|---|
| RK-001 | عملٌ يُنفَّذُ مرّتَينِ من جهتَينِ لا تعرفُ إحداهما الأخرى | مرتفع | إهدارُ جهدٍ وتعارضُ دمج | بندٌ نشِطٌ يتقاطعُ في مسارٍ · أو تغييرٌ لمسارٍ غيرِ محجوز | البوّابةُ تُسقِطُ `CLAIM_CONFLICT` / `PATH_UNCLAIMED`؛ يُقسَمُ البندُ أو يُستلَم — **حرسٌ مُثبَتٌ**: `tools/governance/check_work_governance.py` و`tests/governance/test_work_governance_gate.py`؛ قِيسَ في `W-060` أنَّ تعطيلَ فحصِ تقاطعِ المساراتِ يُسقِطُ فحصًا، وتعطيلَ فحصِ المسارِ غيرِ المحجوزِ يُسقِطُ فحصَينِ | قائدُ التنفيذ | قيدُ `W-024`: خارطةٌ أرسلَت عاملًا إلى عملٍ كان مُنجَزًا |
| RK-002 | وثيقةٌ تُعلِنُ إنجازًا لا يقيسُه دليل | مرتفع | قرارٌ يُبنى على رقمٍ غيرِ صادق | تعارضُ وثيقةٍ مع `truth_matrix.json` أو مع تعريفِ الإنجاز | `DISC-###` ثمَّ إصلاحُ الوثيقةِ أو إخضاعُها تاريخيًّا — **حرسٌ مُثبَتٌ لسطحٍ مُعلَنٍ**: `tools/governance/measurement_provenance.py` و`tests/governance/test_w037_measurement_provenance.py`؛ قِيسَ في `W-060` أنَّ تحييدَ فحصِ الطزاجةِ يُسقِطُ فحصَينِ. **حدٌّ مُعلَنٌ**: يحرسُ القياساتَ المنشورةَ تحتَ مجلَّدِ القياساتِ لا كلَّ نصٍّ يُعلِنُ إنجازًا | مالكُ `audit-truth` | `DISC-001` أدناه في [`DISCOVERIES.md`](DISCOVERIES.md) |
| RK-003 | دَينُ الأسطحِ غيرِ السياديّةِ يتجاوزُ قدرةَ المعالجةِ التدريجيّة | مرتفع | تعثُّرُ المسارِ السياديّ كلِّه | اتِّجاهُ عدِّ الأسطحِ في الجردِ صعودًا بينَ قيدَينِ متتاليَينِ — **رقمٌ يُعادُ بأمرٍ واحدٍ**: `python tools/governance/surface_debt_trend.py` | إيقافُ توسيعِ السطحِ وإدخالُ كلِّ سطحٍ جديدٍ بندًا مُقيَّدًا — **حرسٌ مُثبَتٌ بطفرةٍ**: `tools/governance/surface_debt_trend.py` و`tests/governance/test_w061_surface_debt_trend.py`؛ قِيسَ في `W-061` أنَّ تعطيلَ شرطِ الصعودِ يُسقِطُ فحصَينِ، وتصفيرَ العدِّ يُسقِطُ خمسةَ فحوصٍ، وقلبَ فحصِ مطابقةِ الحِملِ المنشورِ يُسقِطُ فحصَينِ، ونسبةَ القيدِ إلى آخرِ التزامٍ مسَّهُ لا أوّلِهِ يُسقِطُ فحصًا، وإلغاءَ حدِّ النقطتَينِ يُسقِطُ فحصًا، وابتلاعَ فشلِ `git` يُسقِطُ فحصًا، وأن تكتبَ الأداةُ سطرًا في الشجرةِ التي تحكمُ عليها يُسقِطُ فحصًا. **حدٌّ مُعلَنٌ**: يقيسُ الاتِّجاهَ بينَ أحدثِ قيدَينِ في `§ 8` افتراضًا (و`--entries N` يوسِّعُ النافذةَ)، ويقيسُ اللقطتَينِ كلتيهما بقاعدةِ الشجرةِ الحاضرةِ لئلّا يُقرأَ فارقُ أداتَينِ فارقَ دَينٍ | مالكُ `audit-truth` | `REMAINING_SURFACES_INVENTORY.md` |
| RK-004 | قرارٌ بشريٌّ متأخِّرٌ يحجبُ سلسلةَ أعمال | مرتفع | توقُّفُ مسارٍ كاملٍ بلا مالكِ جواب | عددُ `Q-###` المفتوحةِ ومدّةُ انتظارِها · وعددُ بنودِ `BLOCKED` | رفعُ الأسئلةِ المحجوبةِ إلى المالكِ دفعةً واحدةً بأثرِ كلٍّ منها | المجلس التأسيسي | `SOVEREIGN_DECISION_REGISTER.md` |
| RK-005 | بوّابةُ CI حمراءُ تُطبَّعُ فتفقدُ البوّاباتُ معناها | متوسط | حكمٌ آليٌّ يُتجاهَل | وظيفةٌ تبقى حمراءَ بينَ قيدَينِ متتاليَينِ بلا قيدِ سببٍ ومالكٍ | إمّا إصلاحُها أو تجميدُها بقرارٍ مُقيَّدٍ يذكرُ سببَها ومالكَها — **موقوفٌ على المالكِ**: قياسُ «تبقى حمراءَ بينَ قيدَينِ» يقتضي حكمًا مقروءًا، وأحكامُ CI **غيرُ مقروءةٍ** (`DISC-006`) وإصلاحُ ذلك بيدِ المالكِ — فلا استحقاقَ يُخترَعُ ولا حرسَ يُدَّعى | مالكُ `ci-pipeline` | وظائفُ الأمنِ الحمراءُ المُقيَّدةُ بانتظارِ إذنِ المالك |
| RK-006 | إصلاحٌ يُدفَعُ ولا يُشاهَدُ حكمُ CI عليه فيُحسَبُ مُثبَتًا | متوسط | ادّعاءُ إنجازٍ بلا دليل | قيدٌ يذكرُ إصلاحًا بلا رقمِ تشغيلٍ ورابطِه | لا يُغلَقُ البندُ قبلَ مشاهدةِ الحكم (§ 7 البند 6) — **موقوفٌ على المالكِ**: «مشاهدةُ الحكمِ» شرطٌ لا يُقاسُ ما دامَت الأحكامُ غيرَ مقروءةٍ (`DISC-006`)، وإلزامُ القيدِ برقمِ تشغيلٍ اليومَ يُجرِّمُ كلَّ قيدٍ — فالتفعيلُ بيدِ المالكِ | قائدُ التنفيذ | سابقةُ `W-040` |
| RK-007 | تعدُّدُ الخرائطِ والترقيماتِ يُنتِجُ مصادرَ حقيقةٍ متنافسة | متوسط | تشتُّتُ العملِ وتناقضُ الحالة | ظهورُ ملفٍّ جديدٍ يُعلِنُ ترتيبًا أو حالةً يملكُها ملفٌّ قائم | ردُّه إلى مالكِه في § 3 من الخارطةِ أو رفضُه — **حرسٌ مُثبَتٌ**: `tools/governance/check_root_file_names.py` و`tests/governance/test_w042_root_name_guard.py` — إعلانٌ مُغلَقٌ: كلُّ ملفٍّ جذريٍّ غيرِ مُعلَنٍ مخالفةٌ؛ قِيسَ في `W-060` أنَّ إفسادَ رمزِ المخالفةِ يُسقِطُ فحصًا. **حدٌّ مُعلَنٌ**: يحرسُ الجذرَ لا ما تحتَ `docs/` | مالكُ `governance-docs` | `THE_ROADMAP § 15.2` |
| RK-008 | حجزٌ يُترَكُ مفتوحًا فيُعطَّلُ نطاقٌ بلا عمل | متوسط | جمودٌ صامت | `RESERVED`/`IN_PROGRESS` بعدَ تاريخِ انتهاءِ الحجز | البوّابةُ تُسقِطُ `RESERVATION_EXPIRED` ويُعادُ النطاقُ متاحًا — **حرسٌ مُثبَتٌ**: `tools/governance/check_work_governance.py` و`tests/governance/test_work_governance_gate.py`؛ قِيسَ في `W-060` أنَّ تعطيلَ شرطِ انتهاءِ الحجزِ يُسقِطُ فحصًا | قائدُ التنفيذ | `THE_ROADMAP § 6.3` |
| RK-009 | نطاقاتٌ بلا مالكٍ معيَّنٍ تُبقي المساءلةَ معطَّلة | مرتفع | خرقٌ بلا مسؤول | صفوفُ `OWNERSHIP.md` بمالكٍ `— (غيرُ معيَّن)` | حسمُ القرارِ `A-2` في § 16 من الخارطة | المجلس التأسيسي | [`OWNERSHIP.md`](OWNERSHIP.md) |
| RK-010 | إضافةُ تبعيّةٍ لبوّابةٍ لا تُثبَّتُ في بيئةِ CI فتسقُطُ البوّابةُ لسببٍ غيرِ حقيقيّ | منخفض | ثقةٌ زائفةٌ أو حمرةٌ كاذبة | بوّابةٌ تستوردُ حزمةً غيرَ مُعلَنةٍ في ملفِّ تبعيّاتِ البيئةِ التي تُشغِّلُها | كلُّ بوّابةٍ بالمكتبةِ القياسيّةِ وحدَها، أو تُعلَنُ تبعيّتُها في ملفِّها — **ويُقاسُ هذا آليًّا لا يُصدَّقُ نصًّا**: `python tools/governance/gate_dependency_closure.py . --check` (‏`W-053`)، فما كانَ تخفيفًا مكتوبًا صارَ حرسًا له رمزُ خروجٍ. **وقد كشفَ الحرسُ أنَّ الخطرَ متحقِّقٌ لا مفترَضٌ**: `sovereignty-kernel` تستوردُ `amos-federation` ولا تُثبِّتُها (`DISC-009` · `RK-013`)، فخطورةُ هذا الصفِّ «منخفض» **قِيسَت أعلى ممّا كُتِبَ** — ورفعُ التصنيفِ حكمٌ يخصُّ مالكَ السجلِّ لا مُنفِّذًا | مالكُ `dependencies` | `DISC-002` · و`DISC-009` في [`DISCOVERIES.md`](DISCOVERIES.md) |
| RK-011 | **بنيةُ CI تسقُطُ قبلَ تنفيذِ خطوةٍ فيُظَنُّ العطبُ في الشِفرةِ** — أو الأسوأُ: يُدفَعُ عملٌ بلا شاهدٍ ويُقالُ «حكمُ CI أحمرُ» كأنَّه حكمُ شجرةٍ | مرتفع | **حكمٌ غيرُ مقروءٍ يُقرأُ حكمًا** · وسقوطُ الشاهدِ الأقوى الذي تُحيلُ إليه حدودُ بيئةِ العاملِ | وظيفةٌ تسقُطُ في ثوانٍ و`steps=[]` و`runner_id=0` في `GET /actions/runs/{id}/jobs` — **لا سجلَّ خطوةٍ يُقرأُ** | **يُقاسُ بالواجهةِ لا بالعينِ**: يُطلَبُ `jobs` ويُعَدُّ `steps`؛ فإن كان صفرًا **يُعلَنُ الحكمُ «غيرَ مقروءٍ»** ولا يُنسَبُ إلى الشجرةِ، ويُطلَبُ إذنُ المالكِ في حسابِ `Actions`. و**التخفيفُ صارَ شِفرةً محروسةً لا وصفًا** في `W-051`: `tools/governance/ci_verdict_readability.py` يُصنِّفُ الوظائفَ بالخطواتِ ويُخرِجُ حكمًا برمزِ خروجٍ، ولا يُقتَبَسُ `conclusion` إلّا مع `READABLE`؛ ويحرسُ **معيارَه** في كلِّ دفعةٍ `tests/governance/test_w051_ci_verdict_readability.py` (24 فحصًا). والحدُّ مُعلَنٌ: حرسُ معيارٍ لا شاهدٌ — لا يُنشِئُ تشغيلًا يُنفَّذُ ولا يُغلِقُ الخطرَ | **المالك** (‏فاتورةٌ وحدُّ إنفاقٍ — لا سلطانَ لعاملٍ عليها) | الاكتشافُ `DISC-006` · قيدُ `W-049` · حرسُ `W-051` (‏البندُ `WI-006`) |
| RK-012 | **مساراتٌ فيها شِفرةٌ تعملُ ولا نطاقَ مُسجَّلًا لها** — فتُبنى بلا مالكٍ ولا مراجعٍ، أو يُرتجَلُ لها تسجيلٌ عندَ أوّلِ دفعةٍ فيصيرُ حدُّ النطاقِ أثرًا جانبيًّا لعجَلةِ عاملٍ | مرتفع | حدودُ نطاقٍ تُرسَمُ بالضرورةِ لا بالقرارِ · وعملٌ لازمٌ يُترَكُ لأنَّ بابَه إداريٌّ لا فنّيّ | مجلَّدٌ فيه `*.py` ولا يُطابِقُ مسارَه صفٌّ في `OWNERSHIP.md § 1` · أو صفٌّ مُسجَّلٌ لمسارٍ فارغٍ من الشِفرةِ (`tools/dev` اليومَ) | **يُقاسُ لا يُقدَّرُ**: تُعَدُّ مجلَّداتُ `tools/` وملفّاتُها وتُقابَلُ بجدولِ النطاقاتِ؛ فما وُجِدَ بلا نطاقٍ **يُقيَّدُ اكتشافًا ويُرفَعُ للمالكِ** ولا يُسجَّلُ بيدِ منفِّذٍ إلّا حينَ تأمرُ القاعدةُ 3 بتسجيلِ مسارِ عملِه بعينِه، فيُلحَقُ بأقربِ نطاقٍ جنسًا ويُعلَنُ أنَّ للمالكِ نقضَه | **المالك** (‏تعيينُ حدودِ نطاقٍ ومالكٍ — `A-2` معلَّقٌ) | الاكتشافُ `DISC-008` · قيدُ `W-052` (‏البندُ `WI-007`) |
| RK-013 | **حكمُ بوّابةٍ يُقرأُ وهو غيرُ صالحٍ للقراءةِ لأنَّ وظيفتَها لا تُثبِّتُ ما تستوردُه** — فالأحمرُ يُنسَبُ إلى الشِفرةِ وهو من البيئةِ، أو يُعتادُ الأحمرُ فيُبلَعُ سقوطٌ حقيقيٌّ بينَه | **مرتفع** (‏متحقِّقٌ لا مفترَضٌ: مرّتانِ مقيستانِ) | ثقةٌ زائفةٌ في قدرةٍ لم تُقَسْ · أو ترْكُ عطبٍ حقيقيٍّ في زحامِ حُمرةٍ كاذبةٍ | وظيفةٌ في `ci.yml` تُشغِّلُ مدخلَ بايثونَ يبلُغُ استيرادًا غيرَ محروسٍ لتوزيعٍ لا تُثبِّتُه — يُقاسُ آليًّا لا بالعينِ | **حرسٌ لا فقرةٌ**: `python tools/governance/gate_dependency_closure.py . --check` — يُقابِلُ ما تُثبِّتُه كلُّ وظيفةٍ بما تستوردُه بوّاباتُها عبورًا (`ast`)، ويستثني الاستيرادَ المحروسَ (`try/except ImportError|Exception`) لئلّا يُحمِّرَ بلا سببٍ، **ويرفضُ برمزِ 2** حينَ لا يُصنَّفُ اسمُ توزيعٍ. والعطبُ القائمُ مُقيَّدٌ في `KNOWN_OPEN` بمُوجِّهٍ (`DISC-009`) لا مطويٌّ، وتقادُمُ الأساسِ يُسقِطُ الأداةَ | **المالك** (‏اختيارُ حلٍّ لـ`DISC-009` قرارٌ سياديٌّ) | الاكتشافُ `DISC-009` · قيدُ `W-053` (‏البندُ `WI-008`) · وأصلُه الخطرُ `RK-010` |
| RK-014 | **حرسٌ يقيسُ رابطًا يكتبُه من يحرسُه** — فيصمُتُ بعينِ الحالةِ التي أُنشِئَ لها، ويُقرأُ ضمانًا وهو ليسَ ضمانًا. وأخطرُ من غيابِ الحرسِ حرسٌ يُطمئِنُ | **مرتفع** (‏متحقِّقٌ لا مفترَضٌ: `DISC-010`) | واجبٌ منصوصٌ يمرُّ بلا صوتٍ، وبوّابةٌ خضراءُ تُستشهَدُ بها على انتظامٍ لم يحدُث | حرسٌ مدخلُه حقلٌ يكتبُه الطرفُ المحروسُ (خليّةٌ · وسمٌ · إقرارٌ في نصٍّ) ولا يقرأُ شاهدًا خارجَ يدِه | **قياسٌ من خارجِ يدِ المحروسِ**: يُقرأُ سجلُّ الإكمالِ من `origin/main` لا من الفرعِ، ويُقاسُ الاتِّجاهُ المعاكسُ (قيدٌ ← بند) — `tools/governance/check_work_governance.py` (`ledger_reverse_links`) وحرسُه `tests/governance/test_w054_post_merge_reverse_link.py` (‏11 فحصًا، ومنها إثباتُ سقوطِ الفحصِ عندَ إضعافِ الحرس) | `tools/governance` — ديوان التدقيق | `DISC-010` · `W-054` |
| RK-015 | **قاعدتانِ منصوصتانِ تتناقضانِ فتُنتِجانِ واجبًا لا يُستطاعُ أداؤه** — فيُخرَقُ إحداهما بالضرورةِ، ويصيرُ الخرقُ عادةً مكتوبةً بحُجّةِ «الطريقُ مسدود» | مرتفع | بنودٌ مدموجةٌ تبقى مفتوحةً إلى الأبدِ · أو إغلاقٌ بانتحالِ صفةِ مراجعٍ · أو تعديلُ الخارطةِ لتوافقَ عملًا خُرِقَت فيه (‏ما نهى عنه § 16.3) | واجبٌ في § 7 لا يُقابِلُه انتقالٌ مسموحٌ في § 4.3 · أو حالةٌ يضعُها دورٌ لم يُعَيَّنْ في § 16 | **لا يُحَلُّ بيدِ منفِّذٍ**: يُقيَّدُ اكتشافًا (`DISC-012`) ويُرفَعُ للمالكِ قرارًا؛ ويبقى الحرسُ **إبلاغًا** لا إسقاطًا حتّى يُعتمَدَ `A-2`/`A-3` — فلا صمتٌ ولا عقوبةٌ على ما لا يُستطاع (§ 13.3 · § 16.4) | المالك (`zoorooz`) — قرارُ `A-2` | `DISC-012` · `W-054` |
| RK-016 | **قفلُ النطاقِ على سجلٍّ مركزيٍّ يُجمِّدُ أعمالًا مشروعةً، فيتراكمُ دَينُ توثيقٍ لا يُرى** — `measurement_provenance.py` محجوزٌ لبندٍ `IN_REVIEW` لا يُغلَقُ إلّا بقرارٍ سياديٍّ معلَّقٍ، وكلُّ عملٍ يُنتِجُ قياسًا يحتاجُ قيدًا فيه | **مرتفع** — تحقَّقَ **ثلاثَ مرّاتٍ** بالفعلِ لا بالتوقُّعِ | **متوسّط** — لا كذبَ يُنشَرُ، لكنَّ القياسَ الحيَّ **يُفقَدُ** لأنَّه لا يُعادُ توليدُه بأمرٍ، والحدُّ يُكتَبُ في قيودٍ متفرِّقةٍ لا في موضعٍ واحدٍ | عدُّ `DISC-` المفتوحةِ التي عِلّتُها «مسارٌ محجوزٌ»: بلغَ **3** (`DISC-007` · `DISC-011` · `DISC-014`) | يُقاسُ ويُعلَنُ ولا يُخرَقُ القفلُ: حقولُ كلِّ قيدٍ مؤجَّلٍ تُكتَبُ في سجلِّ الإكمالِ فيُوضَعُ بلا اجتهادٍ · والفَكُّ بقرارِ `A-2` أو بإخراجِ سجلِّ النَّسَبِ من القفلِ | المالك (`A-2`) | `DISC-014` · `W-055` |
| RK-017 | **حالةٌ سياديّةٌ يُبنى عليها الحجبُ والإفراجُ ولا يقرؤها شيءٌ** — فتُدارُ بالذاكرةِ: بندٌ يبقى مجمَّدًا بعدَ اعتمادِ قرارِه، أو يُفرَجُ عنه قبلَه، وكلاهما يُقرأُ نظامًا. | مرتفع (وقعَ فعلًا: `DISC-016`) | تجميدٌ زائدٌ أو إفراجٌ سابقٌ لأوانِه، وكلاهما بلا أثرٍ يُقاس | صفٌّ في § 16 بحالةٍ غيرِ معجميّةٍ · اعتمادٌ بلا تاريخٍ · إشارةٌ إلى `A-#` لا صفَّ له · وعدُ تصعيدٍ في تعليقٍ لا في شِفرة | `sovereign_decision_status.py` يقيسُ الجدولَ في كلِّ تشغيلٍ ويُسقِطُ على الأربعةِ الأولى ويُبلِغُ بالخامسِ · والربطُ بالبوّابةِ مؤجَّلٌ إلى `WI-009` لأنَّه عينُ `A-3` | المالك (لحسمِ `A-2`/`A-3` ونصِّ § 16) · قائدُ التنفيذِ (للربطِ بعدَه) | `W-056` · `DISC-016` · `DISC-017` |
| RK-018 | حرسُ وثائقِ الحالةِ يقرأُ **قائمةً مُعلَنةً في الشِفرةِ** لا اكتشافًا: وثيقةٌ جديدةٌ تُعلِنُ حالةَ المشروعِ ولا تُدرَجُ في `STATE_DOCUMENTS` تتقادَمُ ولا يراها أحدٌ | متوسّطٌ | متوسّطٌ — عطبُ `DISC-013` نفسُه يعودُ في ملفٍّ آخرَ | مراجعةٌ تجدُ ملفًّا يذكرُ «Last Completed Work» أو «حالةُ المشروعِ» وليس في القائمةِ | القائمةُ مكتوبةٌ في موضعٍ واحدٍ مُوثَّقٍ حدًّا مُعلَنًا، وتُوسَّعُ بقيدٍ عندَ كلِّ وثيقةٍ جديدةٍ؛ والاكتشافُ التلقائيُّ يحتاجُ تعريفًا مُعلَنًا لـ«وثيقةِ حالةٍ» وهو حسمٌ لا اجتهادُ منفِّذٍ — **حرسٌ مُثبَتٌ للنصفِ المقيسِ**: `tools/governance/state_document_drift.py` و`tests/governance/test_w057_state_document_drift.py`؛ قِيسَ في `W-060` أنَّ إفسادَ رمزِ التقادُمِ يُسقِطُ خمسةَ فحوصٍ. **والنصفُ الآخرُ** — تعريفُ «وثيقةِ حالةٍ» لاكتشافٍ تلقائيٍّ — حسمٌ سياديٌّ لا اجتهادُ منفِّذٍ | Driving H | `W-057` · `WI-012` |
| RK-019 | **قيدٌ مفتوحٌ بلا مِرساةٍ هو الحالُ الغالِبُ لا الشاذُّ** — قاعدةُ `DISC-018` (لكلِّ قيدٍ حرسٌ أو تاريخُ استحقاقٍ) صارَ لها عدَّادٌ، والعدَّادُ يُعلِنُ اثني عشرَ صفًّا بلا مِرساةٍ وصِفرَ تاريخِ استحقاقٍ في السجلَّينِ كلَّيهما | مرتفعٌ — مقيسٌ لا متوقَّعٌ (`DISC-020`) | متوسّطٌ — لا كذبَ يُنشَرُ، لكنَّ العَطبَ المُقيَّدَ ينمو بينَ القيدِ والإصلاحِ كما نمَا في `DISC-013` | `python tools/governance/open_record_accountability.py` يصعدُ عددُ `OPEN_RECORD_WITHOUT_ANCHOR` فيه، أو يظهرُ `DEAD_ANCHOR` لحرسٍ حُذِفَ | يُقاسُ في كلِّ تشغيلٍ بالأداةِ نفسِها وحرسُها `tests/governance/test_w059_open_record_accountability.py`؛ وقد أُرسِيَ الصفُّ الأخيرُ في `W-061` فصارَ العدَّادُ يُعلِنُ **صِفرَ صفوفٍ بلا مِرساةٍ**؛ وصدقُ المِرساةِ نفسِه صارَ مقيسًا في `W-062` بـ`python tools/governance/mutation_probe.py` (دعاوى الحرسِ تُعادُ عَطبًا عَطبًا ويُقاسُ الساقطُ). ويبقى الصفُّ **مفتوحًا** لأنَّ المُصنِّفَ يمكنُ أن يُخدَعَ بصياغةٍ (`DISC-021`) ولأنَّ المُسجَّلَ من الدعاوى قيدانِ لا كلُّها، ولا يُخترَعُ استحقاقٌ لما هو موقوفٌ على `A-2`/`A-3` | Driving H | `W-059` · `WI-014` · `DISC-020` |

---

## 2 · قواعدُ إدارةِ الخطر

1. **لا خطرَ بلا إشارةٍ تُقاس.** «قد يتعطَّلُ النظام» ليس خطرًا مُدارًا؛ الإشارةُ رقمٌ أو حالةُ بوّابةٍ يُمكنُ رؤيتُها.
2. **لا خطرَ بلا مالك.** والمالكُ نطاقٌ من [`OWNERSHIP.md`](OWNERSHIP.md) لا شخصٌ مجهول.
3. **الخطرُ الذي وقعَ ليس خطرًا؛ صارَ حالةً.** يُنقَلُ إلى بندِ عملٍ أو قيدٍ أو `Q-###`، ويُقيَّدُ في هذا السجلِّ أنَّه تحقَّق.
4. **مستوياتُ الخطورةِ** `P0…P4` كما في [`THE_ROADMAP.md § 11`](THE_ROADMAP.md)، و`P0` يوقفُ المسار.
5. **يُراجَعُ هذا السجلُّ بعدَ كلِّ دمج** (§ 7 البند 5)، ويُقيَّدُ ما جدَّ أو تغيَّر.
