# حالة المشروع — AMOS-Federation

<!--
الهدف: مرجعٌ سريعٌ لحالة المشروع بين الوكلاء
النطاق: جميع مراحل المشروع ومؤشراته
المالك: ديوان التدقيق
تاريخ الإنشاء: 2026-08-18
تاريخ آخر تعديل: 2026-09-08 (W-155 — حكمُ CI على عقدةِ W-154 قُرِئَ أخضرَ 14/14 وقُيِّدَ برقمِه ورابطِه · والجولةُ الرابعةُ من المراجعةِ المعزولةِ مُعلَنةٌ موقوفةً لنفادِ رصيدِ النماذجِ)
tags: project-state, handoff, quick-reference, governance-active
-->

> **المرجع السريع للوكيل القادم:** الخارطة الحاكمة للعمل هي [`docs/governance/work/THE_ROADMAP.md`](docs/governance/work/THE_ROADMAP.md)
> وسجل العمل المفتوح [`docs/governance/work/ACTIVE_WORK.md`](docs/governance/work/ACTIVE_WORK.md). وسجل الإنجاز التاريخي في
> [`docs/audit/COMPLETION_LEDGER.md`](docs/audit/COMPLETION_LEDGER.md). **لا تبدأ عملاً دون تسجيل بند وحجز مساراته.**

## الهوية والحالة

| الحقل | القيمة |
|---|---|
| المشروع | AMOS-Federation — النواة السيادية لدولة فدرالية |
| المستودع | `https://github.com/xoos-beep/AMOS-Fedration` |
| الفرع | `main` |
| **Current Stage** | المسارات الحاكمة (T0: قابليّة القياس · T2: الحوكمة والقرارات السيادية · T4: الطبقة الحقيقية) |
| **Last Completed Work** | **W-155 · 2026-09-08**: حكمُ CI على عقدةِ `W-154` قُرِئَ **أخضرَ 14/14** بالعدِّ الثلاثيِّ (0 حمراءَ · 0 متخطّاةٍ) وقُيِّدَ برقمِه ورابطِه (‏التشغيلُ 34301063337 · ومصفوفةُ الحقيقةِ 34301063334)، **والجولةُ الرابعةُ من المراجعةِ المعزولةِ لم تُشغَّلْ**: الحاويتانِ مُهيَّأتانِ والتكليفُ أعمى، لكنَّ المراجعَينِ توقَّفا لنفادِ رصيدِ النماذجِ — فـ`WI-051` يبقى `IN_REVIEW` ومعيارُه 6 غيرُ مُستوفًى |
| **W-143 · 2026-09-07**: اعتمادُ WI-049 وWI-050 `VERIFIED` تحتَ A-2. التفاصيلُ في القيدِ `W-143`.
| **W-142 · 2026-09-07**: أوّلُ CI أخضرُ بالكاملِ 13/13 على المستودعِ الجديد. التفاصيلُ في القيدِ `W-142`.
| **W-141 · 2026-09-07**: مزامنةُ وثائقِ الحالةِ مع رأسِ السجلِّ. التفاصيلُ في القيدِ `W-141`.
| **W-140 · 2026-09-07**: تفعيلُ A-3 + إصلاحُ إخفاقاتِ Identity Law + مزامنةُ وثائقِ الحالةِ. التفاصيلُ في القيدِ `W-140`.
| **W-139 · 2026-09-07**: أوّلُ تشغيلٍ لـ CI على المستودعِ الجديدِ `vooovg-ui/AMOS-Fedration` — الحكمُ مقروءٌ (13/13) و`DISC-006` لا ينطبقُ على هذا الحسابِ. التفاصيلُ في القيدِ `W-139` من `COMPLETION_LEDGER.md`.
| **W-138 · 2026-09-06**: **السببُ الجذريُّ لعودةِ `DISC-006` **السببُ الجذريُّ لعودةِ `DISC-006` صارَ مقيسًا من حسابِ المالكِ لا مُستنتَجًا من أثرِه**: النقطةُ المهجورةُ `.../billing/actions` تُرجِعُ **410** فأُعلِنَ في `W-137` أنَّ العلّةَ «لا تُعرَفُ عيانًا» — وجُرِّبَت النقطةُ الحاليّةُ `.../billing/usage` فرجعَت **200**. **فأيلولُ 2026 = 2674 دقيقةَ `Actions Linux`** ‏763 + 716 + 1195 مقابلَ مُعلَنٍ منشورٍ **2000** لطبقةِ `Free`، وعبورُ الحدِّ **خلالَ 2026-09-03** — وهو عينُ حدِّ العَطبِ الزمنيِّ. ولا صفَّ دقائقَ يومَي 09-04 و09-05 وصفوفُ الخزنِ مستمرّةٌ، و`netAmount = 0.0` في كلِّ صفٍّ. ونُفِيَ البديلانِ: الصلاحيةُ مفتوحةٌ و`total_count: 0` للعواملِ الذاتيّةِ. **فالحاجزُ حصةٌ لا شِفرةٌ**، ويدُه في حسابِ المالكِ كما كانَ: `DISC-006` باقٍ **مفتوحًا** و`RK-011` **مرتفعًا** وشرطُ § 4.3 غيرُ قابلٍ للاستيفاءِ — لم تُرقَّ حالةٌ ولا كُتِبَ `VERIFIED` ولا `PROVEN`. **وحكمُ عقدةِ `97105c8` قُرِئَ**: التشغيلُ **96** ⇒ `UNREADABLE · 0/10` والتشغيلُ **97** ⇒ `failure` — مُقيَّدٌ مجموعًا بحدِّ التسلسلِ المُعلَنِ. و`DISC-057` قُيِّدَ: حدُّ صدقٍ أُعلِنَ عن نقطةٍ مهجورةٍ بلا تجريبِ بديلِها. **وتاريخُ التصفيرِ 2026-10-01 توقُّعٌ لا حكمٌ** |
| **Governing Roadmap** | [`docs/governance/work/THE_ROADMAP.md`](docs/governance/work/THE_ROADMAP.md) (الحالة: `ACTIVE` نافذة) |
| **Active Work Register** | [`docs/governance/work/ACTIVE_WORK.md`](docs/governance/work/ACTIVE_WORK.md) (حجز النطاقات ومنع التكرار) |
| **Central Ledger** | [`docs/audit/COMPLETION_LEDGER.md`](docs/audit/COMPLETION_LEDGER.md) (سجل W-000 إلى W-106 — 107 صفًّا مقروءًا بالأداةِ) |
| **Truth Matrix Status** | **63 مخالفة مقيسة وثابتة بالترباس** (60 مخزن ذاكرة · 2 حقيقة مضمنة · 1 عزل معطل) |

## ما بينَ W-048 وW-065 — مُلخَّصٌ مُحالٌ إلى § 8 لا حقيقةٌ جديدة

**هذا القسمُ إحالةٌ**: كلُّ سطرٍ فيه صفٌّ في
[`COMPLETION_LEDGER.md § 8`](docs/audit/COMPLETION_LEDGER.md)، ومن أرادَ التفصيلَ
قرأَه هناك لا هنا.

| القيد | ما صارَ مقيسًا بعدَ أن كانَ مكتوبًا |
|---|---|
| W-048 | الرباطُ بينَ الحرسِ والقياسِ يُقاسُ ولا يُصدَّقُ (وضعُ `bound`) |
| W-049 · W-050 | قيدُ ما بعدَ الدمجِ · ودمجٌ بإذنِ المالكِ صريحًا **بلا شاهدٍ من CI** |
| W-051 | مقروئيّةُ حكمِ CI صارت أداةً محروسةً لا حكمًا بالنظر |
| W-052 | صدقُ قاعدةِ القياسِ نفسِها — مِسبرُ بصماتِ السجلِّ |
| W-053 | تخفيفٌ مكتوبٌ صارَ حرسًا — تبعيّاتُ بوّاباتِ CI |
| W-054 | حرسٌ كانَ أعمى عن حالتِه صارَ يُعلِنُها |
| W-055 | الجردُ المُعلَنُ في `ARCHITECTURE.md` صارَ رقمًا يُقاسُ (T0.6) |
| W-056 | حالةُ القراراتِ السياديّةِ في § 16 صارت رقمًا: `A-1` معتمدٌ · **`A-2` و`A-3` معلَّقانِ** |
| W-057 | موضعُ هذه الوثيقةِ ودليلِ المشروعِ من السجلِّ صارَ مقيسًا ومُسقِطًا عندَ التأخُّرِ |
| W-058 | مِسبرُ الطفراتِ: دعوى «الفحوصُ تكشِفُ» صارت مقيسةً لا مُقدَّرةً |
| W-059 · W-060 | مساءلةُ القيدِ المفتوحِ صارت عدَّادًا · ونسبُ القياسِ إلى مصدرِه صارَ محروسًا |
| W-061 · W-062 · W-063 | اتِّساعُ الدَّينِ صارَ خطًّا يُقاسُ · وحدودٌ مكتوبةٌ صارت حرسًا |
| W-064 | حكمُ CI صارَ مقروءًا (`READABLE` · 13/13) فقُرِئَت حمرتُه وأُصلِحَ سببُها بلا تخفيفٍ |
| W-065 | `A-2` قُيِّدَ بحدِّه فمرَّ أربعةَ عشرَ بندًا إلى `VERIFIED` — **والاستقلالُ منتفٍ ومُسجَّلٌ** |
| W-066 | حكمُ CI على دفعةِ `W-065` قُرِئَ **أحمرَ** بسببٍ واحدٍ في القياسِ (`DISC-033`) فأُصلِحَ لفظُه بلا تخفيفِ حرسٍ |
| W-067 | واجبُ § 7 وُفِّيَ: أربعةَ عشرَ بندًا `CLOSED` بحكمِ CI على عقدِها كما كانَ · `--enforce-post-merge` ⇒ **0** |
| W-068 | `WI-020` أُغلِقَ بحكمِ CI مقروءًا · لا بندَ باقٍ في `IN_REVIEW` أو `VERIFIED` |
| W-069 | ماسحٌ واحدٌ مرجعيٌّ لبوّابةِ السيادةِ 6 و`--repo-root` صريحٌ (‏استُنقِذا من فرعٍ لم يُدمَجْ · `DISC-034`) · وحالةُ البندِ لا تُكتَبُ مرّتَينِ متناقضتَينِ (`STATUS_CONTRADICTION` · `DISC-035`) · و`T0.4ب` أُنجِزَت بقياسٍ مُشاهَدٍ |
| W-070 | حكمُ CI على عقدةِ `W-069` قُرِئَ **أخضرَ 13/13** (تشغيلُ 33342938099 · `READABLE`) فنُقِلَ `WI-021` إلى `VERIFIED` — والإغلاقُ في الالتزامِ التاليِ |
| W-071 | `WI-021` أُغلِقَ بحكمٍ مقروءٍ ثانٍ (13/13 على `0ce4d5d`) · مساراتُه الثمانيةُ انفكَّت · لا بندَ باقٍ في `IN_REVIEW` أو `VERIFIED` |
| W-072 | إعادةُ قياسِ `origin/develop`: **خمسةُ التزاماتٍ** لا واحدٌ · و`DISC-036` تصادمُ ثلاثةِ معرِّفاتِ قيدٍ بينَ الفرعَينِ · والقرارُ **بيدِ المالكِ** بثلاثةِ خياراتٍ |
| W-073 | حكمُ CI على عقدةِ `W-072` قُرِئَ **أخضرَ 13/13** (تشغيلُ 33348727892) فصارَ `WI-022` في المراجعةِ |
| W-074 | حكمٌ مقروءٌ ثانٍ 13/13 على `afae5e0` (تشغيلُ 33350334816) فصارَ `WI-022` **مُراجَعًا** — والإغلاقُ في الالتزامِ التاليِ |
| W-075 | `WI-022` أُغلِقَ بحكمٍ مقروءٍ ثالثٍ (13/13 على `2a5c1f6`) · ولا بندَ مفتوحٌ · والباقي **قراراتُ مالكٍ** |
| W-076 | دعوى «يُشغَّلُ في كلِّ دفعةٍ» قِيسَت: **1 من 4 بلا إنفاذٍ** و**17** أداةً بلا خطوةِ تشغيلٍ ⇒ `DISC-037` و`WI-023` محجوزًا |
| W-077 | الحرسُ نافذٌ (19 فحصًا) وأربعُ أدواتٍ صارت خطواتِ تشغيلٍ · وحكمُ عقدةِ `W-076` قُرِئَ **أحمرَ 11/13** فأُصلِحَ من سببِه (`DEAD_ANCHOR`) |
| W-078 | حكمُ عقدةِ `4764a12` قُرِئَ **أخضرَ 13/13 · READABLE** (تشغيلُ 33391458311) فصارَ `WI-023` في `IN_REVIEW` · والإغلاقُ **بيدِ المالكِ** |
| W-096 | حكمُ `daaed73` مقروءٌ 13/13 · نمطُ `DISC-032` حُصِيَ (24 · 5 · 4 · **1**) · `WI-029` محجوزٌ |
| W-097 | حرسُ نمطِ `DISC-032` نزلَ (24 · 19 · 5 مُرساةٌ · **0**) · الموضعُ أُرسِيَ بـ`--repo-root` · `WI-029` `IN_PROGRESS` |
| W-098 | حكمُ تشغيلِ 55 أحمرُ 12/13 · سببُه قُطِعَ في المرآةِ (`origin` رُبِطَ) · الأثرُ المُولَّدُ دُفِعَ |
| W-099 | تشغيلُ 56 أخضرُ 13/13 · `WI-029` `IN_REVIEW` · `DISC-040` مُقيَّدٌ باستحقاقٍ · `WI-030` محجوزٌ |
| W-100 | تشغيلُ 57 أخضرُ 13/13 · وجهُ `--check` وحرسُه نزلا (‏6 فحوصٍ) · السقّاطةُ ثابتةٌ عندَ 63 · `WI-030` `IN_PROGRESS` |
| W-101 | تشغيلُ 58 أخضرُ 13/13 · الحرسُ منفَذٌ في CI · `WI-030` `IN_REVIEW` · `DISC-040` محروسٌ ومفتوحٌ |
| W-102 | تشغيلُ 59 أخضرُ 13/13 · `DISC-041` مقيسٌ 25/53 (‏2/37 بلا وجهٍ) · `WI-031` محجوزٌ |
| W-103 | حرسُ نسَبِ جذرِ القياسِ نزلَ · سقّاطتانِ لا تعلوانِ · `WI-031` `IN_PROGRESS` |
| W-104 | تشغيلا 60 و61 أخضرانِ 13/13 · `WI-031` `IN_REVIEW` · `DISC-041` محروسٌ ومفتوحٌ |
| W-105 | تشغيلُ 62 أخضرُ 13/13 `READABLE` · سبعُ عُقَدٍ متَّصلةٍ بأحكامٍ مقروءةٍ |
| W-125 | إعلانٌ مُهيكَلٌ للحرسِ + سقّاطةُ «مسارٍ في نثرٍ» 25 · `WI-039` `IN_PROGRESS` |
| W-124 | حكمُ تشغيلِ 81 أخضرُ 13/13 `READABLE` · حاجزُ `DISC-021` أُعيدَ قياسُه فسقطَ · `WI-039` `RESERVED` |
| W-123 | حكمُ تشغيلِ 80 أخضرُ 13/13 `READABLE` · `WI-038` ← `IN_REVIEW` بلا `VERIFIED` |
| W-122 | `DISC-047` قُيِّدَ وأُغلِقَ · الإعفاءُ البيئيُّ يُقرأُ بأسمائِه · 17 فحصًا · حكمُ 79 الأحمرُ قُرِئَ ولم يُطوَ |
| W-121 | `DISC-046` محروسٌ · عددُ الجمعِ يُقاسُ حيًّا · 4 مواضعَ صُحِّحَت · 13 فحصًا · تشغيلا 77 و78 أخضرانِ 13/13 |
| W-120 | `DISC-046` مقيسٌ: المكتوبُ 1748 والمقيسُ 2351 (‏2352 مُجمَّعًا) · `WI-038` محجوزٌ · لا حرسَ بعدُ |
| W-119 | تشغيلُ 76 أخضرُ 13/13 `READABLE` · `WI-037` `IN_REVIEW` · 15 بندًا في يدِ المراجعِ |
| W-118 | `DISC-042` محروسٌ · نثرٌ لا يُغطّي ملفًّا · 8 ← 0 · التغطيةُ ثابتةٌ على 1855 ملفًّا · 11 فحصًا |
| W-117 | حجزُ `WI-037` · `DISC-042` مقيسٌ: 8 أجزاءٍ غيرِ مساريّةٍ في 4 بنودٍ · لا حرسَ بعدُ |
| W-116 | تشغيلُ 73 أخضرُ 13/13 `READABLE` · `WI-036` `IN_REVIEW` · 14 بندًا في يدِ المراجعِ · `DISC-045` محروسٌ ومفتوحٌ |
| W-115 | تشغيلُ 72 أخضرُ 13/13 · حرسُ `WI-036` نزلَ: التباسُ § 10 أربعةُ أرقامٍ تُعادُ وسقّاطةٌ لا تعلو · والمحكومُ به الإحالةُ **الملتبسةُ** لا «المعلَّقةُ» (‏تصحيحٌ مقيسٌ) |
| W-114 | حكمٌ أخضرُ **13/13** قُرِئَ (‏تشغيلٌ 71) فصارَ `WI-035` `IN_REVIEW` · و`DISC-045`: الرقمُ 9 في § 10 مكرَّرٌ فـ7 إحالاتٍ لا تُحَلُّ · و`WI-036` حُجِزَ |
| W-113 | حكمٌ أحمرُ ثالثٌ: العقدةُ أسقطَت سقّاطتَها · المُعلَنُ 134 ← **135** · حرسٌ سابقٌ للدفعِ (`baseline_node`) · `DISC-044` |
| W-112 | القياسُ صُحِّحَ بمصدرِه: `non_utc` 0 ← **134** · دعوى كاذبةٌ حُذِفَت |
| W-111 | حكمٌ أحمرُ مقروءٌ · الرقمُ المُعلَنُ صُحِّحَ 3 ← 4 · لا بوّابةَ خُفِّفَت |
| W-110 | حدُّ الصدقِ 9 مقيسٌ ومحروسٌ · `WI-034` `IN_REVIEW` · `DISC-043` |
| W-109 | حدُّ الصدقِ 8 محروسٌ · `WI-033` `IN_REVIEW` · الدَّينُ 25→24 تحتَ `tools/` |
| W-108 | حكمُ W-107 مقروءٌ 13/13 · `WI-033` `IN_REVIEW` · 11 بندًا تنتظرُ مراجعًا |
| W-107 | حكمُ W-106 مقروءٌ 13/13 · `WI-032` `IN_REVIEW` · الدَّينُ 19→10 و12→3 |
| W-106 | `WI-032`: مُكتشِفُ جذرٍ بعلامةٍ · 34 موضعًا رُحِّلَت · الدَّينُ 53→19 و37→12 |
| W-095 | حكمٌ مقروءٌ · `WI-028` ⟶ IN_REVIEW |
| W-094 | حكمٌ قُرِئَ فأُصلِحَ السببُ · المِسبارُ 31/31 |
| W-093 | الدعوى تُقرأُ بإعلانٍ صريحٍ · 20 وجهًا · العَضُّ مقيسٌ |
| W-092 | `WI-028` محجوزٌ · فحصُ التداخلِ مُقيَّدٌ · خطُّ الأساسِ 13 صفًّا |
| W-091 | ثلاثةُ أحكامٍ خضراءَ 13/13 · READABLE فصارَ `WI-027` `IN_REVIEW` |
| W-090 | مِرساةُ الحقلِ المُعلَنِ · أربعةُ أنواعٍ تُسقِطُ · 53 وجهًا |
| W-089 | `WI-027` محجوزٌ · فحصُ التداخلِ مُقيَّدٌ · الحدُّ مُعلَنٌ قبلَ العملِ |
| W-088 | ثلاثةُ أحكامٍ خضراءَ 13/13 · READABLE فصارَ `WI-026` `IN_REVIEW` |
| W-087 | أثرُ التشغيلِ عُزِلَ عن الشجرةِ في مصدرَيهِ · وضعُ القرصِ من مخالفتَينِ إلى صفرٍ |
| W-086 | `WI-026` محجوزٌ ومُعلَنُ الحدِّ: أثرُ التشغيلِ يُعزَلُ عن الشجرةِ بلا تخفيفِ بوّابةٍ |
| W-085 | حكمُ عقدتَي `W-083` و`W-084` أخضرُ 13/13 · READABLE فصارَ `WI-025` `IN_REVIEW` |
| W-084 | مفتاحُ الموقعِ في القياسِ المنشورِ نُقِلَ عن رقمِ السطرِ · 11 فحصًا تحرسُ الطزاجةَ والإنفاذَ معًا |
| W-083 | حُجِزَ `WI-025` — مفتاحُ الموقعِ في القياسِ المنشورِ يُنقَلُ عن رقمِ السطرِ (‏`DISC-039`) |
| W-082 | حكمُ عقدةِ `W-081` أخضرُ 13/13 · READABLE · ومعيارُ القبولِ السبعُ مُستوفًى فصارَ `WI-024` `IN_REVIEW` |
| W-081 | حكمُ عقدةِ `W-080` قُرِئَ أحمرَ في وظيفةٍ واحدةٍ · قياسٌ منشورٌ مُفتَّحٌ برقمِ سطرٍ أُعيدَ توليدُه · `DISC-039` |
| W-080 | طريقُ إنفاذِ كلِّ أداةٍ قابلةٍ للتشغيلِ يُعلَنُ أو تسقُطُ البوّابةُ (43 ملفًّا · 11 إعلانًا مُصنَّفًا · `DISC-038`) |
| W-079 | جردُ الأدواتِ غيرِ المربوطةِ قِيسَ سلوكًا وكلفةً ورمزَ خروجٍ (13 ⇒ ثلاثةُ أنواعٍ لا نوعٌ) · وحُجِزَ `WI-024` بلا مساسٍ بمسارٍ مقفولٍ |

**ثلاثُ حقائقَ لا تُطوى، مقيسةٌ في 2026-08-27:**

1. **حكمُ CI غيرُ مقروءٍ — لا أخضرَ ولا أحمرَ يُدَّعى.** ستةَ عشرَ تشغيلًا قِيسَ
   حكمُها بـ[`ci_verdict_readability.py`](tools/governance/ci_verdict_readability.py)
   فكانت **16/16 غيرُ مقروءةٍ**: لا وظيفةَ نُفِّذَت ولا مُنفِّذَ عُيِّنَ. والسببُ
   `DISC-006`/`RK-011` — إعدادُ الحسابِ، بيدِ المالكِ لا بيدِ من يعملُ.
2. **بنودٌ مدموجةٌ وما زالت `IN_REVIEW`.** `WI-006`…`WI-012` دُمِجَت أو رُفِعَت،
   وإغلاقُها يحتاجُ مراجعًا مستقلًّا وهو عينُ القرارِ `A-2` **المعلَّقِ** — فالخليّةُ
   تُترَكُ لمن يجبُ عليه الإغلاقُ (‏`DISC-012`) ولا تُملأُ بيدِ المنفِّذِ.
3. **مصفوفةُ الحقيقةِ ثابتةٌ عندَ 63** (60 مخزنَ ذاكرةٍ · 2 حقيقةً مضمَّنةً ·
   1 عزلًا معطَّلًا). والاثنتانِ والواحدةُ **محجوبةٌ نصًّا** بـ`Q-35` و`Q-33`:
   يقولُ `Q-35` حرفًا «العدُّ يبقى 2 كما هو ولا يُمَسّ» حتى يُحسَمَ المعيارُ.

---

## Current Capability — ما صارت الدولة قادرة عليه بعد 2A

**تم إثبات نمط Sovereign Runtime Integration على كتابتين إنتاجيتين مختلفتين، وتم
إغلاق مساراتهما القديمة، مع بقاء بقية نقاط التجاوز القائمة كـ migration debt.**

العمليّتان: تأسيسُ المؤسسةِ في `state_registry.register_institution`، وتوزيعُ
الميزانيةِ في `governance.state_runtime.allocate_budget`. كلتاهما تنفُذُ الآن من
`guard_declared → SovereignExecutionBoundary → 1F–1N` بأثرٍ مُعلَنٍ قبلَ وقوعِه،
ومفتاحِ ذرّيّةٍ، ومعوّضٍ يعكسُ عكسًا حقيقيًّا، وإغلاقٍ عندَ الفشل، وبلا مسارِ
تجاوز. ومسارُهما القديمُ صارَ يُرفَعُ ولا يُنتِجُ أثرًا. والتفصيلُ والدَّينُ
المُعلَنُ في [`docs/audit/STAGE_2A_HANDOFF.md`](docs/audit/STAGE_2A_HANDOFF.md).

**ولا يُقال إنَّ State Runtime أصبح Sovereign بالكامل:** بقيّةُ كتاباتِ السجلِّ
وزمنِ التشغيلِ والخزانةِ والاقتصادِ **لم تُهاجَر**، وهي دَينُ هجرةٍ لا إنجاز.

## Capability بعد 1N

**التنفيذُ الفدراليُّ صار يمرُّ من الحدِّ فعلًا، لا يستطيعُ ألّا يمرَّ.** 1M بنَت
`SovereignExecutionBoundary` وأثبتت أنَّ حرسَه لا يُتجاوَز، لكنّها تركَت سؤالًا
مقيسًا: **مَن يناديه؟** فالمُنادي الإنتاجيُّ الوحيدُ للبوّابة كان
`ConstitutionalAuthorizer.guard` يُمرِّر `Callable[[], Any]` مُبهَمة من
`ExecutiveCore.submit` و`_guarded_transition` — أي أنَّ الحدَّ كان قدرةً **قائمةً
غيرَ مُنفَذَة**. الآن: كلُّ انتقالِ حالةٍ في النواةِ التنفيذيّة (إيداعٌ · انتقالٌ ·
إلغاء) لا يقعُ إلّا بأثرٍ **مُعلَنٍ قبلَ وقوعِه**، تحتَ إذنٍ مُصدَرٍ لذلك الأثرِ
بعينِه، ومفتاحِ ذرّيّةٍ يمنعُ وقوعَه مرّتين، ومعوّضٍ مربوطٍ بعكسٍ حقيقيّ
(`repository.delete` — بُنيَ في 1N لأنَّ المعوّضَ بلا عكسٍ ادّعاء).

**والمسارُ الذي كان يتجاوزُ الحدَّ أُغلِق:** `ConstitutionalAuthorizer.guard`
و`SubordinateGateway.execute` بقيَتا بتوقيعِهما وصارتا ترفعان
`UndeclaredExecutionError` **ولا تستدعيان المُنفِّذَ أصلًا** — مقيسٌ باختبارٍ يُثبِتُ
أنَّ الحالةَ لم تتغيّر لا أنَّ استثناءً ارتفع. ولا حدَّ افتراضيًّا يُبنى من داخلِ
الطبقةِ التابعة: غيابُ الحدِّ **توقُّفٌ لا تجاوز**.

**ما دخلَ الإنتاجَ بهذا:** 1H — لم يكن في المستودعِ قبلَ اليومِ مُنشئٌ إنتاجيٌّ
واحدٌ لِـ`IdempotencyLedger`؛ و1I — لم يكن للمهمّةِ المُنشأةِ عكسٌ حقيقيّ؛ و1M —
كان له اختبارٌ ولم يكن له مُنادٍ إنتاجيّ.

**الحدُّ المُعلَن:** أدواتُ الإثباتِ والعرض (`cli.py` · `tools/sovereignty/`) لم
تُهاجَر — مُقاسٌ لا مُقدَّر: مُنفِّذُها يُرجِعُ نصًّا ثابتًا ولا يمسُّ حالةً. والقائمةُ
مُعلَنةٌ في الاختبارِ فلا تنمو بصمت. ولا حرسَ ساكنًا يمنعُ النداءَ المباشرَ
للبوّابة — المنعُ باختبارِ حرسِ انحدارٍ مُعلَنِ النطاق، والحرسُ الأوسعُ **متاحٌ
وغيرُ مُنفَذ**.

## Previous Capability — ما صارت الدولة قادرة عليه بعد 1M

**للدولة الآن حدُّ تنفيذٍ لا يستقبل دالّةً مُبهَمة.** قبل 1M كان المسار الحقيقي
`SovereignGateway.execute(request, executor)` و`executor` فيه `Callable[[], T]`
مُبهَمة: البوّابة تفرض الهوية والسلطة (1D) والإغلاق عند الفشل (1J) — وهذا **مقيس
لا مُفترَض** — ولا تعرف شيئًا عمّا يجري بداخلها. وقياسُ `6562c61` أظهر أنّ
`gateway.py` **لا يستورد** الاختصاص ولا الذرّيّة ولا التعويض ولا الصادر، وأنّ
`run_effects_once` و`enqueue_write_ahead` و`.enforce(` و`WALL.evaluate`
و`bind_compensation_plan` و`.deliver(` **بلا موضع نداء إنتاجي واحد** خارج
وحداتها. الآن `SovereignExecutionBoundary` يستقبل **خطّة مُعلَنة**
(`declared_effects` · `planner` · `applier` · `operation_key`) ويُشغّل السلسلة
بالترتيب: الاختصاص (1G) ← العقد (1E) ← خطّة التعويض (1I) ← الهوية والسلطة (1D)
← الإغلاق عند الفشل (1J) ← الإذن (1F) ← الذرّيّة (1H) ← الصادر للأثر الخارجي (1K).

**مقيسٌ لا موصوف:** `BoundaryOutcome.stages` يُعلِن ما عُبِر فعلًا؛ وكلُّ محاولة
تجاوز (سلطة · اختصاص · إذن · ذرّيّة · إغلاق · صادر) **تفشل وحالةُ الدولة لا
تتغيّر**؛ والأثر الخارجي **لا يبلغ مُطبِّق المُنادي ولا المزوّد** بل يُقيَّد
`PENDING`. و**8 طفرات من 8 قُتِلَت**: إلغاءُ أيِّ حرس يُسقط اختبارًا.

**الحدّ المُعلَن:** 1M **بنَت الحدّ ولم تُهاجرْ به المُنادين القائمين**
(`SubordinateGateway` · `sovereignty_bridge` · `cli.py` ما زالت تستدعي
`gateway.execute` مباشرةً، محروسةً بـ1D و1J كما كانت). والحرسُ الساكن يعمل
ويُصيب لكنّه **غير مربوط بـCI** (ولا جولة CI مُشاهَدة قطُّ). ولا يُدَّعى أنّ
المستودع كلَّه محروس: `claims_whole_repository_guarded is False` في الكود.

## Earlier Capability — ما بُنيَ في 1K (باقٍ)

**الدولة تُثبِّت الأثر الخارجي قبل أن تحاوله، ولا تشهد له إلّا بتأكيد، ولا تكذب
حين يكون مصيره مجهولًا.** قبل 1K كان الأثر الخارجي **ممنوعًا** في المسار الذرّي
(1H يرفض بصمة `EXTERNAL:` ويُحيل إلى «outbox» غير موجود) و**مرفوض التعويض** في
1I: صدقٌ بالامتناع. الآن سجلّ دائم على القرص بكتابة ذرّية + حجز بعقد زمني
(lease) + علامة محاولة «قيد الطيران» تُثبَّت **قبل** نداء المزوّد + عامل تسليم
حقيقي قابل لإعادة التشغيل. و`DELIVERED` هي الحالة الوحيدة الشاهدة بالتسليم، ولا
تُكتَب إلّا من إيصال مزوّد.

**مقيسٌ لا موصوف:** انهيارٌ داخل نداء المزوّد يترك `INDETERMINATE` لا `PENDING`
ولا `DELIVERED` ولا `FAILED`؛ وعاملان متنافسان يُنتجان **عنصرًا واحدًا** في حالة
خارجية تُعَدّ عدًّا. وست طفرات على التصميم تُسقط 25 · 1 · 4 · 2 · 1 · 1 اختبارًا.

**الحدّ المُعلَن:** **لا ذرّيّة عبر نظامين** — ديمومةٌ مرتَّبة: الاتجاه الخطِر
(نجاحٌ مُثبَت بلا أثر مُدرَج) مرفوضٌ بنيويًّا، والحميد (أثرٌ مُدرَج بلا عملية
مكتملة) مكشوفٌ بـ`orphaned_effects`. و**لا ادّعاء exactly-once** — منفيٌّ في
الكود: `self_check()["claims_exactly_once"] is False`. والصادر **غير مفروض**
كمسار وحيد للأثر الخارجي (دَين 1M)، ولا مُجدوِل يُشغّل عامله.

## Earlier Capability — ما بُنيَ في 1J (باقٍ)

**الدولة لا تستطيع أن تدّعي تنفيذًا ناجحًا والمعاملةُ السيادية ناقصة.** قبل 1J
كان `ExecutionRecord.executed` يُكتَب `True` **قبل** استدعاء المُنفِّذ، فانهيارُه
— أو نجاحه نصفًا — يترك في سجلّ الدولة سطرًا يشهد بالنجاح، و`self_check()` يعُدّ
به «التنفيذات السيادية». الآن `executed` **خصيصة مقروءة مُستنبَطة** من حالة
اكتمال صريحة، و`True` منها حالة واحدة: `COMPLETED` — تنفيذٌ تامٌّ مختومٌ بمرتكز
تدقيقي. وبناءُ سجلٍّ يزعم النجاح زعمًا مباشرًا صار **غير ممكن بنيويًّا**.

**مقيسٌ لا موصوف:** انهيار المُنفِّذ بعد أثر جزئي يترك سطر إذن (`AUTHORIZED`)
وسطر فشل (`EXECUTION_FAILED`)، و`any(r.executed for r in records) is False`
(`test_انهيارُ_المُنفِّذِ_لا_يترك_ادِّعاءَ_نجاحٍ_واحدًا`). وإعادةُ الثغرة
التاريخية كطفرة تُسقط **9 اختبارات**.

**الحدّ:** الفرض محصور في `gateway.execute` عبر نقطة واحدة (`_run_and_seal`)
مُقاسة على شجرة المصدر؛ لا حرس ساكن يمنع مسارًا مستقبليًّا من تجاوزها (دَين 1M).

## المراحل المُنجَزة

| المرحلة | الموضوع | الحالة |
|---|---|---|
| 1A | إصلاح أدوات قياس الحقيقة | مُنجَز — جزئي (97 مخالفة لم تُعالَج) |
| 1B | المصالحة الدستورية | مُنجَز |
| 1C | الجذر البشري (تنسيب التاج) | مُنجَز — HUMAN PROVISIONING REQUIRED |
| 1D | نموذج السلطة | مُنجَز |
| 1E | عقد التنفيذ السيادي | مُنجَز |
| 1F | فصل موضع القرار عن الإنفاذ | مُنجَز |
| 1G | جدار الاختصاص القضائي | مُنجَز |
| 1H | الذرّيّة ومنع تكرار الأثر | مُنجَز |
| 1I | التعويض عن الأثر المُطبَّق | مُنجَز |
| 1J | الإغلاق عند الفشل (Fail-Closed) | مُنجَز |
| 1K | صادرُ الآثارِ الخارجيّةِ الدائم (Outbox) | مُنجَز |
| 1M | حدُّ الإنفاذِ السياديُّ والحرسُ الساكن | مُنجَز |
| **1N** | **وصلُ الإنفاذ — هجرةُ المُنادين إلى الحدّ** | **مُنجَز — هذا التسليم** |

## Proven Capabilities — مُثبَتة بدليل مُشغَّل

| القدرة | الطبقة | الدليل |
|---|---|---|
| سحب الصلاحية يمنع فعلًا | 1D | مقيس من البوابة |
| الأثر يُعلَن قبل وقوعه ويُمنَع ما خرج عن العقد | 1E | مقيس بحالة دولة حقيقية |
| موضع إنفاذ بلا محرّك ينفّذ إذنًا موقَّعًا | 1F | مقيس بحالة دولة حقيقية |
| حدود الاختصاص القضائي تُفرَض قبل التجاوز | 1G | `test_jurisdiction_wall.py` |
| عملية واحدة تُنتِج أثرًا واحدًا (تسلسليًّا ومتزامنًا) | 1H | `test_idempotency.py` — 67 اختبارًا |
| **الحالة ترجع بالضبط إلى ما قبل فشل جزئي** | **1I** | `test_partial_failure_restores_exact_prior_state` |
| **التعويض نفسه ذرّي — تكرارٌ أو تزامنٌ لا يعكس مرّتين** | **1I** | `test_repeated_compensate_does_not_double_reverse` · `test_concurrent_compensation_reverses_once` |
| **الأثر الخارجي يُرفَض بدل ادّعاء عكسه** | **1I** | `test_external_effect_contract_cannot_be_compensated` |
| **فشل التعويض يُعلَن ولا يُقلَب نجاحًا** | **1I** | `test_failing_compensator_yields_partial_and_names_residual` |
| عكسٌ كامل يرفع `RECOVERY_REQUIRED` وتُعاد العملية كاملة مرّة واحدة | 1I | `test_retry_after_clean_compensation_reapplies_everything` |
| **`executed True` لا تُنتجه إلّا معاملة تامّة مختومة بمرتكز تدقيقي** | **1J** | `test_الاكتمالُ_وحدَه_يعني_نُفِّذ` · `test_تنفيذٌ_تامٌّ_يُنتِج_executed_True` |
| **ادّعاء النجاح غير قابل للبناء — لا حقل يُضبَط** | **1J** | `test_executed_ليس_حقلًا_يُضبَط_عند_البناء` |
| **انهيار المُنفِّذ لا يترك ادّعاء نجاح واحدًا، والاستثناء يُعاد بنوعه** | **1J** | `test_انهيارُ_المُنفِّذِ_...` · `test_الاستثناءُ_يُعادُ_بنوعِه_لا_مُترجَمًا` |
| **فشل الكتابة في السجلّ الدستوري يمنع التنفيذ** | **1J** | `test_انهيارُ_السجلِّ_الدستوريِّ_يمنع_التنفيذ` |
| **كلا مسارَي التنفيذ يمرّان من نقطة الفرض نفسها** | **1J** | `test_المسارُ_السياديُّ_يمرُّ_من_قاعدةِ_الاكتمالِ_نفسِها` |
| **القرار الملكي بلا مرتكز: يُنفَّذ ولا يُصدَّق** | **1J** | `test_قرارٌ_ملكيٌّ_بلا_مرتكزٍ_يُنفَّذ_ولا_يُصدَّق` |
| **الأثر الخارجي يُثبَّت على القرص قبل محاولته ويبقى مكتشفًا بعد إعادة التشغيل** | **1K** | `test_الادراج_يكتب_على_القرص_لا_في_الذاكرة` · `test_المعلق_يبقى_بعد_اعادة_التشغيل` |
| **الحجز دائم بعقد زمني؛ المهجور يُستردّ والساري لا يُنتزَع** | **1K** | `test_الحجز_يثبت_على_القرص` · `test_الحجز_المهجور_بلا_نداء_يعود_معلقا` · `test_الحجز_الساري_لا_يستعاد` |
| **`DELIVERED` لا تُكتَب إلّا بتأكيد المزوّد، وهي الحالة الوحيدة الشاهدة** | **1K** | `test_النجاح_يثبت_تسليما` · `test_التسليم_لا_يشهد_له_الا_حالة_واحدة` |
| **المصير المجهول يُثبَّت `INDETERMINATE` ولا يُقلَب نجاحًا ولا فشلًا ولا تعليقًا** | **1K** | `test_السقوط_داخل_النداء_يقرأ_غموضا_لا_تعليقا` · `test_استثناء_غير_مصنف_يقرأ_غموضا_لا_فشلا` |
| **هوية التفرُّد حاسمة وثابتة عبر كل المحاولات — مقيسة سلوكيًّا وبنيويًّا** | **1K** | `test_رقم_التفرد_ثابت_عبر_كل_المحاولات` · `test_مادة_الهوية_محصورة_في_وسائطها` |
| **عاملان متنافسان لا يُضاعفان الأثر الخارجي (مقيس بعدّ الحالة الخارجية)** | **1K** | `test_عاملان_متنافسان_لا_يضاعفان_الاثر_الخارجي` |
| **الصادر لا يمنح سلطة ولا يتجاوز إذنًا ولا يقلب `DENY` إذنًا** | **1K** | `test_الادراج_بلا_اذن_غير_ممكن` · `test_المنع_لا_يتحول_الى_اذن` · `test_لا_رايات_تجاوز_في_اي_دالة` |
| **لا مادة سرّية تُثبَّت في حمولة ولا في نص خطأ** | **1K** | `test_لا_سر_في_الملف_المثبت` · `test_نص_الخطأ_ينقى_من_السر` |
| **حالة العملية السيادية وحالة الأثر الخارجي منفصلتان ولا تُدمَجان** | **1K** | `test_حالة_العملية_لا_تدمج_في_حالة_الاثر` |
| **المُنادي لا يبلغ أثر التنفيذ إلّا بعبور المراحل الإلزاميّة المُعلَنة** | **1M** | `Testإثباتُالتكامل` — مقيس بـ`BoundaryOutcome.stages` |
| **محاولات تجاوز السلطة والاختصاص والإذن والذرّيّة والإغلاق والصادر تفشل ولا تُنتِج أثرًا** | **1M** | 24 اختبار تجاوز — كلٌّ يؤكّد أنّ حالة الدولة لم تتغيّر |
| **لا سبيل لتمرير `Callable[[], T]` مُبهَمة إلى حدّ الإنفاذ** | **1M** | `Testإثباتٌخصميّ` — الإبهام مُغلَق بالتوقيع لا بالتوثيق |
| **الأثر الخارجي لا يُسلَّم لمُطبِّق المُنادي ولا لمزوّد؛ يُقيَّد `PENDING`** | **1M** | `Testتجاوزُالصادر` |
| **لا تنفيذ بلا مفتاح ذرّيّة — ولا إذن يُصدَر للعملية غير المُفتَّحة** | **1M** | `Testتجاوزُالذرّيّة` |
| **الأثر القابل للعكس بلا مُعوِّض مرفوض، والعقد المختلط مرفوض باسمه** | **1M** | `Testتجاوزُالتعويض` |
| **لا معامَل تجاوز في حدّ الإنفاذ ولا في البوّابة — قياسًا ببصمات حقيقية** | **1M** | `Testلامعامَلَتجاوز` · `bypass_parameters_of` |
| **حرس ساكن يُصيب أربعة أنماط تجاوز في `core/sovereignty` (18 ملفًّا)** | **1M** | `Testالحرسُالساكن` — 15 اختبارًا |
| **إلغاء أيِّ حرس من الثمانية يُسقط اختبارًا** | **1M** | 8 طفرات ⇒ 8 قتلات |
| **التنفيذُ الفدراليُّ للمهام يمرُّ من الحدِّ فعلًا — بمراحلِه الإلزاميّةِ مقروءةً من الحصيلة** | **1N** | `test_guard_declared_passes_all_mandatory_stages` · `test_التنفيذ_التابع_يمر_بكل_مراحل_الحد` |
| **المسارُ المُتجاوِزُ لا يُنفِّذ ولا يستدعي المُنفِّذَ أصلًا، والحالةُ لا تتغيّر** | **1N** | `test_guard_refuses_and_never_calls_executor` · `test_المنفذ_المبهم_يُرفَض_ولا_يُستدعى` |
| **دلالاتُ النجاحِ والفشلِ العامّةُ محفوظة: `IllegalTransitionError` غيرُ ملفوفٍ في خطأِ سجلّ** | **1N** | `test_illegal_transition_keeps_its_own_error_and_state` |
| **الإيداعُ والانتقالُ والإلغاءُ يُغيِّرون قاعدةَ البياناتِ فعلًا تحتَ الحدّ** | **1N** | `test_submit_succeeds_and_really_creates_a_task` · `test_legal_transition_really_changes_state` |
| **الأثرُ الخارجيُّ لا يبلُغُ مزوّدًا من مسارِ التنفيذ — مزوّدٌ يُفشِلُ الاختبارَ إن نُودي لم يُنادَ** | **1N** | `test_الاثر_الخارجي_يُرتَّب_في_الصادر_ولا_يبلغ_المطبِّق` |
| **الإعادةُ تُعلَن ولا تُعَدُّ نجاحًا ثانيًا، والأثرُ يُطبَّقُ مرّةً واحدةً لمفتاحٍ واحد** | **1N** | `test_replay_does_not_apply_effect_twice` · `test_الاعادة_تُعلَن_ولا_تُعَد_نجاحا_ثانيا` |
| **مُنادٍ إنتاجيٌّ جديدٌ يتجاوزُ الحدَّ يُفشِلُ الاختبار — قائمةٌ مُعلَنةٌ لا تنمو بصمت** | **1N** | `test_لا_منادي_انتاجي_جديد_يستدعي_البوابة_مباشرة` · `test_الحد_لا_يُنسَخ_في_البوابات_التابعة` |

## Available but not Enforced — متاح ولم يُفرَض

| القدرة | لماذا لم تُفرَض |
|---|---|
| عقد التنفيذ (1E) | المسار القديم `execute` بلا عقد باقٍ ومعدود ومُعلَن |
| الإذن الموقَّع (1F) | لا شيء يُلزِم مكوّنًا باستعماله |
| جدار الاختصاص (1G) خارج حدّ الإنفاذ | **مفروض على مسار `SovereignExecutionBoundary` (1M)**؛ ومن استدعى البوّابة مباشرةً لا يُلزمه شيء. والجدار **قضائي بحصره** — لا جدار اختصاص فدرالي عامّ |
| ذرّيّة العملية (1H) خارج حدّ الإنفاذ | **مفروضة على مسار حدّ الإنفاذ (1M)**؛ ومن استدعى `run_once` أو البوّابة مباشرةً ما زال بلا إلزام |
| التعويض (1I) خارج حدّ الإنفاذ | **مفروض على مسار حدّ الإنفاذ (1M)**: الأثر المُغيِّر القابل للعكس يُوجِب مُعوِّضًا. ومن استدعى `run_effects_once` مباشرة ما زال يستطيع |
| **الإغلاق عند الفشل (1J) خارج البوابة** | **مفروض داخل `gateway.execute` وحدها، وحدُّ الإنفاذ (1M) يعبرها دائمًا. ومن نفّذ أثرًا دون المرور بأيّهما لا يُلزمه شيء.** |
| **`MandatoryStage` كسلسلة مُلزِمة** | **التعداد يُسمّي المراحل الموجودة، ولا حرس يُلزم كل مسار بالمرور بها جميعًا.** |
| **`RECOVERY_REQUIRED` ⇒ تعويض تلقائي** | **الحالة تُعلَن ولا تستدعي خطّة 1I آليًّا؛ الوصل لم يُبنَ.** |
| **الصادر (1K) كمسار وحيد للأثر الخارجي** | **مفروض على مسار حدّ الإنفاذ (1M): المُنادي لا يملك سبيلًا لتمرير أثر خارجي إلى دالّته. وساكنًا: `R2_DIRECT_PROVIDER_DELIVER` يرفض النداء المباشر داخل `core/sovereignty` فقط.** |
| **تشغيل `OutboxWorker` دوريًّا** | **`run_once`/`drain` قائمتان ولا مُجدوِل ولا خدمة تُشغّلهما؛ لا تسليم بلا نداء صريح.** |
| **`DEAD` ⇒ تعويض 1I · `INDETERMINATE` ⇒ استعلام لدى المزوّد** | **الحالتان تُعلَنان ولا وصل آليًّا لهما.** |
| **توزيع الصادر عبر عُقَد** | **قفل داخل العملية وملفّ محلّي؛ التنافس مقيس داخل عملية واحدة لا عبر آلات.** |
| **الحرس الساكن (1M) كبوّابة CI** | **الحرس يعمل ويُصيب، و`self_check` نظيف على `core/sovereignty`؛ لكن لا جولة CI مُشاهَدة في هذا المستودع قطُّ، فلا يُدَّعى أنّه يمنع دمجًا. تشغيله بوّابة = قرار مؤسَّسي.** |
| **حدّ الإنفاذ كمسار المُنادين القائمين** | **أُغلِقت في 1N للمُنادين الإنتاجيّين: `sovereignty_bridge` و`SubordinateGateway` هُوجِرا والقديمُ يُغلِق. وتبقى `cli.py` و`tools/sovereignty/` غيرَ مُهاجَرتين — أدواتُ إثباتٍ لا تمسُّ حالةً، وهجرتُها أو إعلانُها استثناءً دائمًا = قرار بشري (#11).** |
| **حرس ساكن يمنع النداء المباشر للبوّابة** | **لم يُبنَ في 1N قصدًا: أيُّ قاعدةٍ من هذا النوع كانت تستلزم إعفاءَ `gateway.py` و`cli.py` والأدوات، فتُصبح حرسًا مُهندَسًا ليَخضَرّ. المنعُ باختبارِ حرسِ انحدارٍ مُعلَنِ النطاق، والحرسُ الأوسع متاحٌ وغيرُ مُنفَذ.** |
| **انحدار الحزمة الفدراليّة** | **مقيسٌ يدويًّا في جلسة 1N (1149 اختبارًا) ولا بوّابة آليّة تُشغّله؛ وتبعيّاته غير مُعلَنة في المستودع فالقياس غير قابل للتكرار بلا تثبيتها.** |
| **الحرس الساكن للنداء غير المباشر** | **نصّي: لا يرى `getattr`/`eval`/استيرادًا ديناميكيًّا — مُعلَن في الكود `detects_indirect_calls is False`.** |

## المؤشرات الحالية — مقيسة 2026-08-18

| المؤشر | القيمة |
|---|---|
| اختبارات `tests/` | **1440 ناجح · 1 متجاوَز · 3 أخطاء قائمة** (نقص حزمة `amos_federation` في الساندبوكس) — كانت 1423 على `1cae95f`. الفارق 17 = اختبارات 1N في الجذر بالضبط |
| **الحزمة الفدراليّة كاملةً (1149 اختبارًا)** | **1115 ناجح · 19 فاشل · 25 متجاوَز** — والأساس `1cae95f` قِيس بإعادة تشغيل الحزمة نفسها بعد `git stash`: 1105 · 19 · 25. **مجموعة الفشل متطابقة حرفًا بحرف** (`diff` على الأسماء المرتَّبة) — لا فشل واحدًا أضافته 1N |
| اختبارات 1N | **27 ناجحًا** (17 في الجذر: `test_enforcement_integration.py` · 10 فدراليّة: `test_1n_boundary_integration.py`) |
| اختبارات `tests/sovereignty/` | **723 ناجح · 1 متجاوَز · 3 أخطاء قائمة** — كانت 634 |
| اختبارات 1M | 52 ناجحًا (قياس 1M — لم يُعَد) |
| اختبارات 1K | 89 ناجحًا (قياس 1K — لم يُعَد) |
| اختبارات 1J | 37 ناجحًا (قياس 1J — لم يُعَد) |
| تغطية `outbox.py` | **96% فرعية** (504 عبارة · 132 فرعًا · 14 عبارة و8 فروع غير مُغطّاة) — لم تُطارَد |
| تغطية `fail_closed.py` | 100% فرعية (قياس 1J — لم يُعَد) |
| تغطية `compensation.py` | 98% فرعية (قياس 1I — لم يُعَد) |
| تغطية `idempotency.py` | 95% فرعية (قياس 1H — لم يُعَد) |
| فحص الطفرات على 1N | **لم تُشغَّل أداةُ تحويلٍ آليّة**. والهجرةُ نفسُها كانت تحويلًا مقصودًا على المسار الحقيقي: إغلاقُ `guard` كشفَ أنَّ المُنادي الإنتاجيَّ الوحيدَ هو المحرّك · إغلاقُ `SubordinateGateway.execute` أسقطَ اختبارًا واحدًا ولم يُسقِط إنتاجًا · نقلُ `assert_transition` قبلَ الحدِّ كشفَ لفَّ `IllegalTransitionError` في `IdempotencyError` · ربطُ المعوّضِ بعكسٍ حقيقيٍّ كشفَ أنَّ المستودعَ لا يملكُ `delete` |
| فحص الطفرات على 1M | **8 طفرات ⇒ 8 قتلات (4 · 1 · 2 · 3 · 1 · 2 · 1 · 2 اختبارًا ساقطًا)** · ولا طفرة نجت · وبعد الاستعادة 52 ناجحًا. حملة **موجَّهة** لا شاملة، بإذن نصّ القرار البشري. أداة الطفرة مؤقّتة وحُذِفت قبل التثبيت |
| فحص الطفرات على 1K | 6 طفرات ⇒ 25 · 1 · 4 · 2 · 1 · 1 (قياس 1K — لم يُعَد) |
| `ruff` | نظيف على كلِّ ملفّات 1N المُعدَّلة والجديدة · **167 مخالفة قائمة في باقي المستودع** (كانت 168؛ النقصانُ أثرٌ جانبيٌّ لتعديلٍ في ملفٍّ مُهاجَر لا حملةَ تنظيف). **لا يُدَّعى نظافة المستودع** |
| `truth_audit --ratchet` | **97 مخالفة — ثابت بلا زيادة بعد 1N** (IN_MEMORY_STORE 60 · SILENT_FALLBACK 26 · HARDCODED_TRUTH 10 · SANDBOX_DISABLED 1). وسُجِّل في 1M: الصياغة الأولى للوحدة أضافت `SILENT_FALLBACK` واحدة (`except (TypeError, ValueError): continue`) فرفضتها البوّابة؛ **صُلِح المصدر ولم يُرفَع السقف**: ما لا تُقرَأ بصمته يرفع `StaticGuardError` بدل أن يُتخطّى صامتًا |
| المصالحة الدستورية | **15** (7 تُسَدُّ بالتنفيذ + 8 قرارات بشرية) — مطابقة للأساس |
| مسح الأسرار على الملفّات المتغيّرة | **نظيف** — لا رمز مستودع ولا مفتاح Supabase ولا سلسلة اتصال حقيقية |
| `check_repository_identity` | ناجح |
| `COMPLETION` | **NOT YET COMPUTABLE** |
| التاج | **UNPROVISIONED** |
| بيئة القياس | Python 3.14.3 (بيئة الوكيل) — **لم تُشاهَد جولة CI فعلية**. وتبعيّاتُ الحزمة الفدراليّة (`sqlalchemy` · `pydantic-settings` · `pyjwt` · `structlog` · `greenlet`) ثُبِّتت في بيئة التشغيل **ولم تُضَف إلى المستودع** |

> **تصحيح قياس سابق (سُجِّل في 1I ويبقى):** كان مُسجَّلًا «المصالحة الدستورية =
> 20 — ثابت»، والقياس الفعلي لمخرَج الأداة هو **15**. الرقم لم يتغيّر بفعل 1I ولا
> 1J، لكنّ المُسجَّل سابقًا لم يكن مطابقًا للمخرَج. صُحِّح ولم يُترَك.

## Open Issues — المشكلات المفتوحة

| # | المشكلة | الحالة |
|---|---|---|
| 1 | 3 أخطاء في `test_enforcement_separation.py::Testالوصولُالفدراليّ` — `ModuleNotFoundError: amos_federation` | قائمة من قبل 1H · **ليست من 1J** — لم تُلمَس |
| 2 | 97 مخالفة `truth_audit` | قائمة منذ 1A · ثابتة بلا زيادة |
| 3 | outbox مقاوم للانهيار للأثر الخارجي | **أُغلِقت — بُنيَت في 1K** بحدودها المُعلَنة (لا ذرّيّة عبر نظامين · لا exactly-once · غير مفروضة كمسار وحيد) |
| 4 | استئناف تلقائي لتعويض انقطع في منتصفه | لم يُبنَ · جديد في 1I |
| 5 | فشل المعوّض نفسه لا يُعوَّض (لا إعادة محاولة ولا backoff) | مقصود · يُطلَب قرار بشري |
| 6 | `ExecutionRecord.executed = True` يُكتَب قبل استدعاء المُنفِّذ | **أُغلِقت — صُلِحت في 1J** |
| 7 | لا توزيع للتعويض بين العمليات أو العُقَد (سجلّ ملفّ محلّي) | لم يُبنَ |
| 8 | 168 مخالفة `ruff` في المستودع (لا واحدة منها من 1J) | قائمة من قبل 1H · لم تُلمَس |
| 9 | Supabase DB المباشر غير متاح من الساندبوكس | قائمة |
| 10 | لا وصل تلقائيًّا بين `RECOVERY_REQUIRED` وخطّة تعويض 1I | **جديدة في 1J** |
| 11 | الإغلاق عند الفشل مفروض داخل `gateway.execute` وحدها | **ضاقت في 1M**: حدُّ الإنفاذ يعبرها دائمًا، والحرس الساكن يمنع ابتلاع خطأ حرس داخل `core/sovereignty`. وتبقى مفتوحةً لمن استدعى البوّابة من خارج الحدّ |
| 12 | لا مُجدوِل يُشغّل `OutboxWorker`؛ لا تسليم بلا نداء صريح | **جديدة في 1K** |
| 13 | لا وصل بين `DEAD` (صندوق الموتى) وتعويض 1I | **جديدة في 1K** |
| 14 | لا استعلام لدى المزوّد لحسم `INDETERMINATE`؛ ومع مزوّد غير متفرِّد يقف الآليّ عند حدّه | **جديدة في 1K** · قرار بشري |
| 15 | لا تراجُع أُسِّي (backoff) بين محاولات التسليم؛ الحدّ الأقصى والعقد الزمني ثابتان افتراضيًّا | **جديدة في 1K** |
| 16 | الصادر غير مفروض كمسار وحيد للأثر الخارجي | **أُغلِقت على مسار حدّ الإنفاذ (1M)**: لا سبيل لتمرير أثر خارجي إلى مُطبِّق المُنادي. وتبقى مفتوحةً لكود خارج `core/sovereignty` ينادي مزوّدًا مباشرةً |
| 17 | الحرس الساكن (1M) غير مربوط ببوّابة CI — ولا جولة CI مُشاهَدة قطُّ | **جديدة في 1M** · قرار مؤسَّسي |
| 18 | المُنادون القائمون لم يُهاجَروا إلى حدّ الإنفاذ | **أُغلِقت في 1N** للمُنادين الإنتاجيّين (`sovereignty_bridge` · `SubordinateGateway` · `engine`). وتبقى `cli.py` و`tools/sovereignty/` خارجَ الهجرة بحدٍّ مُعلَن |
| 19 | لا جدار اختصاص فدرالي عامّ — الجدار القائم قضائي بحصره | **جديدة في 1M** · خارج نطاقها |
| 20 | أسماء 1K (`SovereignOutbox` وأخواتها) غير مُسجَّلة في خريطة تصدير `core/sovereignty/__init__.py` | **جديدة في 1M** · مقصودة: 1K مغلقة فلم تُلمَس |
| 21 | تشغيلُ الانحدار يُولِّد `royal/authority/CONSUMED_PERMITS.json` في الشجرة فيُسقِط `check_repository_identity` حتى يُمسَح. أُضيف استثناءُ `.gitignore` فلا يُدفَع، لكنّ **تكوينَه** لم يُمنَع والموضعُ الافتراضيُّ في `core/sovereignty/enforcement.py` لم يُنقَل | **جديدة في 1N** · نقلُ الموضع = تغييرُ عقدِ مرحلةٍ مغلقة (1G) · قرار بشري |
| 22 | تبعيّاتُ الحزمة الفدراليّة غير مُعلَنة في المستودع بما يجعل انحدارَها قابلًا للتكرار | **جديدة في 1N** |
| 23 | 19 فشلًا فدراليًّا سابقًا في سلسلة تدقيق الأحداث (`test_audit_canonical_record.py` 15 · `test_common_branches.py::TestEventPublisher` 3 · `test_agent_runtime.py::test_worker_skips_unknown_tool` 1) | **سابقة لـ1N** · قِيست لأوّل مرّة في 1N · خارج نطاقها فلم تُلمَس |
| 24 | لا حرسَ ساكنًا يمنعُ النداءَ المباشرَ للبوّابة؛ المنعُ باختبارِ حرسِ انحدارٍ مُعلَنِ النطاق فقط | **جديدة في 1N** · مقصودة (حرسٌ يستلزم إعفاءاتٍ مُهندَسة = حرسٌ ليَخضَرّ) |

## Human Decisions Required — قرارات لا تُتَّخذ آليًّا

| # | القرار | ما يتوقّف عليه |
|---|---|---|
| 1 | **تنسيب التاج** — التاج `unprovisioned` بانتظار فعل بشري (1C) | كلّ ما يعتمد على المرجعية الملكية |
| 2 | **هل يُلزَم القرار الملكي بمرتكز تدقيقي؟** نفّذت 1J الاختيار الأقلّ تدخُّلًا: يُنفَّذ ولا يُصدَّق (`RECOVERY_REQUIRED`). البديل منعُ الفعل حتى يُثبَت — وهو حَجر على التاج لا تحسمه الهندسة | الدلالة النهائية للتنفيذ السيادي بلا إثبات |
| 3 | **تثبيت نطاق 1K** | **مُحسَم 2026-08-18 — 1K = Outbox** (`MASTER_PLAN § 4` صفّ 4). أُغلِق |
| 4 | 8 قرارات دستورية معلّقة (`RECON-002` وأخواتها) | إغلاق المصالحة الدستورية |
| 5 | **حسم الأثر الغامض لدى مزوّد غير متفرِّد** (استعلام · تسوية يدوية · قبول خطر المضاعفة) — لا تحسمه الهندسة | تسوية `INDETERMINATE` آليًّا |
| 6 | **هل يُلزَم كل أثر خارجي بالمرور من الصادر في كامل المستودع؟** أُلزِم على مسار حدّ الإنفاذ وداخل `core/sovereignty` ساكنًا (1M). توسيعه إلى المستودع كلّه أثر معماري واسع | فرض 1K خارج النطاق المحروس |
| 7 | **من يفتح صندوق الموتى؟** لا وليّ أمر مؤسَّسيًّا لـ`DEAD` | تصريف الآثار الميتة |
| 8 | **هل يُهاجَر المُنادون القائمون إلى `SovereignExecutionBoundary`؟** | **مُحسَم 2026-08-18 — نعم، تُهاجَر** (`MASTER_PLAN § 4` صفّ 6). نُفِّذ في 1N. أُغلِق |
| 9 | **هل يصبح الحرس الساكن بوّابة مانعة في CI؟ ولمن سلطة الإعفاء؟** | منع التراجع المعماري مستقبلًا |
| 10 | **هل يُبنى جدار اختصاص فدرالي عامّ بجانب القضائي، وفي أيّ مرحلة؟** | فرض الاختصاص على الأفعال غير القضائية |
| 11 | **أدواتُ الإثباتِ والعرض (`cli.py` · `tools/sovereignty/`): تُهاجَر إلى الحدّ، أم تُعلَن رسميًّا غيرَ مسارِ تنفيذٍ وتُوثَّق كذلك للأبد؟** الهجرةُ تُغيِّر سلوكَ أدواتِ عرضٍ يقرأُها البشر، والإعلانُ يُثبِّت استثناءً دائمًا | إغلاق آخر نداءٍ مباشرٍ للبوّابة |
| 12 | **هل تُعلَن تبعيّاتُ الحزمة الفدراليّة في المستودع ويُشغَّل انحدارُها في بوّابة آليّة؟** (يتّصل بالقرار #9) | قابليّةُ تكرارِ الانحدار الفدراليّ |
| 13 | **هل يُنقَل الموضعُ الافتراضيُّ لسجلِّ الأذون المُستهلَكة (1G) خارجَ الشجرة المُتعقَّبة؟** تغييرُ عقدٍ في مرحلةٍ **مغلقة** — لا يُفتَح بقرارِ مُنفِّذ | نظافةُ الشجرة بعد كلِّ تشغيلِ انحدار |

## Dependencies — الاعتماديات

| الطبقة | الاعتماد |
|---|---|
| 1I ← 1E `contract.py` | قراءة: `ExecutionContract` · `SovereignEffect` · `EffectKind` · `in_scope` — **لم يُعدَّل** |
| 1I ← 1H `idempotency.py` | تنفيذ: `IdempotencyGuard` · `IdempotencyKey` · `OperationStatus` — **لم يُعدَّل** |
| 1I ⇸ 1F `enforcement.py` · 1G `jurisdiction.py` · `gateway.py` | **لا اعتماد** — مُثبَت باختبار (اتجاه الاعتماد أحادي) |
| 1J ← 1B `constitutional_engine` | قراءة: `Verdict.ledger_entry_hash` مرتكزًا تدقيقيًّا — **لم يُعدَّل** |
| 1J → `gateway.py` | **مُعدَّل**: دلالة السجلّ ونقطة الفرض `_run_and_seal` |
| 1J ⇸ 1H `idempotency.py` · 1I `compensation.py` | **لا تعديل ولا نسخ** — التوافق مقيس باختبارات |
| 1K ← 1E `contract.py` | قراءة: `SovereignEffect` · `EffectKind.EXTERNAL` · `in_scope` — **لم يُعدَّل** |
| 1K ← 1F `enforcement.py` | قراءة: `EnforcementPermit` (تحقُّق توقيع بمفتاح عامّ فقط) — **لم يُعدَّل** |
| 1K ← 1H `idempotency.py` | قراءة: `IdempotencyLedger` · `OperationStatus` — **لم يُعدَّل** |
| 1K ⇸ 1I · 1J · `gateway.py` · المحرّك الدستوري · التاج | **لا اعتماد ولا تعديل** — مقيس باختبار سطور الاستيراد |
| 1M ← 1E · 1F · 1G · 1H · 1I · 1J · 1K | **استدعاء فقط، ولا تعديل ولا نسخ منطق** — `bind_contract` · `PolicyEnforcementPoint` (تحقُّق بمفتاح عامّ فقط) · `WALL` · `IdempotencyGuard` · `bind_compensation_plan` · `gateway.decide` · `enqueue_write_ahead` |
| 1M → `core/sovereignty/__init__.py` | **مُعدَّل**: تسجيل أسماء الوحدة الجديدة في خريطة التصدير المتأخّرة فقط |
| 1M ⇸ `gateway.py` · المحرّك الدستوري · التاج · الدستور | **لا تعديل** — 1M لم تُعدِّل ملفَّ حرسٍ واحدًا |
| فرض 1F/1G/1H/1I/1J/1K على المُنادين القائمين | يعتمد على **قرار بشري بالهجرة** — الحدّ بُنيَ ولم تُنفَّذ هجرة |

## الخدمات الخارجية

| الخدمة | الحالة |
|---|---|
| GitHub (`origin/main`) | متصل عبر token |
| Supabase REST API | متصل ويستجيب — لا جداول مشروع شائعة مُتاحَة (PGRST205) |
| Supabase DB المباشر | غير متاح من الساندبوكس (IPv6 فقط، الساندبوكس لا يدعمه) |

## القواعد الحاكمة

1. **لا تبدأ من الصفر** — المشروع قائم، الوثائق معتمدة، لا تُعِد فحص ما تم إثباته.
2. **MASTER_PLAN هو المرجع التنفيذي الأول؛ وهذا الملف مصدر الحالة الرسمي.**
3. **المرجعية الملكية عليا** — لا سلطة فوق التاج (المادة العاشرة).
4. **الاختبارات وسيلة إثبات، ليست المنتج النهائي.** إنشاء ملفّ أو صنف ليس إنجازًا؛
   الإنجاز قدرة جديدة مُثبَتة.
5. **لا تخترع قرارًا سياسيًّا — سجّل: HUMAN DECISION REQUIRED.**
6. **لا تُطارَد التغطية ولا تُغيَّر العتَبات لتخضير النتائج.**
7. **سير العمل:** READ STATE → IDENTIFY NEXT STAGE → TARGETED VALIDATION → BUILD →
   PROVE → DOCUMENT → HANDOFF → UPDATE PROJECT STATE → COMMIT → PUSH → STOP.

## الخطوة التالية

**2A مُنجَزة ومُثبَتة على عمليّتينِ إنتاجيّتينِ فقط** (تأسيسُ المؤسسةِ · توزيعُ
الميزانية). وهجرةُ بقيّةِ الخدماتِ **لم تبدأ ولا تبدأُ بقرارِ مُنفِّذ** — وكذلك
تشغيلُ التعويضِ تلقائيًّا عندَ الفشلِ بعدَ وقوعِ الأثر: قرارٌ بشريٌّ يُسجَّلُ في
[`docs/MASTER_PLAN.md § 4`](docs/MASTER_PLAN.md) قبلَ أيِّ بناء. والدَّينُ المُعلَنُ
كلُّه في [`docs/audit/STAGE_2A_HANDOFF.md`](docs/audit/STAGE_2A_HANDOFF.md).

**1N مُنجَزة ومُثبَتة.** والتاليةُ **غير مُثبَتة في أيّ وثيقة معتمدة** — تُقرَأ من
[`docs/MASTER_PLAN.md`](docs/MASTER_PLAN.md) § 3 و§ 4 و
[`docs/audit/PHASE_E_ROADMAP.md`](docs/audit/PHASE_E_ROADMAP.md)، **ولا يُخترَع لها
نطاق هنا**؛ وإلّا فهي **HUMAN DECISION REQUIRED** كما كانت 1J و1K و1M و1N قبل حسمها.

**ما تركته 1N للقرار البشري قبل أيّ توسيع في نطاق الإنفاذ:** حسمُ أدواتِ الإثبات
(#11 — هجرةٌ أم استثناءٌ مُعلَن)، وإعلانُ تبعيّاتِ الحزمة الفدراليّة وبوّابةٌ آليّة
لانحدارها (#12 · #9)، ونقلُ موضعِ سجلِّ الأذون المُستهلَكة وهو عقدُ مرحلةٍ مغلقة
(#13). ولا يُبنى أيٌّ منها بلا قرار.

> **1I = Compensation. 1J = Fail-Closed. 1K = Outbox. 1M = Enforcement Boundary /
> Static Guards. 1N = Enforcement Integration.** خمسةُ نطاقاتٍ مختلفة حُسِمت بقرار
> بشري مُسجَّل في [`docs/MASTER_PLAN.md § 4`](docs/MASTER_PLAN.md). التسليماتُ
> التاريخيّة **لم تُعدَّل**، و1N **لم تُعِد بناءَ أيِّ مرحلةٍ سابقة ولم تُضعِف حرسًا
> ولم تنسخه**.
